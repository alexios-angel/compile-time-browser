// A script in a file of its own, so the packager has to notice a resource it
// was never given the name of and carry it under the name the document used.
function greeting() {
    return 'hello';
}
