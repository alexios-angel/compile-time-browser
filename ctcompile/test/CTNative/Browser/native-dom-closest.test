# Guarded selector results call the public DOM/Style APIs without retaining handles.
# RUN: python3 %S/native_dom_closest.py --translate ctjs-translate --opt ctjs-opt --clang %dom_clang --build %build --include %{monorepo}/ctbrowser/include --work %t --nm %nm
