# A direct catch consumes its node before the caller's document can die.
# RUN: python3 %S/native_dom_caught_node.py --translate ctjs-translate --opt ctjs-opt --clang %dom_clang --build %build --include %{monorepo}/ctbrowser/include --work %t --nm %nm --node %node --reference %native_reference
