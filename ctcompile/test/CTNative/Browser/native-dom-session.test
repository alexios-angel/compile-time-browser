# Gate owned synchronous DOM entries; retained Data keys remain outside this contract.
# RUN: python3 %S/native_dom_session.py --translate ctjs-translate --opt ctjs-opt --clang %dom_clang --build %build --include %{monorepo}/ctbrowser/include --work %t --nm %nm
