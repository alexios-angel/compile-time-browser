# The source document is explicitly bound to a validated element's owning DOM and live Style.
# RUN: python3 %S/native_dom_document.py --translate ctjs-translate --opt ctjs-opt --clang %dom_clang --build %build --include %{monorepo}/ctbrowser/include --work %t --nm %nm
