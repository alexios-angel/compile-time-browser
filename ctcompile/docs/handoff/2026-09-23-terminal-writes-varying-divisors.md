# Writes after terminal iterator-close reads and varying divisors, 2026-09-23

Resumed clean **8d66dca6** and retained source `ce6d790d` from HANDOFF, current00,
the terminal-read-sequences detail and iteration 105's final journal. No dirty
work or unmerged `codex-wip-20260907` remained; unrelated branches were preserved.
Source, raw-host and escape workflows ran in parallel. All three agents saved
implementation results before model rate limits. The parent completed integration,
source formatting repair, validation and review; independent review did not finish.

## Landed

**c00c569d** removes the terminal-selector restriction from the existing final
`setAttribute` lookup. Existing ordered cloning, exact per-write suppression,
complete operand-use checks and typed DOM/Style reproof handle the later writes.
Unused reads remain unused; their results cannot feed later writes yet. Every
receiver, literal name, selector and primitive value still needs proof. Original
source order/guard, private callable/capture/frame checks, saved Boolean ownership
and proof/expansion budgets remain. No runtime fallback or platform copy was added.

The original source is unchanged, SHA-256
`ce6d790dd97fe5468d695afed17594278f2cdcd2cd9ac5f0c0955c57dfdfa1e7`.
Getter `95e69eec` and reordered-read `3d17d379` execute too. The fixture checks
false/true saved exceptions, normal completion, exhaustion, repeated calls,
separate documents and owned sessions. Raw tests retain direct-call admission,
exact operation counts/order, typed refusals, observed-result/method-reuse
refusals and exact/one-less expansion budgets. One historical raw late-write
refusal is promoted without changing its body.

**8a013b5e** extends existing whole-key subdivision to bounded varying division
and remainder divisors. No arithmetic transfer changes: each accepted subrange
uses the existing exact singleton divisor proof, preserving operand order,
negative divisors and zero/fractional refusal. Every producer/receiver reload and
later store remains in the complete census. Reload gaps require independent
reproof, full index bounds are restored, and actual writes determine retention.
The existing every-prefix contents/retention budget harness covers the new cases.

Twelve new raw and ten source controls cover exact correlated quotients,
remainder, negative divisors, saved/retained children, producer reload gaps,
later stores, overlap, interior zero divisors and fractional keys. All 235
historical source bodies/CHECKs remain unchanged. Two historical raw refusals
now admit: exact Node witnesses show writes `0,2,0`, reads `0,x,0`, unchanged
own keys and retained original child identity. Historical source 187 therefore
correctly keeps `escapes:stored`.

## Focused validation

- Exact `ctcompile_host_contract`: **1/1 PASS, 2.52 s test / 2.53 s total**.
- Three selected saved-throw sources: **48 native executions, 76 refusals and
  24 Node/VM observations PASS**, both providers and optimization policies,
  explicit/deduced C++, GCC and Clang. The selector runs the committed fixture.
- Admission preflight: three positives and ten refusals under both policies.
- Exact `ctcompile_escape_analysis_arrays`: **1/1 PASS, 2.49 s test / 2.50 s total**.
- Exact `Analysis/Escape/escape-claims/power-index-overwrite.test`:
  **1/1 PASS, 0.16 s**, with 355 other lit cases excluded.
- `tools/format.sh --check`: **1126 C++, 157 Python, 114 web files PASS**;
  scoped formatting, Python/Node syntax, `git diff --check` and scratch gate
  `bash -n` checks pass. No code changed after the full formatting pass.
- All **seven final code/test hashes match the devbox**. All **24 generated C++
  files** preserve both selectors, terminal reads then the late write, and the
  preceding read's Boolean passed directly to its earlier write. No Script/VM
  or nullable-scalar fallback appears; fixture standalone and linked checks pass.
- Source preservation retains **233 historical iterator bodies, 86 metadata
  rows, 66 saved cases, 122 retained saved refusal bodies and 66 historical
  oracle constructions**. Twelve positive and twenty refusal local Node
  observations pass; historical oracles were compared without reexecution.
  Ten new escape source output/own-key/identity witnesses and the promoted raw
  witness pass locally; all 245 escape source functions parse.

The draft source fixture was initially formatted outside the repository with
Black's default width. Reformatting alone retained added trailing commas; the
parent regenerated it from the untouched baseline and applied the explicit
repository config. The final diff contains only intended changes. All focused
devbox gates passed on their first run; no production correction was needed.

Builds and tests used `tools/remote-build.sh` with explicit targets under
`/tmp/ctbrowser-devbox-build.lock`. The host gate built `ctjs-opt`,
`ctjs-translate`, `ctcompile-test-host-contract` and `ctcompile-test-native-reference`.
The escape gate built `ctjs-opt`, `ctcompile-test-escape-analysis-arrays`,
`ctcompile-test-escape-claims` and `ctcompile-test-type-oracle`.
Commands, selectors, witnesses, generated artifacts, logs and checksums are in
`../test-results/2026-09-23-terminal-writes-varying-divisors/` relative to the monorepo.

Initial Linux/Windows checks inspected **14/344** accessible processes; the final
check inspected **19/350**. No actual executable/CLI/loop matches were found,
but root-owned Linux executable links were inaccessible, so Claude status was
**uncertain**. Concurrent-agent area rules remained in force. No browser/shared
implementation edits, local C++ builds, pushes or history rewrites were made.

Full CTest/compiler lit, complete iterator fixtures, unaffected native replays,
broad corpus/matrices, full Bootstrap, browser/WPT/test262, Windows and sanitizer
runs were skipped under the focused policy. No full-Bootstrap gain is claimed.

## Next boundary

Retained `unsupported-terminal-read-write-value`, SHA-256
`b1b672877691164ce46b37d674f4adba1dc17c7c93543fcf9cbabaf85992212f`,
refuses **DOM protected helper needs an independent inert-body proof** under both
policies. It passes the final `hasAttribute('data-unvisited')` Boolean directly
to `setAttribute('data-after-terminal', ...)`. Preserve that value and call order
while the original body exception wins; the current unused-read census refuses it.

Single-write selector cleanup, nonterminal exceptional state, multiple protected
regions, implicit cleanup, nested custom iterators, unguarded Bootstrap defaults,
the application driver, full native Bootstrap and general powers remain unfinished.
Wider arithmetic proofs retain the existing work limit.
