# Class/public-family preparation and power indices, 2026-09-21 UTC

Continued clean `ae517a4b` from HANDOFF and the current plan journal. Both commit
histories and unmerged branches were read; `codex-wip-20260907` is already an
ancestor. There was no unfinished code at entry. Linux cmdline/comm inspection
completed for 75 processes and Windows CIM for 350, without a Claude executable,
Node CLI or loop match. No browser or shared implementation was edited.

## Changes

`87789ef2` composes remaining DOM input uses with a completed public
captured-Map family. Every actual input operand must be an exact outer-key
argument of a checked family call. The same proof retains the sole published
root slot, its complete table-use census and the exact wrapper/factory call
chain. Only fully checked family bodies receive source-effect authority;
wrapper and factory bodies still pass the complete class source census. Root
and alias reads require their sole initialization to precede them, including
otherwise inactive source arms. Analysis shares the preparation work budget.
These facts remain separate from rewrite-owned class/helper sets.

Three preparation witnesses preserve the complete vendor Data declaration,
class computations, two record constructions and two local child Maps alongside
the public family's Map and all eight remaining source functions. The class
result is separately observable; the public Map receives an independently proved
Number literal. Holder, constructor and DOM-alias variants prepare. Property,
coercion, capture, wrapper/factory effects, unchecked sibling bodies, extracted
methods, root/Map reassignment and early root reads still refuse. Missing proof
inputs, stale fingerprints and work-budget controls refuse without partial IR.

The original class-result publication witnesses remain unchanged and still
refuse. Preparation does not establish native class ownership: neither the new
prepared modules nor the original result witnesses emit a native DOM session.

`b3cf5e4c` admits one power operation on two direct non-NaN Number literals as a
UTF-16 `charAt`/`slice` bound. Existing native exponentiation supplies JavaScript
Number semantics, including a unit base with infinite exponent producing NaN.
The bound proof handles the same case and keeps the complete bounds-use census;
computed zero/one does not grant literal-only casing authority. The old power
refusal body executes unchanged. Unicode, overflow/underflow, fractional indices
and captured descriptions are covered. All 110 earlier complete intrinsic cases
and 33 general refusal sources are preserved.

## Focused validation

The explicit devbox targets were `ctjs-opt`, `ctjs-translate`,
`ctcompile-test-host-contract` and `ctcompile-test-native-reference`, under the
shared build lock. The first build had no work. An interrupted build did not
finish; the following build found const access to MLIR operation wrappers in the
new file. The accessors were corrected and subsequent builds passed. The final
source synchronization had no work.

- Exact CTest `ctcompile_host_contract`: 1/1, 0.56 s, 0.57 s total.
- Selected lit `CTNative/Browser/native-class-dom-data.test`: PASS, 101.79 s;
  `CTNative/HostContract/dom-data-inputs.test`: PASS, 1.08 s. Total 101.80 s.
  The existing DOM fixture records 56 native record and 24 object-key executions,
  four older preparations, 30 preparation and eight incomplete-session refusals,
  plus nine published source observations and 54 published refusals.
- All eight final code/test hashes match the devbox.
- New family selector: five Node/VM source observations, three preparations and
  29 refusals. No native class execution is claimed.
- Power selector: 40 native executions, 60 related proof refusals, three Node/VM
  agreements and one known UTF-16/byte-index difference. GCC/Clang, both printing
  modes and both optimization policies were exercised, with native symbol gates.
- Scoped formatting and Python syntax checks pass. Required repository
  `tools/format.sh --check` retains 16 pre-existing diagnostics in untouched
  `ctdrive.cpp`, `HostContract/ProviderPaths.h`, `PartialEvaluation/Heap.h` and
  `Symbolic/Facts.cpp`.

Read-only review found one tightening opportunity in root-load ordering; the
final code and early-root refusal cover it. Final review has no remaining
findings. The String agent supplied its complete saved implementation and focused
selector. Service interruptions prevented the fixture agent from saving a draft;
root completed those two files.

No full CTest/compiler lit, full intrinsic-export replay, broad corpus/matrix,
full Bootstrap, WPT/test262, Windows, new sanitizers, local native build or push
ran. Existing String executable mutation tests were skipped by the selector.

Evidence: `/tmp/ctcompile-class-family-build{1,2,3,4}.log`,
`/tmp/ctcompile-class-family-probe.log`, `/tmp/ctcompile-class-family-host.log`,
`/tmp/ctcompile-class-family-gate.log`, `/tmp/ctcompile-class-family-focused.log`,
`/tmp/ctcompile-class-family-lit.log`, `/tmp/ctcompile-class-family-format-final.log`
and `/tmp/ctcompile-string-power-focused.py`. The selected lit report is
`/tmp/ctcompile-class-family-final.json` on the devbox.

## Next boundary

Feed the actual class-derived scalar to the public family. Its source category
proof currently handles exact fresh records, not the retained constructor/local
Map result. Reuse the existing constructor/record proof with its original
allocation and read-time identity. Then compose native ownership: the provider
source census and `OwnedGlobalRoots::analyzeMethodTable` still require the exact
constructor, local Map, function and allocation proofs. Do not whitelist
constructors or remove unused owners to get past these gates.

The new `published_families()` supplies the completed public-family preparation
shape; unchanged `published_source()` supplies the next actual-result witnesses.
Multiple DOM-input alias partitions in local class Maps, original B/Data+B,
broader String methods/indices, conditional callees, mutable cells, String
ordering, document views and the application driver remain. No full-Bootstrap
admission or corpus coverage gain is claimed.
