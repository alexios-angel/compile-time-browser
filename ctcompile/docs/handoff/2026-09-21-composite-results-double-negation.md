# Native Bootstrap results and browser prototype calls, 2026-09-21 UTC

Continued clean `d74968ab` and its recorded composite-result preparation refusal.
There was no dirty predecessor code; `codex-wip-20260907` was already an ancestor.
Standing instructions, sync protocol/claims/journal, plan 00/24/25 and both
compiler/browser histories were read. Initial Linux executable-link inspection
was permission-limited; a complete follow-up inspected 69 cmdline/comm records
without errors, and Windows CIM inspected 353 processes. Neither found Claude.
No browser or shared implementation was edited.

Three agents supplied String work, composite tests and read-only proof advice.
Root finished their saved drafts after service errors. A later user interruption
resumed this same work and emphasized integrating ctbrowser through C++ types.

## Landed integration

`58803194` admits publication of the complete original class-derived result
through the native DOM Data session. The unchanged holder and constructor
sources produce 15927 and 59112. Preparation retains five constructions, eight
functions and the complete vendor Data declaration. Existing Map-operation
evidence supplies only has/delete/clear/size categories. A postorder walk reads
both read-only branch arms; the existing alias census prohibits nested writes.
The shared category query accepts Number/Boolean arithmetic and primitive strict
equality without evaluating results or selecting source branches.

Three previous constructor-field refusals also execute unchanged: Boolean
arithmetic, repeated Boolean initialization and a saved Boolean read followed
by a Number write. Each publishes Number 2 while independently observing the
original class result 15927. The added complete-source Map observation witness
publishes 11115927 after observing has, delete and clear. Generic get results
gain no scalar category; present-record origins still come from the existing
Map/constructor proof. Provider and native ownership proofs remain independent.
Object-valued field mutation still lacks the required primitive owner proof.

`95ea1b19` connects JavaScript `Element.prototype.matches.call` and
`Element.prototype.closest.call` to the existing native C++ method objects:

```cpp
ctnative::Element.prototype.matches.call(element, styles, selector);
ctnative::Element.prototype.closest.call(element, styles, selector);
```

These methods call public ctbrowser DOM/Style. The compiler preserves the initial
Element/Function identities, exact receiver, String selector and complete source
census. Matches returns the native Boolean type; closest returns a nullable
element borrow. Existing document/node validation, Style association and session
ownership remain in force. No selector algorithm or runtime layer was added.

`616ea0f6` separately admits two unary negations of exact Number/null/Boolean
String indices. Four complete former refusal sources execute unchanged.
Intermediate-use accounting, two arithmetic levels, budgets, fingerprints,
UTF-16 indexing and literal-only casing authority remain required. All 161
earlier complete String cases are unchanged.

## Focused validation

All builds used explicit devbox targets under the shared build lock:
ctjs-opt, ctjs-translate, ctcompile-test-host-contract and, for the composite
and String checks, ctcompile-test-native-reference.

- Composite selector: three Node/VM observations, three preparations, three
  provider proofs, 24 native executions and 40 refusals. Controls include ten
  hostile sources, six manifest/budget failures, six altered prepared-IR
  refusals, twelve native forged-proof refusals and six raw-source refusals.
- Final constructor selector: three Node/VM observations, three preparations,
  three provider proofs, 24 native executions and six source refusals. It selects
  the three unchanged promoted Boolean-arithmetic bodies from the registered
  fixture, retaining its executable and source checks.
- Exact ctcompile_host_contract passed before the prototype increment in
  0.55 s (0.56 s total), and afterward in 0.59 s (0.60 s total).
- CTNative/HostContract/dom-data-inputs.test and
  CTNative/HostContract/Provider/objects.test passed in the initial three-case
  lit selection. The remaining native-class-dom-data.test failed after
  completing its earlier groups because Boolean arithmetic was still listed
  as a refusal. Three exact source bodies were moved to execution cases.
- The corrected whole native-class-dom-data.test replay lost its local SSH
  result stream during the user interruption. Its remote descendants continued;
  root stopped only that task tree under the build lock and ran the final focused
  constructor selector. No final whole native-class-DOM pass or aggregate
  whole-case execution count is claimed.
- CTNative/Browser/native-dom-prototype-query.test passed in 167.72 s:
  64 native executions and 100 refusals across four selector methods. Tests use
  borrowed and owned sessions, both native policies, explicit/deduced printing
  and GCC/Clang. Dynamic hover state, detached/shadow trees, nullable results,
  cross-document identity and mismatched Style associations are exercised.
  Generated source and linked symbols remain free of Script/AOT dependencies.
- The String selector passed 64 native executions, 148 proof refusals, six
  Node/VM agreements and one retained byte-index/UTF-16 difference.
- Final code/test hashes match the devbox. Scoped C++/Python formatting and
  Python syntax checks pass. Required tools/format.sh --check reports the same
  16 untouched diagnostics in ctdrive.cpp, ProviderPaths.h,
  PartialEvaluation/Heap.h and Symbolic/Facts.cpp.

The first composite selector stopped after successful holder native execution
because a new test regex omitted the prepared entry's `private` marker. This
was corrected before its final pass. One combined build shell consumed trailing
commands through stdin; only the separately executed tests above count.
There was no C++ build failure. No full CTest/compiler lit, full intrinsic replay,
broad corpus/matrix, full Bootstrap, WPT/test262, Windows, new sanitizers, local
native build or push ran. String executable mutations were skipped.

Evidence lives in /tmp/ctcompile-composite-{build1,build2,build3,build4,build5}.log,
/tmp/ctcompile-composite-focused2.log,
/tmp/ctcompile-composite-constructor-focused.log,
/tmp/ctcompile-composite-ctest.log, /tmp/ctcompile-composite-lit.log,
/tmp/ctcompile-composite-lit-final.log (interrupted),
/tmp/ctcompile-double-negated-focused.log,
/tmp/ctcompile-prototype-selectors-{build,lit,ctest}.log and
/tmp/ctcompile-final-format.log. Focused Python runners remain in /tmp.
The successful prototype lit log is also preserved on the devbox.

## Next boundary

The existing two-input holder/constructor sources are still guarded by
`class nested Map DOM inputs require an alias proof` in
ClassInitialization/CapturedMapHelpers.cpp. Its single elementKey currently
rejects a second distinct formal. Different formals may denote the same DOM
node: any extension must account for both equal and distinct input partitions,
including retained child Maps, missing reads, replacements and cleanup.
Do not assume formal identity implies node inequality.

For the user's browser-integration direction, the planned typed document and
element views can reuse these public APIs and carry the existing Style association.
The facade alone supplies no source `document` binding, root-nullability proof
or native application driver. Those remain unfinished, along with original
B/Data+B, object-field ownership, tagged Map snapshots, broader Strings,
conditional callees, mutable cells and String ordering. This work establishes
neither full Bootstrap admission nor new corpus coverage.
