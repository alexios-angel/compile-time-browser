# Completed record Map preparation and remainder bands — 2026-09-20 UTC

Continued clean `d5d8a71c` and its post-construction record retention boundary.
The September 7 WIP is already an ancestor of `ctcompile-v1`; no unmerged rescue
or dirty predecessor work remained. Three agents handled source proof, fixtures
and escape analysis. The source agent hit a rate limit after writing its draft;
the root finished those same files, reviewed them and ran the focused gates.

Linux checked 12 readable process identities with no Claude match and 56
unreadable identities. Windows Get-CimInstance checked 346 processes with no
match after a quoting-error retry. Availability remained uncertain; concurrent
restrictions applied. No browser, shared code, runtime-oracle edits or push.

## Landed

- `77e02a65` admits source preparation for completed class instances stored in
  a direct local Map. The existing standard Map identity and complete use proof
  remain required. Every payload is a completed construction from the same
  constructor family in the declaring function's entry block. Literal String
  keys, ordered set/delete/clear effects and present gets are checked. Saved
  get results undergo the receiver-use census even after overwrite/deletion.
- Six new sources prepare: direct registration, replacement, deletion, clear,
  distinct payloads and writes through saved aliases. Every original Map call
  and construction survives preparation. This is **preparation only**, with
  **zero new native executions**: both optimization modes still refuse these
  six programs at the constructor lifter's closed-shape proof.
- Fifteen negative sources retain missing-read, region-owner, mixed-producer,
  dynamic-key, escape, intrinsic-replacement, constructor-publication and
  saved-alias selector boundaries. A stored receiver cannot use an already
  folded own-key snapshot: that earlier proof has not seen Map aliases and
  could miss a newly added field. Constructor/method selectors on saved aliases
  remain refused. Repeated receiver censuses are not cached across changing
  inherited method sets. Every scan spends the existing work budget.
- `5e69b712` keeps remainder endpoints and stride when the input enclosure stays
  within one truncation-quotient band. The existing bounded arithmetic helper
  handles both signs. Crossing a remainder wrap retains the dense fallback.
  Complete reload checks, exact replay, valid final indices and work/depth
  limits remain unchanged. New controls cover translated bands, stride gaps,
  divisor reloads, later writes, wrap boundaries and saved children.

Original class fixture files 01–25 and all 33 existing remainder JavaScript
function bodies are unchanged. Original B/Data+B still refuse constructor
receiver observation at registration. No record pointer carrier, class owner
graph, runtime helper or Script dependency was added.

## Focused validation

Evidence: `/tmp/ctcompile-record-maps-1035/`. All devbox builds and checks held
`/tmp/ctbrowser-devbox-build.lock`.

- Explicit targets: `ctjs-opt`, `ctjs-translate`, `ctcompile-test-host-contract`,
  `ctcompile-test-native-reference`, `ctcompile-test-escape-analysis-arrays`,
  `ctcompile-test-escape-claims`, `ctcompile-test-type-oracle`.
- Exact `ctcompile_host_contract`: **1/1**, 0.48s. Exact
  `ctcompile_escape_analysis_arrays`: **1/1**, 1.51s.
- Lowering lit `object-argument-lift.mlir`, `object-argument-refusals.mlir`,
  `constructor-refusals.mlir`: **3/3**, 4.23s.
- Escape lit `remainder-index-overwrite.test`, `composed-index-overwrite.test`,
  `bitand-index-overwrite.test`: **3/3**, 0.15s.
- Selected class driver: **31 observations / 40 existing native executions /
  62 unprepared refusals / 41 preparation refusals / 12 prepared native
  refusals**. The six new cases account for the latter 12. Existing native
  controls cover GCC/Clang, explicit/deduced output and both optimization modes.
  Ancillary controls: **16 constructed-method executions / 20 refusals** and
  **eight original-r executions / four refusals**; not new main-source coverage.
- Complete source-proof cutoff for the overwrite case: **622 steps**. Lower
  budgets emit no partial result; forged annotations, duplicate closures and
  nested source functions retain their checks. All **10 checked-in exact
  record-Map diagnostic assertions** passed after the final test-only edit.
- Existing remainder recording and claims: **141 sites / 27 sound / 27 of 32
  confined precision (84.4%)**, zero violations, partial, pending, unobserved
  claims or unclaimed sites. Five imprecise Stored claims remain.
- All **nine** tested file hashes match. Node checks cover **21 new class
  sources** and **47 remainder sources**; Python AST/Black and changed C++
  formatting pass. Completed `tools/format.sh --check` still reports **20
  pre-existing diagnostics in six unchanged files**; no whole-repo format pass.

One shell gate fed its remaining commands to SSH after building; its log
contained no tests. The checks above ran afterward from a script file and have
explicit result counts. No build failure or runtime-oracle discrepancy occurred.

Skipped: full CTest/compiler lit, whole class-initialization lit, DOM replay,
broad matrices/corpus runs, full Bootstrap, WPT/test262, Windows and sanitizers.
Browser compliance measurements were not remeasured or changed.

## Next boundary

The six `class-map-record-*` positives are in `PREPARED_ONLY`, with their exact
native refusal pinned: `the instance new makes does not have a closed shape`.
`ClosureLifting/Methods.cpp::usesCloseTheShape` rejects the Map.set payload;
`behind` does not connect present Map.get results to class receiver families.
The standard native Map proof currently runs after closure lifting, so its
annotations cannot simply authorize this earlier lift.

Connect a complete live retention proof to closure lifting, Map payload family
and origin evidence, shape/type inference, admission and EmitC. Prove enclosing
entry-frame owners, every producer/call/use and present reads; preserve actual
saved record identities across overwrite/delete. The existing Map templates
can store borrowed record pointers, but the compiler does not yet choose those
types. No shared class ownership or scalar identity substitute is needed.

Source own-key snapshots also need the saved-alias edges before folding; the
current explicit refusal is required until then. Constructor-time `this`
publication needs a separate exception/reentry proof before the original
nested Data Maps can retain it. Preserve conflict checks, `e.set`, `e.remove`,
`P.off` and configuration/disposal bodies. Full Bootstrap and the application
driver remain unfinished.
