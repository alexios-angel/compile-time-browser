# Iterator callable targets per invocation and wrapped left shifts, 2026-09-22

Resumed clean `f6496ace` and the exact differing-target source recorded in HANDOFF,
plan 00 and iteration 61's journal. The preceding interrupted session had finished
its native and escape commits. `codex-wip-20260907` was already merged; other
branches were preserved. Linux inspection found 14 readable executable identities,
59 permission errors and no Claude match. Windows CIM found no match among 341
processes. Availability remained uncertain, so concurrent-agent rules applied.
No browser, shared implementation or runtime-oracle semantics changed.

## Landed

**cd6d9ab7** replaces the single-target forwarding fixed point with a bounded
proof for each invocation. Exact actual callable identities bind that invocation's
formals and flow through single root returns. Direct targets, receivers, argument
counts and recursion are checked before expansion. Aggregated identities serve
only the complete observer census; unknown actuals, scalar observations and
escapes still refuse. The existing inliner preserves evaluated scalar arguments,
current captured state and ordinary results at each source position. Callable
roots are retired once even when a formal has multiple proved identities.

The saved source, SHA-256
`21eba50b0711b7469fd00929f75b62175bb9763b18faf1d305f4bf9c7bbe22d0`, executes
unchanged: **2729 normally / 3603 on stop**, with `data-closed=false`.
The distinct-writer snapshot source returns **166142 / 192687**; the forwarding
source returns **2734 / 3608**. Four raw fixtures pair ordinary/direct calls and require different
writer effects, current cells, prior scalar snapshots, ordered effects, closure
retirement and bounded completion. All 147 historical complete sources, 52 positive
expectation tuples and both native assertion strings remain intact. The historical
nonprogressing source remains compile-only and was never executed.

**9f01bba4** encloses larger left shifts across input conversion and output wrap
boundaries in the full signed result interval with a proved residue and stride.
The period divides both the shifted input step and 2^32, so every wrapped result
belongs to the enclosure. Exact two-point and affine paths remain unchanged.
Complete reload/store checks still run; actual-write replay retains unwritten
children and earlier snapshots. Paired CFG/SCF tests cover conversion jumps,
interior extrema with equal endpoint images, masked/negative counts, constant
residues and hostile overlapping reloads or later stores. Thirteen new source
functions extend all 62 historical left-shift bodies and call tails unchanged.

Three agents worked on source tests, raw tests and escape analysis. The escape
agent hit a rate limit before final validation; the parent finished that same
four-file draft. The raw agent hit a rate limit after its corrected fixture freeze;
the parent completed its focused gate and final fixture correction.

## Focused validation

| Check | Result |
| --- | --- |
| Explicit affected devbox builds | PASS |
| Three new admissions and all 100 refusal preflights | PASS |
| Seven-positive custom subset, retaining every refusal | 112 native executions, 368 refusals, two nonexecuted admissions, 56 Node/VM observations |
| Exact `ctcompile_host_contract` | 1/1 PASS; 1.45 s test / 1.46 s total |
| Exact `ctcompile_escape_analysis_arrays` | 1/1 PASS; 1.97 s test / 1.98 s total |
| Left/right-shift index-overwrite lit | 2/2 PASS; 0.13 s total |

The custom subset selects the three new differing-target sources, three preceding
returned-callable sources and the callable-argument snapshot source. It covers
borrowed/owned DOM, both optimization policies, explicit/deduced C++ and GCC/Clang.
The complete custom case was not replayed.

The first host run failed at fixture parsing: the new second writer used the
unsupported `binary_static sub` operation. Its replacement with `bitxor` parsed
and passed normalization, refusal and budget checks, but failed eight complete
DOM reproof assertions because this typed DOM path does not admit that operation.
The final fixture uses a doubled-step addition and explicitly checks that extra
operation. It preserves the distinct-target test and every historical body.
Production was unchanged through all three runs. The failed runs took 0.67 s and
1.47 s total; neither is reported as a pass.

The left-shift oracle observes **225 sites, 49 sound confined sites and 49/53
confined precision**; right-shift observes **252 sites, 54 sound and 54/63
precision**. Both have zero violations, unclaimed, partial or pending sites.
Local checks pass: 156 native-source syntax checks, 40 exact Node traces,
75-function left-shift execution and all 13 exact new results.

All seven final code/test hashes match local/devbox files. The snapshot C++ was
inspected: scalar state and public DOM calls remain. All 56 generated C++ files
pass the Script/VM iterator-symbol check. The idle timer is active/enabled.
Every devbox command held `/tmp/ctbrowser-devbox-build.lock`. Build targets were:

```text
ctjs-opt ctjs-translate ctcompile-tool ctcompile-test-native-reference
ctcompile-test-host-contract ctcompile-test-escape-analysis-arrays
ctcompile-test-escape-claims ctcompile-test-type-oracle
```

CTest used exact anchored names with `--output-on-failure --no-tests=error`.
Lit used `build/ctcompile/test` and the exact filter
`^ctcompile :: Analysis/Escape/escape-claims/(left-shift|right-shift)-index-overwrite[.]test$`.
Commands, logs, hashes, sources, generated C++ and next-boundary stderr are in
`../test-results/2026-09-22-iterator-callable-targets/`.

Required `tools/format.sh --check` retains sixteen existing diagnostics in
`ctbrowser/tools/ctdrive/ctdrive.cpp`,
`ctcompile/lib/CTNative/HostContract/ProviderPaths.h`,
`ctcompile/lib/CTNative/PartialEvaluation/Heap.h` and
`ctcompile/lib/CTNative/Symbolic/Facts.cpp`. Changed C++ passes the pinned formatter;
Python passes Black/AST, and diff whitespace and temporary shell syntax pass.
Documentation needs no build or CTest.

No full CTest/compiler lit, complete custom case, unchanged nested/dataset case,
broad corpus/native matrix, WPT/test262, Windows, additional sanitizer, local C++
build or push ran. No full-Bootstrap admission or vendor coverage gain is claimed.

## Exact next boundary

`entry-captured-sibling-returned-callable-branch-join`, SHA-256
`5936d99329018b5c6edbfee9451e9965edbe4cb3da26aacaf62dea660347cb6a`, selects
between two known writers according to captured state before returning the selected
callable. Node returns **2729 normally / 3603 on stop**, with `data-closed=false`.
Both native policies return status 1:

```text
native DOM source: DOM iterator sibling call requires an immutable local helper
```

Both stderr captures are saved; the boundary has not executed natively.
Next prove callable identities through branch/loop joins without losing effects,
argument snapshots, complete observers or current shared state. Nested custom
opens, abrupt iterator close, literal range-for printing, unguarded Bootstrap
defaults and the application driver remain unfinished. Part 25 retains mixed
sparse-gap unions and precision beyond the current enclosures as separate work.
