# Local constructor ownership and ASCII prefixes, 2026-09-21 UTC

Continued clean `0f8628da` and the source-owner census boundary recorded in
`2026-09-21-provider-records-nested-indices.md`. The requested histories,
protocol, claims, journal, HANDOFF and plan 00/24/25 were read.
`codex-wip-20260907` was already an ancestor; no unfinished code was present.
Linux cmdline/comm inspection covered 72 processes without unreadable records;
Windows CIM covered 345. Neither found a Claude executable, CLI or loop.
No browser or shared implementation was changed.

## Complete local source ownership

`32a1ca2f` publishes the existing provider's exact constructor/operation
census only after the whole live HostContractAnalysis succeeds. A fresh owner
query uses these identities to admit distinct constructor functions, local Map
calls, record field reads and primitive arithmetic in read-only branches.
Every field mutation needs a primitive category. Provider analysis still accepts
an unread unknown field where its scalar observations remain valid; ownership
refuses that graph. The retained holder/prototype allocations left by preparation
need a complete census proving no observable use, including Root operand/scope
and dominance checks. Source functions, allocations, aliases and reads remain.

The new C++ query witnesses cover source and lifted public-family forms, with
14 rows each: one complete owner admission, ten provider/owner refusals and
three provider-valid ownership refusals. They check six source functions, three
constructions, three objects and the local graph's 17 operations.
Controls cover replacement constructors, nonliteral actuals, wrong receivers,
Map/record escapes, capture, branch mutation, non-String Map keys, coercing
arithmetic, observable retained allocations, unknown fields and extra functions.
Exact budgets restore both proofs; partial budgets, stale fingerprints and
forged reports expose no partial ownership.

The unchanged complete vendor constructor witness now passes source ownership
and reaches native constructor/type admission in both policies. Its existing
Python fixture pins that progress. Native class-session emission still refuses;
no new class program is claimed to execute natively.

## Concatenated ASCII prefixes

`fc440f29` admits a `startsWith` prefix formed by one addition of two ASCII
String literals. Existing String addition and native prefix matching do the
work. Every literal byte is checked; nested concatenations, nonliteral/coercing
operands and Unicode prefixes retain refusal. Computed prefixes grant no
literal-only dataset membership or transformation authority.

Three new source witnesses cover empty/NUL prefixes, captured Symbol
descriptions, Unicode receivers and reuse through a saved String. The 147
previous complete cases and 33 general refusals remain unchanged.

## Focused validation

All builds used explicit devbox targets under the shared lock:
`ctjs-opt`, `ctjs-translate`, `ctcompile-test-host-contract`,
`ctcompile-test-native-reference` and `ctcompile-test-owned-global-shared-map`.

- Final exact CTests: `ctcompile_owned_global_shared_map` and
  `ctcompile_host_contract`, 2/2, 248.05 s total.
- Final selected lit: `CTNative/Browser/native-class-dom-data.test` and
  `CTNative/HostContract/dom-data-inputs.test`, 2/2, 109.42 s total.
  They retain 56 record and 24 object-key native executions and the existing
  preparation, provider, source-observation and refusal controls.
- After adding the permanent vendor-boundary assertion, the focused constructor
  selector passes nine Node/VM observations, nine preparations, nine provider
  proofs and 33 refusals, plus 16 prepared-IR refusal/report controls. Both native
  policies retain all source functions and refuse at the constructor/type stage.
  This targeted pass is not another whole-DOM-fixture replay.
- An earlier three-case selection also passed
  `CTNative/HostContract/Provider/objects.test` (116.23 s total). An intermediate
  two-DOM selection passed in 108.15 s before inert allocation admission.
- The String selector passes 40 native executions and 76 proof refusals, using
  GCC/Clang, both printing forms and both optimization policies, including output
  and symbol gates. Four Node/VM observations agree. Complete intrinsic replay
  and executable mutations were skipped.
- All nine final code/test hashes match the devbox. Seven C++ and two Python
  files pass scoped formatting/syntax checks. The required repository formatter
  reports the same 16 untouched diagnostics in `ctdrive.cpp`, `ProviderPaths.h`,
  `PartialEvaluation/Heap.h` and `Symbolic/Facts.cpp`.

An interrupted build stopped at 74/103; it was resumed, not counted as a pass.
Two early owner test runs failed in new fixture setup: a selector found a direct
call before the setter definition, and an invented non-report attribute changed
the fingerprint. The final test selects the function definition and forges the
real `ctnative.host_proved` report. The corrected intermediate exact CTests passed
2/2 in 244.64 s. Vendor probes separately identified branch arithmetic and retained
inert allocations; their focused checks were added before the final gate.

No full CTest/compiler lit, broad corpus/matrices, full Bootstrap, WPT/test262,
Windows, new sanitizers, local native build or push ran.

Evidence: `/tmp/ctcompile-owner-retained-{gate,ctest,lit}.log`,
`/tmp/ctcompile-owner-final-source-gate.log`, `/tmp/ctcompile-prefix-native.log`,
`/tmp/ctcompile-string-next-focused.py`,
`/tmp/ctcompile-owner-format-final.log` and
`/tmp/ctcompile-owner-final.sha256`.

## Next native boundary

Both native policies now report `standard Map identity is unproved across an
unknown constructor`, alongside `a closure used as a value - Phase 59`, `uses
this`, unproved capture and caller diagnostics. Explicitly lift the proved
constructors in `ClosureLifting/MethodTables.cpp`, preserving exact callable
provenance and rebuilding the source graph after each rewrite. The generic
constructor guard still rejects Map payload uses, and its rewrite substitutes
an undefined callee while `HostContract/Values.cpp::exactCall` requires the
actual source callable. Do not remove either guard globally.

Original composite-result publication still refuses class preparation.
Multiple DOM-input alias partitions, original B/Data+B, broader Strings,
conditional callees, mutable cells, String ordering, document views and the
application driver remain. No full-Bootstrap admission or corpus coverage gain
is claimed.
