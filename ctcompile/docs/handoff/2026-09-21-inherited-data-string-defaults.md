# Inherited Data publication and omitted String indices, 2026-09-21 UTC

Continued clean `97f32ae8` from its recorded inherited three-argument Data boundary.
No native draft was left uncommitted; `codex-wip-20260907` is already merged. Old unmerged
branches were inspected and retained. The Linux executable/script-path census
found no Claude process (19 readable executable identities); the corrected
Windows CIM census found none among 357 processes. The first PowerShell command
had a quoting error and did not establish availability. A later complete Linux
cmdline/comm census read 73 records with zero denied, and Windows CIM read 354;
neither found Claude. Parallel agents supplied
fixtures, omitted String indices and proof review; root resumed their saved work
across two user interruptions and owned all devbox validation and commits.

`5ab02710` reuses the existing `normalizeSuper` routine
for one unconstructed base and its sole leaf. Both registration keys follow the
original super arguments, and the exact base capture cell transfers to the leaf.
The shared publication-suffix proof permits only literals and arithmetic proved
numeric from every exact construction. The recursive publication check must
actually consume the copied helper call before the original base edge retires.
Captured helper expansion, nested routing and the full class/record owner census
remain required; no source observer or unknown helper receives new authority.

Three new source admissions cover repeated/distinct keys, called Data holder
slots, saved child and record aliases, deletion/recreation, a numeric leaf suffix
and short-circuit child allocation. The original file-45 inherited refusal now
executes unchanged. Fifteen new complete-source refusals cover observations,
throwing/reentrant/coercing suffixes, replacement returns, shared/deeper families,
bad key origins, hidden base/leaf callers, an unused unsafe holder slot, escaped
child ownership and failed helper expansion. The inherited direct witness also
uses the existing ancestry, rooted-super, stale/proof-input and exact-budget
rollback controls. All 45 earlier class source files remain byte-identical.

`19d43e03` admits omitted `charAt()`/`slice()` indices as zero through
the existing UTF-16 path. Explicit undefined, coercing/dynamic arguments and
unproved receiver origins still refuse. Direct-literal first-unit lowercase and
dataset-tail authority stays unchanged. Both prior missing-argument refusal
programs execute unchanged; combined UTF-16 and captured-description checks cover
empty strings, NUL, non-ASCII text and isolated surrogates. Generated output
continues to use ordinary native ownership and public Core functions, with no
Script or VM dependency.

## Focused validation

| Check | Result |
| --- | --- |
| Exact CTest `ctcompile_host_contract` on final emitter | 1/1, 0.58 s; 0.59 s total |
| `CTNative/Lowering/Objects/class-captured-map-helpers.test` | PASS, 341.71 s |
| `CTNative/Lowering/Objects/class-terminal-publication.test` | PASS, 152.26 s |
| `CTNative/Browser/native-dom-strings.test` | PASS, 164.27 s |
| Focused intrinsic-export execution selector, final emitter | PASS, 48 native executions, 564 proof refusals |

These are three distinct lit passes and a targeted export check, not a full-suite
or final whole-export-fixture pass. Captured classes measure **187 source
observations, 392 main native executions, 374 unprepared refusals and 248
preparation refusals**; the new inherited direct budget is **4712**. Four admissions
add 32 executions. The terminal fixture measures 58 observations, 160 executions,
116 unprepared and 70 preparation refusals. Both also run constructed-method
controls (16 executions, 20 refusals) and original helper guards (eight executions,
four refusals). DOM Strings measure 809 observations and eight GCC/Clang binaries,
plus the existing source, provenance, capture, replacement and completion controls.

The first selected class run passed 2/2 in 341.71 s. The next String selection
passed DOM Strings but failed exports before native execution because observation
names crossed 99: the VM sorts globals lexicographically. Sorting only the VM
comparison fixed that test expectation; Node order and Unicode expectations remain
unchanged. That selection finished 1/2 in 164.28 s.

The corrected export-only run failed after 449.17 s, at the first no-argument
witness: GCC `-Werror` caught an unused generated UTF-16 length variable. Its
preceding 81 cases had completed 648 native executions and two executable mutations.
The emitter now creates length only when an explicit index needs it. Following
the focused policy, the final run reused the export harness with a temporary
`standalone` selector for all four `DEFAULT_INDICES` cases plus `string-indices`
and `string-wide-indices`. It ran **48 native executions** across both compilers,
print modes and optimization settings, **564 proof refusals**, and **101 Node/VM
observations: 93 agreements and eight explicit known casing/indexing differences**.
The selector skips the earlier 648-execution replay and executable mutations;
its report counts only executed binaries. No final whole-export pass is claimed.

All builds ran under `/tmp/ctbrowser-devbox-build.lock` with explicit targets
`ctjs-opt`, `ctjs-translate`, `ctcompile-test-host-contract` and
`ctcompile-test-native-reference`. The five builds compiled 10, 6, 3, 0 and 3
steps respectively. The unchanged original file-45 inherited source also passed
preparation. An initial ad-hoc probe used the wrong pass/option spelling; the
corrected command passed. All **11 final code/test SHA-256 hashes** match the
devbox after the emitter correction.

Evidence is in `/tmp/ctcompile-inherited-data-*`: build1 through build5 logs,
class-results, dom-results, local.sha256, remote-hashes-final and format-final2.
The remote lit JSON files are `-first.json`, `-strings.json` and `-exports.json`.
The final selector is `/tmp/ctcompile-string-defaults-focused.py` on both machines;
its remote work directory and log use `/tmp/ctcompile-string-defaults-focused`.
It imports the existing harness, selects only the six named executable cases,
runs every proof control and prints actual execution counts. The final host check
and selector invocation are captured in `-build5.log`.

Local source checks pass 18 new class Node observations and 101 intrinsic Node
observations. Six C++ and four Python files pass scoped formatting/syntax checks.
The required repository formatter retains 16 existing diagnostics in untouched
`ctbrowser/tools/ctdrive/ctdrive.cpp`, `HostContract/ProviderPaths.h`,
`PartialEvaluation/Heap.h` and `Symbolic/Facts.cpp`.
No browser/runtime implementation changed. Full CTest/compiler lit, broad corpus
or native matrices, full Bootstrap, WPT/test262, Windows, new sanitizers, local
native builds and push were skipped.

## Exact next boundary

Next prove child `has`/`size`/`delete` observations for the original Data
holder's guarded remove and empty-parent cleanup. `normalizeNestedMaps` currently
evaluates outer membership/size and requires a present child; compose child
content evidence with the unchanged record-owner census. Retain the complete
unused slots and every exception/reentry observer. Conflict reporting and nullable
lookup remain additional original Data requirements. This increment still requires two
literal String keys and one base-to-leaf family; actual element-identity keys
and DOM entry ownership need the public browser/HostContract seam. Original B
and Data+B remain refused with complete sources. Dynamic/coercing String indices,
NaN origins, broader methods, conditional callees, branch-mutated boxed locals,
String ordering, document views and the application driver remain unfinished.
No full-Bootstrap admission or coverage gain is claimed.
