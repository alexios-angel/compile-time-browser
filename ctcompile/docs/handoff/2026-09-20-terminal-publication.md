# Terminal constructor Map publication and AND input congruence — 2026-09-20 UTC

Continued clean `865e67ec` and the constructor-publication thread in HANDOFF,
plan 00 and the latest journal. No dirty predecessor work or unmerged September
7 rescue remained. Parallel agents supplied the escape draft, publication
witnesses and an independent proof review; the root finished the rate-limited
escape draft, integrated native changes and ran focused gates.

Linux process inspection found 18 readable executable identities without Claude
matches and 56 unreadable identities. Windows Get-CimInstance returned 344
processes without matching Claude identities. Availability remained uncertain;
concurrent restrictions applied. No browser, shared-code or runtime-oracle edits,
no push and no history rewrite.

## Changes

- `05dbf77e` sinks one exact terminal constructor `Map.set(literal, this)`
  immediately after every proved entry-block `new`. The Map is a fixed sole
  capture; the constructor returns undefined and its suffix has no observable
  work. Complete closure and symbol callers, original Map identity and all
  captures are checked. Inheritance, earlier publication, helpers, replacement
  returns, dynamic keys and escapes retain their boundaries.
- The same registration survives at its effect position. A throwing prefix
  skips registration; a throwing registration skips the caller continuation.
  The dead constructor capture is removed, and fixed-cell Map reads join the
  existing saved-alias census. Remaining captures are rejected before folding
  snapshots, including captures in functions not yet examined.
- The two unchanged terminal-publication sources from files 26 and 28 and four
  new positives run natively: direct registration, overwriting a saved record,
  saved-alias disposal and deletion. Records stay in the enclosing stack frame;
  the existing Map holds concrete borrowed pointers. No new runtime carrier,
  class ownership graph or Script/VM/GC dependency is introduced.
- `7497424c` preserves input congruence through AND indices, combining fixed
  input low bits with the mask's trailing zero bits. Existing signed conversion
  guards, exact replay, complete reload/write census and work limits remain.
  Twelve source witnesses and focused CFG/SCF positive/refusal rows were added;
  all 43 prior AND source functions and calls are unchanged.

## Focused validation

Evidence: `/tmp/ctcompile-terminal-publication-1216/`. Devbox builds, tests and
artifact reads held `/tmp/ctbrowser-devbox-build.lock`.

- Explicit devbox targets: `ctjs-opt`, `ctjs-translate`,
  `ctcompile-test-native-reference`, `ctcompile-test-host-contract`,
  `ctcompile-test-escape-analysis-arrays`, `ctcompile-test-escape-claims`,
  `ctcompile-test-type-oracle`. Later source fixes rebuilt the affected native
  targets; the final fixture sync reported no build work.
- Exact `ctcompile_escape_analysis_arrays`: **1/1**, 1.54s. Final exact
  `ctcompile_host_contract`: **1/1**, 0.49s.
- Final lowering lit `object-argument-lift.mlir`, `object-argument-refusals.mlir`,
  `constructor-refusals.mlir`: **3/3**, 4.37s. Escape lit
  `bitand-index-overwrite.test`, `bitor-xor-index-overwrite.test`,
  `composed-index-overwrite.test`, `remainder-index-overwrite.test`: **4/4**;
  the combined initial lowering/escape run was **7/7**, 4.26s.
- Final focused class probe: **33 source observations / 128 main native
  executions / 66 unprepared refusals / 58 preparation refusals / zero prepared
  native refusals**. Six newly admitted sources add **48 executions** across
  both optimization modes, explicit/deduced output and GCC/Clang. Ancillary
  constructed-method controls: **16 executions / 20 refusals**; original-r
  controls: **eight executions / four refusals**. Missing Map identity,
  forged proof, complete work-budget and original Bootstrap refusals pass.
- First complete proof budgets: constructor direct **610**, completed-record
  overwrite **826**, helper holder **1478**, empty **222**, own-field loop
  **1337**. Every smaller tested budget withholds the complete rewrite.
- **44/44** concrete record-pointer artifacts across eleven positive sources
  and **10/10** raw forged Map/receiver refusal controls pass. All **ten** tested
  file hashes match. Inspected the constructor-publication disposal C++ output:
  stack owner, direct constructor call, immediate Map registration, borrowed
  get and direct disposal field assignments.
- AND recording: **165 observed sites / 35 sound / 35 of 41 confined precision
  (85.4%)**; zero violations, partial, pending, unobserved claims or unclaimed
  sites. Six imprecise Stored claims remain. These numbers use the focused lit
  recording, without another corpus execution. All 55 AND source functions
  execute in Node; the recorder sees 56 functions including script entry.

The focused probe is recorded in `probe.py`/`probe.sh` under the evidence
folder. It selects `class-map-record-constructor-*` and these adjacent sources:
`empty`, `class-map-direct-string-keys`, `class-map-helper-holder`,
`class-map-inherited-constructor`, `class-map-stored-receiver`,
`class-map-helper-stored-receiver`, `class-map-inherited-stored-receiver`,
`bootstrap-base`, `bootstrap-base-data`, `own-fields-loop`,
`class-map-record-direct`, `class-map-record-overwrite`,
`class-map-record-delete`, `class-map-record-missing`,
`class-map-record-mixed-constructors`, `class-map-record-map-escaped`,
`class-map-record-alias-escaped`, `class-map-record-alias-snapshot-order`,
`class-map-record-alias-snapshot-dispose`,
`class-map-record-alias-snapshot-constructor-publication`, and
`class-map-record-alias-method-constructor-publication`.


Required `tools/format.sh --check` retains 20 baseline diagnostics in six
HEAD-identical paths: ctdrive.cpp, PrefixAnalysis.cpp, ProviderCallbacks.cpp,
ProviderPaths.h, Heap.h and Facts.cpp. Whole-repository formatting does not pass;
changed C++ formatting, Python AST/Black and whitespace checks pass. Original
class fixture files 01–29 are unchanged; Node checks cover all 11 new sources.

The first publication probe executed direct and overwrite cases, then found
that Map reads through fixed cells were missing from the saved-alias census.
That shared proof now resolves those reads and checks every remaining capture.
A later run executed all four new positives before the escaped-Map control
printed an extra scalar global. The new fixture's footer now uses only `a` while
preserving its global Map and read after the owner frame returns. No runtime
semantics or existing source bodies were changed to satisfy these checks.

Skipped: full CTest/compiler lit, whole class-initialization lit, broad native
matrices/corpus, DOM replay, full Bootstrap, WPT/test262, Windows and sanitizers.
Historical browser compliance measurements remain historical.

## Next boundary

Original B/Data+B still refuse constructor-time `e.set(..., this)`. Their
publication crosses helpers and inherited construction, rather than the proved
single terminal direct Map call. Data additionally retains the receiver through
a captured outer element Map and nested DATA_KEY Map, conditional conflict
checks, nullable gets, and delete/empty cleanup. Connect the complete call and
Map origins with partial initialization, exception/reentry and enclosing owner
lifetime before admitting it. Preserve original conflict checks, `e.set`,
`e.remove`, `P.off`, configuration and disposal bodies.

The unchanged file29 publication snapshot has a field write after registration
and remains refused. Captured/transported, nested and region-local record Maps,
full Bootstrap and the application driver remain unfinished.
