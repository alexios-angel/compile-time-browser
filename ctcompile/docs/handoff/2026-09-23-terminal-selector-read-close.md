# Reads after terminal iterator-close selectors and varying masks, 2026-09-23

Resumed clean **b9a880eb** and retained source `54c46eea` from HANDOFF, current00,
the terminal-selector detail and iteration 103's final journal. No dirty drafts
or unmerged `codex-wip-20260907` remained; unrelated branches were preserved.
Parallel agents prepared source and escape regressions. Interruption preserved
seven frozen code/test files; the parent resumed them and the remaining checks.

## Landed

**b0745168** registers each unused terminal selector immediately in the existing
suppression and complete use census, freeing the final `hasAttribute` slot. A
pending or subsequent attribute write still refuses. Sequential cloning retains
the original guard and call order; complete DOM/Style reproof validates receivers,
primitive arguments and literal selectors. Private callable/frame checks, confined
results, the original saved Boolean exception and proof/expansion budgets remain.

The original source is unchanged, SHA-256
`54c46eea3a0177a82cfd63247ab637b9d24bdc7f86e32979faa54d5bf814181f`.
Getter `c24ef382`, missing final read `c9f6622f` and order-sensitive `5efdfd2a`
execute too. Tests preserve false/true saved exceptions, normal completion,
exhaustion, repeated calls, separate documents and owned sessions. Generated
C++ retains both selector calls and the final attribute read after every write.

**ecdb8d0e** extends existing bounded whole-key subdivision to AND/OR/XOR
with two nonsingleton bounded operands. Each accepted leaf reuses the existing
singleton-mask transfer; no new arithmetic approximation is introduced. Complete
producer/receiver reload and later-store census, restored induction bounds,
independent reload-gap reproof and actual-write replay remain. Twenty raw CFG/SCF
and fourteen source controls cover operand order, retained/saved children,
reload gaps, later stores, fractional operands and non-own writes.

## Focused validation

- Exact `ctcompile_host_contract`: **1/1 PASS, 2.43 s test / 2.44 s total**.
- Four selected saved-throw sources: **64 native executions, 96 refusals and
  32 Node/VM observations PASS**, both providers and optimization policies,
  explicit/deduced C++, GCC and Clang. The selector executes the committed fixture.
- Admission preflight: four positives and twelve refusals under both policies.
- Final exact `ctcompile_escape_analysis_arrays`: **1/1 PASS, 2.56 s test / 2.57 s total**.
- Exact `Analysis/Escape/escape-claims/power-index-overwrite.test`:
  **1/1 PASS, 0.15 s**, with 355 other lit cases excluded.
- Final `tools/format.sh --check`: **1126 C++, 157 Python, 114 web files PASS**;
  scoped checks, `git diff --check` and scratch gate `bash -n` checks pass.
- All **eight final code/test hashes match the devbox**. All **32 generated C++
  files** retain both selectors, the preceding attribute Boolean feeding its
  write and the final unused read after the terminal selector. No Script/VM or
  nullable-scalar fallback appears; linked-symbol checks pass in the fixture.
- Source preservation retains **233 historical iterator bodies, 86 metadata
  rows, 56 saved cases, 100 retained saved refusal bodies and 56 historical
  oracle constructions**. Sixteen positive and twenty-four refusal local Node
  observations pass. Historical oracle scripts/expectations were compared without
  reexecution. All **210 historical escape source bodies/expectations** survive;
  fifteen source outputs/own-key sets and eight historical raw write/read/identity
  witnesses pass.

The interrupted native execution had no completion result and was restarted for
only the selected four sources. Completed host and preflight checks were retained.
The first exact array gate failed 24 checks: six older varying-mask refusal rows
were now sound admissions, and a new structured producer lacked its expected
`storage_test_id` label. Node confirmed repeated AND/OR operands write `0,1` and
release all prior children; repeated XOR writes `0,0`, preserving the second child.
The exact historical bodies now assert contents, reads and retained identities;
neighboring invalid bounds/reloads still refuse. Two further historical raw rows
were promoted before the gate with separate witnesses. Only test expectations
and the new scalar label changed after the first gate; production stayed fixed.
The power lit case passed separately and was not replayed for raw-only edits.
Independent native and escape reviews found no correctness blockers; the parent
reviewed the final expectation corrections.

Initial WSL-root `/proc` and Windows CIM checks inspected **81/353** processes;
prelanding checks inspected **87/353**. Both found no actual Claude executable,
CLI or loop matches and no errors. No browser/shared implementation edits, local
C++ builds, pushes or history rewrites were made.

Commands, selectors, source witnesses, generated artifacts, logs and checksums
are preserved in `../test-results/2026-09-23-terminal-selector-read-close/` relative
to the monorepo. Full CTest/compiler lit, complete iterator fixtures, unaffected
native replays, broad corpus/matrices, full Bootstrap, browser/WPT/test262,
Windows and sanitizer runs were skipped under the focused policy.

## Next boundary

Retained `unsupported-terminal-match-second-read`, SHA-256
`5cd57a592ed7f10369ebcdd72c3d597b9f88a0f990e204e50d97566e4b194e39`,
refuses **DOM protected helper needs an independent inert-body proof** under both
policies. After its second selector it calls `hasAttribute('data-closed')`, then
throws `hasAttribute('data-unvisited')`. Preserve both reads in order and the
original saved body exception.

Single-write selector cleanup, nonterminal exceptional state, multiple protected
regions, implicit cleanup, nested custom iterators, unguarded Bootstrap defaults,
the application driver, full native Bootstrap and general powers remain unfinished.
Wider varying-mask proofs remain bounded by the existing work limit.
No full-Bootstrap admission or coverage gain is claimed.
