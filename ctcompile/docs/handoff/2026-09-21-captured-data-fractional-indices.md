# Captured Data helpers and fractional String indices, 2026-09-21 UTC

Continued clean `5599188c` from the captured three-argument Data helper boundary.
The first process census found no Claude session (80 Linux, 360 Windows processes).
Three agents supplied Data fixtures, fractional String support and proof review;
root integrated the drafts and owned all devbox validation and commits.

`e7ba01dc` expands exact captured Map helpers before nested routing in the
owning entry.
The complete recursive helper census admits only existing Map operations, exact
child allocations and bounded if/short-circuit transport. Every invocation keeps
its own actual arguments and captured Map. Original callee, symbol, holder,
initialization-order and fixed-cell checks remain required. Recursive clone work
is charged before mutation; branch-local captures and cached call-result cells
are repaired before their original producers retire. Newly exposed child Maps
enter the existing owner census. Publication and record-owner guards are unchanged.

Four source witnesses cover both literal key dimensions, direct helpers, called
set/get/remove holder slots, short-circuit allocation, distinct children and saved
record/child aliases through replacement and deletion. Fifteen complete-source
refusals cover unknown conditions, missing keys, escapes, dead calls/coercion,
reentry, region owners, constructor publication, exceptions and non-String keys.
The direct witness carries the existing fingerprint, forged-proof and budget
rollback controls. All 43 earlier class fixture files remain byte-identical.

`72a99948` extends the bounded finite literal proof and UTF-16 lowering to
fractional String indices. `std::trunc` precedes sign testing and unsigned conversion, so `-0.5` selects
index zero. The raw literal magnitude remains at most 4294967295; broader values,
coercion and dynamic indices still refuse. Original exact `charAt(0)` and `slice(1)`
authority stays unchanged. Seven former fractional refusal bodies execute unchanged;
two new bodies exercise UTF-16, NUL, negative zero, extreme bounds and saved guarded
Symbol descriptions. Fourteen new two-mode refusals, missing-String contracts,
budget limits and a stale fingerprint accompany them. VM casing/byte-indexing
differences remain explicit; the runtime is unchanged.

## Focused validation

| Check | Final result |
| --- | --- |
| Exact CTest `ctcompile_host_contract` | 1/1, 0.58 s; 0.59 s total |
| `CTNative/Lowering/Objects/class-captured-map-helpers.test` | PASS, 280.64 s |
| `CTNative/Lowering/Objects/class-terminal-publication.test` | PASS, 204.69 s |
| `CTNative/Exports/native-intrinsic-symbols.test` | PASS, 468.89 s |
| `CTNative/Browser/native-dom-strings.test` | PASS, 226.97 s |

These are four distinct lit cases across corrected runs, not a full-suite pass.
The first completed selection was 3/4 in 468.90 s. The captured fixture failed
at its fourth new witness after 38.10 s: caller boxing left a proved fixed-cell
Map read inside a short-circuit branch. Moving the existing read discharge ahead
of branch census/erasure fixed it without changing the source or widening effect
admission. The final captured-only selection passed 1/1 in 280.65 s.

Captured class checks measure **155 source observations, 328 main native executions,
310 unprepared refusals and 198 preparation refusals**. Four new admissions add
32 executions; fifteen new source bodies remain native refusals. The new direct
helper's first complete budget is **1810**. Earlier short-circuit/conditional/direct
nested witnesses measure 1704/1001/837. The adjacent terminal fixture measures
58 observations, 160 main executions, 116 unprepared refusals and 70 preparation
refusals. Both selections also run constructed-method controls (16 executions,
20 refusals) and original helper controls (eight executions, four refusals).

Intrinsic exports measure **504 native executions, 502 refusals and two mutations**,
with **71 Node/VM agreements and seven explicit known casing/indexing differences**.
Nine new admissions add 72 executions, including seven former fractional refusal
programs preserved byte-identically. DOM Strings measure **809 Node/VM observations
and eight GCC/Clang binaries**, plus 1,000 source refusals, 44 provenance/depth
refusals, 24 method provenance checks, 241 capture provenance/budget checks,
101 replacement checks, 22 branch checks and 31 completion checks.

All builds and native executions ran on the devbox under the shared lock.
Explicit targets were `ctjs-opt`, `ctjs-translate`, `ctcompile-test-host-contract`
and `ctcompile-test-native-reference`. The first eight-step build passed. A
heredoc's trailing CTest did not execute; the separately run exact CTest passed
1/1 in 0.56 s (0.57 s total). User continuation interrupted its following two-case
lit run, leaving an idle orphan worker pool with no result JSON. Only that
session's three worker PIDs were stopped under the lock; it counts as no lit pass.
The final-source sync rebuilt three steps, and the fixed-cell correction rebuilt
another three before the final CTest and captured-only pass. Later lit logs were
saved on the devbox to survive an interrupted local session.

All **nine final code/test SHA-256 hashes** match the devbox. Four C++ and four
Python files pass scoped formatting and syntax/whitespace checks. All 43 prior
class fixture files, 54 earlier complete intrinsic cases and 33 general refusal
sources remain unchanged. Separate source checks pass 19 new class Node observations
and 78 intrinsic Node observations; the executable gates check VM expectations too.
Required `tools/format.sh --check` retains **16 existing diagnostics** in untouched
`ctbrowser/tools/ctdrive/ctdrive.cpp`, `HostContract/ProviderPaths.h`,
`PartialEvaluation/Heap.h` and `Symbolic/Facts.cpp`.
Full CTest/compiler lit, broad corpus/native matrices, full Bootstrap, WPT/test262,
Windows, new sanitizers, local native builds and push were skipped.

Evidence uses `/tmp/ctcompile-data-helpers`: `-build1.log`, `-initial.log`,
`-gate2.log`, `-final.json`, `-final.log`, `-gate3.log`, `-captured.json`,
`-captured.log`, `-local.sha256`, `-evidence-final.log` and `-format3.log`.

## Exact next boundary

Constructor-time three-argument registration into the selected nested child.
`normalizePublicationHelper` still accepts only one/two-argument direct registration,
and `sinkConstructorPublication` expects a terminal two-argument `Map.set`. Extend
these with both key origins and the selected child owner while preserving every
holder observer and constructor exception/reentry order. Original Data also needs
element-identity keys, conflict reporting, nullable lookup and conditional
child/empty-parent cleanup. Original B/Data+B stays refused with complete sources.
Dynamic/coercing String indices, broader methods, conditional callees,
branch-mutated boxed locals, String ordering, document views and the application
driver remain. No full-Bootstrap admission or coverage gain is claimed.
