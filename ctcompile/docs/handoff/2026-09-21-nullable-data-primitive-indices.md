# Nullable Data getters and primitive String indices, 2026-09-21 UTC

Continued clean `b2d1a4af` from its recorded nullable Data boundary. All earlier
work was committed; `codex-wip-20260907` is already an ancestor. Linux cmdline/comm
checks read 70 processes without denied records; Windows CIM read 356. Neither
found a Claude executable, Node CLI or loop. No browser or shared implementation
files changed. Interrupted agents left drafts, which root preserved and finished;
parallel agents supplied String work, source fixtures and read-only proof review.

## Implementation

`cdbc468e` records truthiness at each child lookup, preserving its
selected child and the original read time across later insertion/replacement.
It follows lookup results through short circuits to include the final `|| null`
in the existing both-arm effect census. Every child whose truth test is folded
passes the complete alias/capture check. Present reads remain for the unchanged
record and lifetime proof. Missing reads disappear only after all non-root
observers are gone; raw missing-result observations remain refused.

Three complete-source admissions cover direct captured helpers, a Data holder
and constructor registration with the original nullable getter. They preserve
saved child/record aliases, cleanup/recreation, absent parent and child keys,
and a missing snapshot used after insertion. Unrelated `has`-controlled arithmetic
retains its ordinary lowering. Fourteen new refusal sources retain raw absence,
null/undefined payloads, unknown keys, escaped/shared/region owners, unused holder
observers and effects in either branch. All 47 earlier class source files remain
byte-identical. Existing fingerprint, proof-input and first-complete-budget
controls cover the new direct admission.

`b64244a7` admits literal null and Boolean values through existing UTF-16
conversion and typed scalar lowering. Null selects zero, including an explicit
slice end; undefined still means the default end. Four admissions include two
byte-identical former refusal bodies, Unicode primitive indices and a captured
Symbol description. Dynamic/coercing indices remain refused. First-unit lowercase
and dataset-tail authority still require the original Number literals.

## Focused validation

| Check | Result |
| --- | --- |
| Final CTest `ctcompile_host_contract` | 1/1, 0.62 s; 0.63 s total |
| `CTNative/Lowering/Objects/class-captured-map-helpers.test` | PASS, 373.10 s |
| `CTNative/Lowering/Objects/class-terminal-publication.test` | PASS, 151.23 s |
| `CTNative/Browser/native-dom-strings.test` | PASS, 188.66 s |
| Targeted primitive String exports | 56 native executions; 638 proof refusals |
| Strengthened raw-absence control | Node/VM `a=1107`; exact preparation diagnostic; two native refusals |

The captured fixture measures **224 source observations, 440 main native
executions, 448 unprepared refusals and 295 preparation refusals**, with new
first-complete budget **6600**. Three new admissions add 24 executions.
Existing constructed-method controls also run 16 executions/20 refusals and
original helper guards run eight/four. After that pass, one new raw-absence
refusal was strengthened to remove a nested helper call that could mask the
intended failure. The final targeted replay verifies the exact present-record
preparation diagnostic, no partial output, both native refusal modes and Node/VM
observations. The whole captured fixture was not replayed after that refinement.
All 47 earlier complete class source files remain byte-identical.

The String selector runs four new cases and three explicit-index/undefined
sentinels in eight native modes each: **56 executions**. All proof controls pass:
**638 refusals, 102 Node/VM agreements and ten explicit known differences**.
It does not replay the whole export fixture or its existing executable mutations.
DOM Strings retain 809 Node/VM observations and eight native binaries.

Build1 compiled seven steps and exact `ctcompile_host_contract` passed 1/1 in
0.58 s (0.59 s total). The initial captured/terminal selection passed 1/2 in
174.11 s: captured found an overly eager child-observer requirement on an
existing direct read. The proof now requires that census only for lookup truths
it actually uses. Build3 had already started with the earlier draft: DOM Strings
passed, while captured repeated the known failure (selection 1/2, 188.67 s).
Build4's corrected captured-only replay passed in 373.11 s total.

The first primitive String selector failed on the promoted explicit-null slice:
GCC rejected a generated zero-versus-zero comparison under `-Werror`. An explicit
null end now emits zero count directly. Build5's corrected focused selector and
exact host-contract CTest passed. Builds 2–5 compiled six, three, three and three
steps. Every build used explicit targets `ctjs-opt`, `ctjs-translate`,
`ctcompile-test-host-contract` and `ctcompile-test-native-reference` under the
shared devbox lock; no broad CTest/lit command ran.

Seventeen final class Node observations, 112 String Node observations, Python
AST/import checks, three C++ and four Python scoped formatting checks pass.
All eight final code/test hashes match the devbox. The required repository
formatter retains 16 existing diagnostics in untouched `ctdrive.cpp`,
`HostContract/ProviderPaths.h`, `PartialEvaluation/Heap.h` and `Symbolic/Facts.cpp`.

Evidence: `/tmp/ctcompile-nullable-build1.log` through `build5.log`,
`/tmp/ctcompile-nullable-class-final.json`, `class1.json`, `final-lit.json`,
`final.sha256`, `refusal.log` and `format-final.log` (same nullable prefix).
The temporary export selector is `/tmp/ctcompile-string-primitive-focused.py`;
its launcher reads tool paths from the generated lit configuration. The final
raw-absence launcher is `/tmp/ctcompile-nullable-refusal.py`. No instrumentation
or generated artifacts were added to repository code.

No full CTest/compiler lit, broad corpus or native matrix, full Bootstrap,
WPT/test262, Windows, new sanitizers, local native builds or push were run.

## Exact next boundary

The original Data registration still contains the conflict-reporting observer
arm (`console.error` and `Array.from`). Preserve that complete arm while proving
its conflict condition, effects and selected child; do not erase the helper
under a partial proof. Actual element keys still need the public DOM/HostContract
owner seam. Original B/Data+B remains a complete-source refusal, so this is not
full-Bootstrap admission or a new corpus measurement. Dynamic/coercing String
indices, NaN origins, broader methods, conditional callees, mutable cells,
String ordering, document views and the application driver remain unfinished.
