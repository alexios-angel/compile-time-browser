# Iterator close regions and singleton products, 2026-09-22

Resumed clean **6d04e448** and the exact `body-throw` source `f1b3f6b8`
from HANDOFF/current00 and iteration 76's final journal. No dirty drafts or
unmerged `codex-wip-20260907` remained. Other branches were preserved.
User continuations interrupted agents and one build; saved source baselines,
claims and evidence were retained. Parallel agents supplied source controls,
a proof review and the independent escape change.

## Landed

**f568bb86** recognizes one original, argument-free, acyclic iterator-close
suppression handler. It retains the close invocation, its true abrupt flag,
record identity and both normal/exceptional outcomes in a zero-result
`ctjs.invoke`. Both outcomes resume the same preceding saved throw value.
Checks require one unobserved landing, private normal predecessors, an exact
shared throw continuation, one unobserved call and one handler removal.
Extra effects, unrelated predecessors, observed catch values, replaced initial
bindings and insufficient budgets refuse without changing the candidate.

The normalizer belongs to HostContract analysis beside custom iteration.
Preparation tries it before routing an entry handler through URI recovery.
Initial intrinsic lookup and constants move outside suppression; the call stays
protected. Complete prefix/global/reentry and DOM reproof is still required
before publication. This is structural progress, **not native throw admission**.
No placeholder return, new runtime value model or browser implementation was added.

Raw positive and hostile controls exercise both DOM providers. Four appended
source refusals cover saved Boolean/Number throws and throwing close methods or
getters. All 135 historical refusal sources, 86 positive metadata rows, the
compile-only source and three mixed-selector sources remain unchanged.

**9c727b92** computes bounded multiplication endpoints before scaling an index
lattice's stride. Equal endpoints use stride 1: no adjacent visit exists whose
stride could overflow. Non-singleton products retain their magnitude bound;
intermediate Number bounds, complete store/reload census, actual-write replay
and budgets remain. Five CFG rows, five SCF rows and five source functions cover
wide singleton products, translated intermediates, retained children and bounds.
One historical CFG refusal is promoted with its exact construction unchanged.
All 25 historical source bodies and CHECKs remain intact.

## Focused validation

- `ctcompile_host_contract`: **1/1 PASS, 2.42 s test / 2.43 s total**.
- `ctcompile_escape_analysis_arrays`: **1/1 PASS, 2.15 s test / 2.16 s total**.
- Exact `Analysis/Escape/escape-claims/scaled-index-overwrite.test`:
  **1/1 PASS, 0.11 s**.
- Initial selected native sources `normal`, `body-return-ordered` and
  `body-return-branch-element-selector-mixed-roots`: **48 native executions,
  66 refusal checks and 24 Node/VM observations completed** before the newly
  added `body-throw-observed-catch` failed complete import. That command failed;
  it is not a complete fixture pass.
- Final follow-up runs only the original body throw and four new complete-source
  refusal controls: **10 refusals PASS**, zero executions, admissions or oracle
  observations. Previously completed execution checks were not replayed.
- Six saved throw sources, including the importer boundary, agree in all
  **12 fresh/exhausted Node/VM observations**. Saved values and write order agree.
- Original `json_object` exercises the existing JSON/URI handler route:
  **four native executions and two Node/VM observations PASS**, both providers
  and policies, Clang, explicit printing. Other JSON cases/layouts were skipped.
- Complete `tools/format.sh --check`: **1126 C++, 157 Python, 114 web files PASS**,
  including a final repeat after the source fixture repair.

All 12 final code/test hashes match the devbox. All 28 generated C++ files have
no Script/VM protocol names; the execution harnesses also gate linked symbols.
Supplemental local checks passed: 230 drafted source syntax checks before the
new importer-boundary row was removed, exact historical source preservation,
30 escape source parses, five new escape outputs and the original CFG trace.

The first production build passed. The first host-test link then failed because
the normalizer lived in the lowering library while the test links analysis.
Moving it to HostContract analysis fixed the dependency direction without
broadening test linkage. A public-header rebuild interrupted by continuation
was resumed after confirming no local or remote builder remained.

The newly drafted observed-catch sample imports with `ctjs.skipped`:
**more than one protected region in a function, which is what try/finally and
nested try both compile to**. Only that new native-fixture row was removed;
its source, partial IR and Node/VM evidence remain in the artifacts. Its catch
observation still has an independent raw hostile test. The original body throw
was never rewritten. The first source failure skipped the subsequent VM/JSON
commands; both ran and passed in the final follow-up.

Build targets were `ctjs-opt`, `ctjs-translate`, `ctcompile-tool`,
`ctcompile-test-host-contract`, `ctcompile-test-native-reference`,
`ctcompile-test-escape-analysis-arrays`, `ctcompile-test-escape-claims` and
`ctcompile-test-type-oracle`. All builds and C++ checks used the devbox and
shared build lock. Commands, logs, source snapshots, generated C++, frozen
agent evidence and hashes are in
`../../../../test-results/2026-09-22-iterator-close-regions/`.

Full CTest/compiler lit, complete custom-iterator and JSON fixtures, broad
corpus/native matrices, full Bootstrap, WPT/test262, browser suites, Windows,
sanitizers and local C++ builds were skipped. Nothing was pushed or rewritten.
The first unprivileged process scan was incomplete; the subsequent WSL-root
scan inspected 83 Linux executable identities and Windows CIM 349 processes,
with no matches or errors: Claude was confirmed stopped. No browser or shared
implementation files changed.

## Exact next boundary

Unchanged `body-throw`, SHA-256
`f1b3f6b87564e88d0a47ab525f0e5f3d187819285b6d62645c145d309f1a7551`,
now passes close-handler recognition. Both native policies return status 1:
**native DOM source: DOM custom iterator requires one complete entry**.
No native execution of this source is claimed.

The remaining entry contains mixed throw/return exits. A separate probe that
flattened the guard showed the existing CFG-to-SCF pass leaving a final
`cf.switch` with distinct `ctjs.throw` and `ctjs.return` blocks. That probe is
structural evidence, not an authorized exception-elimination rewrite.

Next, represent and prove those terminal continuations and consume the close
invoke within custom-iterator normalization. Preserve the evaluated throw value,
close effects and suppression, and require full prefix/global/reentry proof
before trusting the moved initial lookup. Use the existing typed C++ throw
emission; do not fabricate an unreachable return or weaken its payload verifier.
General implicit cleanup, multiple protected regions, nested custom iterators,
unguarded Bootstrap defaults, the application driver and full native Bootstrap
remain unfinished.
