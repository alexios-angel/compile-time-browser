# Published record fields, 2026-09-21 UTC

Continued clean `c475dde8` from the recorded published DOM Data boundary. Both
commit histories, unmerged branches and synchronization records were read;
`codex-wip-20260907` is already an ancestor. There was no unfinished source to land.
Linux checked 71 processes, with 57 unreadable executable links; Windows CIM
checked 358 processes. Neither found a Claude executable, CLI or loop, but the
incomplete Linux check left availability **uncertain**. Concurrent-agent rules
applied; no browser/shared implementation changed. Agents supplied fixtures,
String work and partial read-only reviews; repeated service limits required root
to finish their saved drafts.

## Landed

`f65dedd5` permits a scalar argument read from an exact fresh entry-local record
field in a real published DOM Data family. The existing source `currentWrite`
query identifies the read-time write. Category evidence preserves the original
read and does not supply a constant or branch predicate. The complete existing
caller-leaf census now also checks these scalar dependencies, retaining every
field, alias and observer before exposing the record to the existing owner proof.
Factory publication, input-family completeness, root ownership and native
admission remain mandatory.

Four native witnesses cover a field, an alias, a saved read before later field
mutation, and arithmetic on the read. They use a real wrapper/factory/root and
retain the DOM key so repeated invocation checks source allocation reset. The
client checks separate document owners, aliases, detachment, invalid domains
before effects, interleaving and destruction order. Emitted code and linked
symbols must contain no Script/VM dependency.

Two additional witnesses retain the complete vendor Data holder or constructor
inside the same real publication shape. **They still refuse class preparation
and native emission.** The old fake-root preparation controls and their original
source generator remain unchanged. Global field snapshots also remain refused:
their dependency consumer currently requires completed captured method calls.

`5bdb9644` admits one remainder of two direct Number literals
as a UTF-16 `charAt`/`slice` bound. Existing `std::fmod` emission and UTF-16 index
clamping handle fractional results, zero, produced NaN, infinity and subnormal inputs.
Negative source operands still contain unary operations and remain refused,
alongside dynamic/coercing/nested operands and non-index observations. Direct
literal first-unit/dataset-tail authority remains unchanged.

## Focused validation

| Check | Measured result |
| --- | --- |
| `CTNative/Browser/native-class-dom-data.test` | PASS, 66.21 s |
| `CTNative/HostContract/dom-data-inputs.test` | PASS, 1.11 s |
| `CTNative/HostContract/Provider/objects.test` | PASS, 3.34 s |
| Exact CTest `ctcompile_host_contract` | 1/1, 0.55 s; 0.56 s total |
| Added global field-snapshot control | Both native policies refuse |
| Remainder String selector | 40 native executions; 822 proof refusals; 115 Node/VM agreements and 14 known differences |

The DOM fixture ran **32 new record executions** and **24 existing object-key
executions**, using GCC/Clang, explicit/deduced output and both optimization
policies. Its new portion checked six Node/VM source observations, 30 refusals,
and stale/budget/forged-report controls. The old portion checked four Node/VM
observations, four class preparations, 30 preparation refusals and eight missing
session-owner refusals. The global-snapshot control was added afterward and
separately passed two native refusals; the whole fixture was not replayed after
that addition. Final committed new refusal count is therefore 32 across these
focused runs, not a claimed final whole-fixture replay.

Explicit devbox targets were `ctjs-opt`, `ctjs-translate`,
`ctcompile-test-host-contract` and `ctcompile-test-native-reference`, under the
shared build lock. The initial two-input record probe also passed source proof
and native storage lowering. Repository `tools/format.sh --check` reported the
same 16 pre-existing diagnostics in untouched `ctdrive.cpp`,
`HostContract/ProviderPaths.h`, `PartialEvaluation/Heap.h` and `Symbolic/Facts.cpp`.
All seven final code/test hashes match the devbox. Four C++ and two Python
scoped formatting/syntax checks passed. The previous class source generator and
object constructor checks are unchanged; all 104 prior complete intrinsic cases
and 33 general refusals are preserved. The String selector executes three new
cases and two old sentinels, retaining all proof controls; existing executable
mutations were skipped. Root corrected one C++ formatting line in the interrupted
String draft. An initial local preservation check used the wrong Python registry
name (`REFUSED`); the corrected `REFUSALS` comparison passed.

No full CTest/compiler lit, complete captured/export replay, broad corpus/matrix,
full Bootstrap, WPT/test262, Windows, new sanitizers, local native build or push
ran. Evidence: `/tmp/ctcompile-dom-publication-build{1,2}.log`,
`/tmp/ctcompile-dom-leaf-probe.log`, `/tmp/ctcompile-dom-record-inputs.log`,
`/tmp/ctcompile-dom-record-executable1.log`,
`/tmp/ctcompile-dom-record-final-and-remainder.log`,
`/tmp/ctcompile-record-final-control.py` and
`/tmp/ctcompile-scalar-record-format.log`,
`/tmp/ctcompile-published-record-final-format.log`,
`/tmp/ctcompile-published-record-final.sha256` and
`/tmp/ctcompile-string-remainder-focused.py`.

## Exact next boundary

Compose class/local-Map proof with the now-tested **real published family**.
`published_source()` in `native_class_dom_data.py` supplies complete vendor
holder/constructor witnesses, including publication of the computed scalar.
They remain refusals at the existing residual DOM-input observer gate. The
plain-record scalar result path now has a complete native execution witness.

Class preparation must account for the completed public family before retaining
its DOM input uses and wrapper/factory operations. Then retained constructors
and local Maps need their existing closure/record ownership proof composed with
`HostContract` source analysis. `OwnedGlobalRoots` still requires the exact
function chain, allocation census and real factory publication. Reuse these
proofs; do not whitelist constructions or bypass the root/input-family checks.
Persistent class-record storage remains a later step.

Multiple DOM-input alias partitions, global field-snapshot dependencies,
original B/Data+B, broader Strings, conditional callees, mutable captured cells,
String ordering, document views and the application driver remain unfinished.
No full-Bootstrap admission or corpus coverage gain is claimed.
