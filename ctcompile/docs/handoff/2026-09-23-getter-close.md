# Suppressed iterator return getters and signed-unit power ranges, 2026-09-23

Resumed clean **155fd28b** and original getter close `68ea7208` from HANDOFF,
current00 and iteration 90's final journal. No dirty drafts or unmerged
`codex-wip-20260907` remained. Unrelated branches were preserved. Parallel agents
supplied source regressions, the escape proof and independent native review.
The parent completed source promotion after that agent's rate-limit interruption
and owns the devbox gates, final review and commits.

## Landed

**807cd24b** models an exact own throwing `return` getter as the existing
private close callable. The getter has no setter and ends in a terminal throw;
existing proof must establish a literal payload, one confined closure, no symbolic
callers, source ordering and unique slots. The original and post-completion holder
and method censuses exclude aliases and extra observations. Every remaining call
must retain exact unused-result suppression. Exhaustion alone elides lookup.
All preceding effects and payload producers remain for complete helper and typed
DOM reproof, including shadow-frame checks. Failure discards the private candidate.

The original source SHA256 remains
`68ea72087c2bfeb05a790d1edca61871c477df63a5d1c94a65087156aceff5f2`.
Its getter writes `data-closed=yes` and throws Number 2; native execution preserves
the write and the original body exception, Number 1. Boolean snapshot witness
`48cc2d6b` writes `data-closed=getter` and retains its saved Boolean. Exhaustion
and reentry bypass the getter. Returning getters, setters, duplicate definitions,
nonliteral throws, saved/transported method aliases and ordinary closes refuse.
Generated C++ uses ordinary owned values and public DOM calls; no runtime carrier
or browser implementation was added.

**5d6676f4** extends the existing power range proof only to positive bounded
integer exponents over bases `-1, 0, 1`. Existing scalar transfer proves parity at
-1; the interior zero determines the lower bound for even powers. Odd powers keep
all three signed values. Stride one conservatively includes every image for the
invariant reload census, while actual-write replay still establishes overwritten
children. Negative exponents across zero, fractional exponents and general powers
remain refused. Reloads, later stores and pre-loop snapshots retain their checks.
All 65 historical source bodies remain unchanged; source 63 (`interiorPowerBase`)
and the matching raw CFG body now prove. Twelve source controls and focused CFG/SCF
checks were added.

## Focused validation

- Exact `ctcompile_host_contract`: **1/1 PASS, 2.42 s test / 2.43 s total**,
  including getter admission, aliases, unsupported accessors and budget thresholds.
- Two affected getter sources: **32 native executions, 48 refusals and eight
  Node/VM observations PASS**, across both providers/policies, explicit/deduced
  C++ and GCC/Clang. This is a selected source check, not the whole iterator suite.
- Exact `ctcompile_escape_analysis_arrays`: **1/1 PASS, 2.29 s test / 2.30 s total**.
- Exact `Analysis/Escape/escape-claims/power-index-overwrite.test`: **1/1 PASS,
  0.12 s**, with 355 other lit cases excluded.
- Eight-source final preflight: **four admissions and twelve refusals** across
  both policies. Both getter witnesses admit; normal, returning and nonliteral
  getters and the two conditional-close boundaries refuse.
- Full `tools/format.sh --check`: **1126 C++, 157 Python, 114 web files PASS**.
  `git diff --check` and scoped source formatting pass.
- Local checks preserve **233 historical iterator bodies and 86 metadata rows**,
  with 233 source syntax checks. Selected getters have four Node observations;
  getter refusal sources have eight Node observations.
- Escape local checks preserve **65 historical source bodies** and every
  unpromoted CHECK, plus historical raw bodies. **77 exact Node outputs and
  own-key sets**, source syntax and scoped formatting pass.
- All **seven final code/test hashes match the devbox**. All **sixteen generated
  C++ files** are free of Script/VM protocol and nullable-scalar fallback;
  native harness linked-symbol checks pass.
- Independent read-only native review found no blockers. No production changes
  followed the successful gates.

User steering interrupted the first execution command after its completed
preflight; it produced no execution summary and is not counted as a pass.
The final command reran only the two affected execution cases after the shared
build lock. The escape and formatting commands completed normally. No compiler
or test failure required a production correction.

Explicit build targets were `ctjs-opt`, `ctjs-translate`,
`ctcompile-test-host-contract`, `ctcompile-test-native-reference`,
`ctcompile-test-escape-analysis-arrays`, `ctcompile-test-escape-claims` and
`ctcompile-test-type-oracle`. Builds, tests, native/VM executions and devbox reads
held `/tmp/ctbrowser-devbox-build.lock`. Commands and retained evidence are in
`../../../../test-results/2026-09-23-getter-close/`, under `native91/`, `tests91/`
and `escape91/`. `first-gate.sh` selects the host check; `execution-gate.sh` selects
the two getter sources; `escape-gate.sh` selects arrays and the single lit case.
`verified.json` and `devbox-hashes.txt` record the final artifact checks.

Full CTest/compiler lit, the complete custom-iterator fixture, prior unaffected
native source replays, broad corpus/native matrices, full Bootstrap, WPT/test262,
browser suites, Windows, sanitizers and local C++ builds were skipped. Nothing
was pushed or rewritten; historical measurements remain historical.

WSL-root /proc and Windows CIM identity checks initially inspected 82/352
processes, then 82/353 and finally 82/354, with no matches or errors: Claude was
confirmed stopped. No browser or shared implementation changed. The external
plan has no Git repository; current00 was updated separately.

## Exact next boundary

Conditional primitive close `1fe1a3e8` and throwing close `fd04cbc3` both still
refuse **DOM iterator primitive close requires saved-throw suppression**.
Their complete sources are retained in `tests91/`, with both-policy diagnostics
in `native91/boundaries/`. Continue with the primitive case first: correlate the
selected loop completion with done state so that exhaustion proves its ordinary
close unreachable, while a saved throw retains its suppression and earlier
Boolean snapshot. Do not relax result validation for ordinary break or return.

Mutable throwing-close state, nonterminal exceptional state, multiple protected
regions, implicit cleanup, nested custom iterators, unguarded Bootstrap defaults,
the application driver and full native Bootstrap remain unfinished. No new full
Bootstrap admission or coverage measurement is claimed.
