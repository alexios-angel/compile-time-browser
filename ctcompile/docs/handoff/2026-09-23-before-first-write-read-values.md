# Saved reads before iterator-close writes, 2026-09-23

Resumed clean **ec34d448** and retained source `7ba9aa6f` from HANDOFF,
Current native work, the between-write-read-values detail and iteration 112's
journal. No dirty drafts or unmerged `codex-wip-20260907` remained; unrelated
branches were preserved. Three agents investigated source, raw-host and escape
items, then rate-limited before saving edits. The parent completed their work,
reviewed both production changes, ran gates and committed each concern separately.

**e4f19f95** selects the existing standalone-read slots whenever the current
feeding-write method has not been acquired. This also preserves reads before the
first protected write. Every saved Boolean remains subject to the complete use
census, including its unique later consumer. Acquiring either initial write's
method still selects its existing feeding-read proof. Direct, closure and custom
iterator calls share this expansion path. Exact suppression, private helper/capture
proof, source guard/order, original saved exception and budgets remain. Complete
typed DOM/Style reproof still precedes publication; generated C++ calls public APIs.

The original source is unchanged, SHA-256
`7ba9aa6f5a66b29ccf92e4a21e78afe2d0ffdbc040961edc539083a84d310090`.
Getter `bac20de3` and order variant `f103bcc7` also execute. The original saves
false before the first close write, or true when a prior invocation left the
attribute present. The order variant saves missing `data-unvisited` as false,
then ignores a later true `data-closed` read. The final write retains the saved
Boolean while the original body exception wins. Four raw admissions, four typed
refusals, nine structural refusals and two budget rows cover order, direct calls,
unused early reads, invalid names/receivers, leaks, reuse and both feeding obligations.
Historical raw MLIR literal bodies remain unchanged.

**9854d3a8** uses `boundEndpointPair` for multiplication with at most two input
values, after both endpoints pass the existing exact scalar product transfer.
For visits 0 and 2, `(i - 1) * 4294967295` yields -4294967295 and 4294967295.
Their distance, 8589934590, is an internal lattice stride and exceeds the scalar
bound. It does not represent an extra JavaScript multiplication. The existing
endpoint representation preserves the two values and supports subsequent `>>> 31`.
No source arithmetic bound changes. Negative factors retain descending order;
actual unbounded/fractional products and a third unbounded value stay refused.
Both CFG and SCF callers retain the complete reload/store census, independent
reload-gap checks, work budget and actual-write replay. Six raw admissions and
five raw refusals plus ten source controls cover retained children and snapshots.

Focused validation on the devbox:

- Exact `ctcompile_host_contract`: **1/1 PASS, 2.53 s test / 2.54 s total**.
- Three selected saved-throw sources: **48 native executions, 76 refusals and
  24 Node/VM observations PASS**, both browser providers and optimization policies,
  GCC/Clang and explicit/deduced C++. Preflight admitted three positives and refused
  ten controls under both policies. Execution used the promoted live fixture.
- Exact `ctcompile_escape_analysis_arrays`: **1/1 PASS, 2.70 s test / 2.71 s total**.
- Exact `Analysis/Escape/escape-claims/power-index-overwrite.test`:
  **1/1 PASS, 0.18 s; 355 other cases excluded**.
- Final `tools/format.sh --check`: **1126 C++, 157 Python, 114 web files PASS**.
  Scoped formatting, Node/Python syntax, scratch `bash -n` and diff checks pass.
- All six final code/test hashes match the devbox. All 24 generated C++ files
  retain the read before the first protected write, its Boolean through ignored
  reads to the final write, and the original saved exception. Script/VM and nullable
  fallback checks plus standalone/linked symbol checks pass.

The first host run failed a new unused-read assertion that assumed the read
followed a write. The corrected assertion checks the early read's source position
and guard, alongside the exact zero-preceding-writes check. The first escape build
found a new fixture variable colliding with an existing name; only that new variable
was renamed. Production was unchanged after the first gates. Scratch source setup
syntax/import errors and a default-width Black invocation were corrected before
final snapshots; the candidate was regenerated from the original baseline with the
repo formatter configuration. Historical source and oracle construction checks pass.

Preserved 233 historical iterator bodies, 86 metadata rows, 87 saved cases,
184 retained saved-refusal bodies and 87 historical oracle constructions.
Local source checks cover 12 positive Node observations and 13 syntax checks.
All 304 historical escape source bodies/CHECKs and historical raw bytes remain
unchanged. Local checks cover 314-source syntax and ten exact Node write-key,
read-snapshot, own-key and original-child identity witnesses plus uninstrumented
outcomes. Before the escape change, all ten scratch child sites were
`escapes:stored`; the selected lit now proves three confined and retains seven
stored verdicts. The retained negative controls include semantically safe final
indices with unproved actual intermediate arithmetic.

Builds used `tools/remote-build.sh` with explicit targets under the shared lock.
Host targets: `ctjs-opt`, `ctjs-translate`, `ctcompile-test-host-contract`,
`ctcompile-test-native-reference`. Escape targets: `ctjs-opt`,
`ctcompile-test-escape-analysis-arrays`, `ctcompile-test-escape-claims`,
`ctcompile-test-type-oracle`. CTests used exact anchored names with
`--output-on-failure --no-tests=error`; lit used the generated build configuration
and the exact anchored filter above. Source selection used `throw_gate_mutable.py`
in the evidence folder. Exact commands, selected harnesses, source snapshots,
Node witnesses, logs and checksums are retained in
`../test-results/2026-09-23-before-first-write-read-values/` relative to the monorepo.

Initial Linux executable/CLI/loop checks inspected 15 accessible processes, with
61 executable-link errors; Windows CIM inspected 350 processes. Final checks
inspected 15 accessible Linux and 347 Windows processes, with 60 Linux errors.
No actual Claude matches appeared; status stayed uncertain and concurrent-agent
area rules remained in force. No browser/shared implementation edits, local C++
builds, pushes or history rewrites occurred.

Full CTest/compiler lit, complete iterator fixtures, unaffected native replays,
broad corpus/matrices, full Bootstrap, browser/WPT/test262, Windows and sanitizers
were skipped. No full-Bootstrap coverage gain is claimed.

**Next:** retained `unsupported-earlier-read-inside-first-write`, SHA-256
`9533d4795f42ca4159beec6d3a4c202898473c57f3ed19ed0248d23048ae9e63`,
refuses **DOM protected helper needs an independent inert-body proof** under both
policies. Preserve the Boolean assigned inside the first write's argument list:
`let present; anchor.setAttribute('data-closed', (present = anchor.hasAttribute('data-closed'), anchor.hasAttribute('data-visited')));`.
The earlier saved read and the feeding read have distinct later consumers;
preserve both snapshots and source evaluation order while the original exception
wins. Single-write selector cleanup, nonterminal exceptional state, multiple
protected regions, implicit cleanup, nested custom iterators, unguarded Bootstrap
defaults, the application driver, full native Bootstrap and general powers remain.
