# Object keys, DOM Data preparation and subtraction indices, 2026-09-21 UTC

Continued clean `c455efb0` from the recorded element-key boundary. Both histories,
unmerged branches and recent journals were checked; `codex-wip-20260907` is already
an ancestor. Linux checked 75 readable process records and Windows CIM checked 355;
neither found a Claude executable, CLI or loop. No browser/shared implementation
changed. Parallel agents supplied seam analysis and the String draft; root finished
the fixtures and audits after repeated agent service limits.

## Landed

`1add0418` routes outer Maps by literal String value or exact fresh object origin.
Equal object shapes remain distinct, aliases retain identity, and String keys do
not collide with objects. Three complete vendor Data witnesses cover saved aliases,
distinct keys and reassignment to another fresh object. The old nullable-object-key
refusal now executes with its original source unchanged. Record and child Map
allocations remain for the existing concrete ownership proof.

The same preparation pass accepts `ctbrowser-dom-data-session-v1` with one exact
entry element identity per outer Map. Two declared inputs cannot be assumed
unequal merely because their SSA values differ. The complete input-use and source
census rejects residual properties, coercions and captures. Two complete vendor
Data sources prepare with an original input or its alias, preserving two record
and two child Map constructions. **This is preparation only:** no native DOM
entry or document owner was admitted. The final provider proof still refuses.

`30ce626e` admits one subtraction of direct Number literals as a `charAt`/`slice`
bound. Existing Number lowering and UTF-16 clamping handle negative/fractional
results, infinities and proved NaN. One old arithmetic refusal executes unchanged.
Dynamic, coercing and nested expressions remain refused; the original direct
Number-literal authority for first-unit casing and dataset tails is unchanged.

## Focused validation

| Check | Measured result |
| --- | --- |
| Final exact CTest `ctcompile_host_contract` | 1/1; 0.57 s, 0.58 s total |
| `CTNative/Browser/native-class-dom-data.test` | PASS; 0.48 s |
| Focused object-key class selector | 9 observations, 48 main native executions, 18 unprepared and 19 preparation refusals |
| Focused subtraction String selector | 40 native executions, 722 proof refusals, 109 Node/VM agreements and 12 known differences |
| Final original B/Data+B checks | Two preparation and four native refusals; no partial output |

The class selector includes six admitted sources and three refused sources. Four
admissions exercise object keys; the other two retain the prior vendor holder and
constructor String-key behavior. Its standard supporting checks also ran 16
constructed-method executions/20 refusals and eight original-r executions/four
refusals. First complete budgets are **5875** for the object holder and **5952** for
the prior vendor sentinel. The new DOM case checks two Node/VM observations, two
prepared entries, 14 preparation refusals and four incomplete-session refusals.

The String selector executes three new cases and two old sentinels in eight modes
each, retaining all proof controls. It skips the full export execution fixture and
its existing executable mutations. All **12 final code/test hashes** match the
devbox. Six C++ and four Python scoped formatting/syntax checks pass; 51 earlier
class source files and 98 earlier complete intrinsic cases are unchanged. The
String source oracle passes 121 Node observations, including the 12 documented VM
indexing/casing differences. Required repository formatting reports the same 16
existing diagnostics in untouched `ctdrive.cpp`, `HostContract/ProviderPaths.h`,
`PartialEvaluation/Heap.h` and `Symbolic/Facts.cpp`.

Builds used explicit targets `ctjs-opt`, `ctjs-translate`,
`ctcompile-test-host-contract` and `ctcompile-test-native-reference`, under the
shared devbox lock. No full CTest/compiler lit, complete captured-class replay,
DOM Strings replay, broad corpus/matrix, full Bootstrap, WPT/test262, Windows,
new sanitizers, local native build or push ran.

## Corrected checks

The first class selector expected reassignment to a fresh object to refuse.
Inspection showed two distinct SSA allocation origins, which the new proof safely
supports. That unchanged source became an admission and passed the final native
checks. The DOM fixture initially omitted required manifest arrays and then failed
to recognize the prepared entry's `private` spelling. Both harness errors were
fixed. The source proof also needed to retain primitive Boolean constants and
checked entry branches after key retirement. Finally, the unused captured observer
was already rejected by the complete closure census; its test now names that
actual diagnostic. No runtime semantics or oracle behavior changed.

Evidence: `/tmp/ctcompile-domkey-build{1,2,3,4,5,6,7,8}.log`,
`/tmp/ctcompile-domkey-format-final.log`, `/tmp/ctcompile-domkey-final.sha256`,
`/tmp/ctcompile-domkey-final-probes.log`, `/tmp/ctcompile-domkey-focused.py` and
`/tmp/ctcompile-string-subtraction-focused.py`.

## Exact next boundary

Compose prepared class construction and local child-Map record ownership with the
DOM Data provider's complete source and session-owner proof. The measured first
refusal is **`native DOM Data source: unsupported provider behavior through
ctjs.construct`**. `HostContract/Analysis.cpp` owns that source census;
`Analysis/OwnedGlobalRoots.cpp` currently requires one published binding/field,
and `OwnedGlobalMethods.cpp` builds the corresponding owned method table. The new
preparation fixtures use the existing shape-test manifest's placeholder root;
they are not a native session contract. Do not bypass those owner/input-family
checks or introduce a VM, GC handle, or a second browser implementation.

Constructor transport of nonliteral keys and alias partitions for multiple DOM
inputs remain separate proofs. Original B/Data+B still refuses. Broader String
operations, conditional callees, mutable captured cells, String ordering, document
views and the application driver remain unfinished. No full-Bootstrap admission
or new corpus coverage measurement is claimed.
