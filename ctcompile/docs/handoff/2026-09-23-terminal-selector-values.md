# Terminal iterator-close selector values, 2026-09-23

Resumed clean **78aacd8e** and retained source `f3f1e6c6` from HANDOFF,
current00, the earlier-terminal-read-values detail and iteration 108's journal.
No dirty drafts or unmerged `codex-wip-20260907` remained; unrelated branches
were preserved. Source, raw-host and escape agents worked in parallel. The user
resumed this iteration after an interruption; saved work continued in place.

**8fcad4df** allows one protected write to consume an earlier terminal `matches`
Boolean. The selector joins the existing read/use census. Its one consumer
removes it from unused-result suppression and requires the existing literal
selector syntax proof. Cloning retains the original source position and guard;
complete typed DOM/Style reproof excludes receiver failure, coercion and reentry.
Every operand use remains checked, so leaks and reuse refuse. The original saved
body exception still wins. No browser implementation or runtime fallback changed.

The original source is unchanged, SHA-256
`f3f1e6c62f7ab403f59b5f4a43015ab7fa5a17731bce8e868280d001233c6312`.
Getter `dfac177f` and order variant `ac276d12` also execute. The order variant
captures the second selector before the last `data-closed=false` write, so its
saved Boolean is false despite later reads. Generated C++ preserves that value
through the final `data-after-terminal` write. Raw tests cover two saved selector
positions, direct calls, typed failures, extra uses, reuse, suppression and exact
budgets. One historical raw refusal was promoted with its body unchanged and an
independent Node witness of all 13 calls and final attributes.

**7ad4962a** adds multiplication to existing bounded varying-operand refinement.
Subdivision must prove every accepted range with the unchanged scalar product
transfer. It retains intermediate bounds, complete reload/store census,
independent producer reload-gap proof, restored induction bounds, shared work
budget and actual-write replay. The `i * (2 - i)` tests exercise an interior
maximum; `(i - 1) * (i - 1)` exercises an interior minimum. Saved/unvisited
children retain identity. Growth, overlapping reloads, later producer stores,
fractional keys and unbounded intermediates remain conservative refusals.

Focused validation on the devbox:

- Exact `ctcompile_host_contract`: **1/1 PASS, 2.73 s test / 2.74 s total**.
- Three selected saved-throw sources: **48 native executions, 76 refusals and
  24 Node/VM observations PASS**, both browser providers and optimization policies,
  GCC/Clang and explicit/deduced C++. Preflight admits three positives and refuses
  ten controls under both policies. Execution selects the committed fixture.
- Exact `ctcompile_escape_analysis_arrays`: **1/1 PASS, 2.64 s test / 2.65 s total**.
- Exact `Analysis/Escape/escape-claims/power-index-overwrite.test`:
  **1/1 PASS, 0.17 s**; 355 other lit cases excluded.
- `tools/format.sh --check`: **1126 C++, 157 Python, 114 web files PASS**.
  Scoped formatting, Python/Node syntax, scratch `bash -n` and diff checks pass.
- All seven final code/test hashes match the devbox. All 24 generated C++ files
  retain both selectors, ordered reads/writes, the saved selector Boolean and
  original body exception, without Script/VM or nullable fallback. Fixture
  standalone and linked checks pass.

All focused gates passed on their first run. No expectations changed after a
failed gate. Three agents independently reviewed the native production change
without actionable findings; the parent reviewed both production changes and
integrated the source candidate only after preflight.

Preserved 233 historical iterator bodies, 86 metadata rows, 75 saved cases,
149 retained saved refusal bodies and 75 historical oracle constructions.
Local Node witnesses cover 12 positive and 20 refusal observations. Escape adds
18 raw and 11 source controls; all historical raw bytes and 263 source bodies
and CHECKs are unchanged. Syntax for 274 source functions and 11 exact Node
write/read/own-key/identity witnesses pass. The growth witness writes indices
0, 3, 4, 3, 0 and exits at length five; the fractional witness creates own key
`0.75`. The bounded analysis conservatively refuses the unbounded intermediate
even though the final product is zero.

Builds used `tools/remote-build.sh` with explicit targets under the shared devbox
lock. Host targets: `ctjs-opt`, `ctjs-translate`, `ctcompile-test-host-contract`,
`ctcompile-test-native-reference`. Escape targets: `ctjs-opt`,
`ctcompile-test-escape-analysis-arrays`, `ctcompile-test-escape-claims`,
`ctcompile-test-type-oracle`. Commands, selectors, witnesses, artifacts and
checksums are in `../test-results/2026-09-23-terminal-selector-values/` relative
to the monorepo.

Initial/final availability checks inspected 14/18 accessible Linux processes
and 346/350 Windows processes. No executable/CLI/loop matches appeared, but 61
Linux executable-link permission errors left Claude status uncertain.
Concurrent-agent rules remained in force. No browser/shared implementation edits,
local C++ builds, pushes or history rewrites occurred.

Full CTest/compiler lit, complete iterator fixtures, unaffected native replays,
broad corpus/matrices, full Bootstrap, browser/WPT/test262, Windows and sanitizers
were skipped. No full-Bootstrap coverage gain is claimed.

**Next:** retained `unsupported-terminal-earlier-read-write-before-selector`,
SHA-256 `abc08f6e295ef84717d9d294826af5a3372f99b74eaafdb13c025795e2ef4f19`,
refuses **DOM protected helper needs an independent inert-body proof** under both
policies. Preserve `hasAttribute('data-closed')` captured immediately before the
second `matches`, across that selector and an ignored `hasAttribute`, for the final
`data-after-terminal` write. The original body exception must still win.
Single-write selector cleanup, nonterminal exceptional state, multiple protected
regions, implicit cleanup, nested custom iterators, unguarded Bootstrap defaults,
the application driver, full native Bootstrap and general powers remain.
