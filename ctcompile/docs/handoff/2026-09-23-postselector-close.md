# Writes after iterator-close selectors and power congruence, 2026-09-23

Resumed clean **1bf155c0** and retained `unsupported-after-selector-effect`
source `6d1b5000` from HANDOFF, current00 and iteration 97's final journal.
No dirty drafts or unmerged `codex-wip-20260907` remained; unrelated branches
were preserved. This iteration was interrupted during its first build. The
resumed session continued the existing edits and frozen source candidate.
Parallel agents prepared source regressions, escape analysis and independent
native review; the parent completed gates, reconciliation and commits.

## Landed

**ca257905** admits the original third-write source unchanged. Its final
`setAttribute` follows the unused `matches` call at the original source guard.
The method and result must be confined, and the write keeps a separate exact
suppression region. Existing complete DOM/Style reproof independently validates
all three writes and the selector, including each receiver, primitive arguments,
literal name and selector syntax. Private callable/frame checks, original saved
Boolean ownership, ordinary-close validation and charged cloning remain.
No browser implementation changed.

Original source SHA-256:
`6d1b50006a02ae3ffa9c40e247f565ade1b71b6f43605c55afb8e9d9dc9ac03c`.
Getter `d441ee98` and literal-write variant `0997103f` also execute. The latter
writes true, then false, evaluates `[data-closed=false]`, then writes true.
Node/VM shims record selector order; raw IR assertions retain the source guard
and all four ordered suppression regions. Generated C++ retains its selector
and following write; execution checks retain saved false/true snapshots,
exhaustion, normal completion, reentry, two documents and owned sessions.

Parallel **f31c88dc** preserves the union lattice for a bounded base or one:
its stride is the gcd of the base stride and its distance from one. This lets
existing exact division prove composed keys such as
`(((2*i+1)**(i%2))-1)/2`. No new scalar powers are admitted. Whole-key refinement,
complete reload/store census and actual-write replay still determine which child
identities escape. All 145 historical source bodies/CHECKs and raw cases remain;
twelve source and fifteen raw CFG/SCF controls were added.

## Focused validation

- Exact `ctcompile_host_contract`: **1/1 PASS, 2.43 s test / 2.44 s total**.
- Three selected saved-throw sources: **48 native executions, 80 refusals,
  24 Node/VM observations PASS**, both providers and optimization policies,
  explicit/deduced C++, GCC and Clang. The selector runs the committed fixture.
- Exact `ctcompile_escape_analysis_arrays`: **1/1 PASS,
  2.33 s test / 2.34 s total**.
- Exact `Analysis/Escape/escape-claims/power-index-overwrite.test`:
  **1/1 PASS, 0.14 s**, with 355 other lit cases excluded.
- Full `tools/format.sh --check`: **1126 C++, 157 Python, 114 web files PASS**;
  `git diff --check` passes.
- All **seven final code/test hashes match the devbox**. All **24 generated
  C++ files** retain one selector call and a following attribute write, with no
  Script/VM protocol or nullable-scalar fallback. Linked-symbol checks pass.
- Source preservation retains **233 historical iterator bodies, 86 metadata
  rows and all 36 previous saved cases**. Recorded local checks cover twelve
  positive and 22 refusal Node observations; the resumed session reran only six
  changed observations. Escape checks preserve all 145 earlier source bodies
  and CHECKs and pass 157 local Node outputs and own-key sets.

The initial host build was interrupted after configuration; no completion was
claimed. Its resumed gate passed without a production correction. Review found
that a new positive test candidate also introduced an extra `hasAttribute` read
after the selector. Preflight confirmed that exact body (`fcad547a`) still refuses;
it is retained as a refusal control. The positive ordering variant instead uses
an independent literal final write. The original `6d1b5000` body was never changed.
Independent review found no remaining issues. No test failure was hidden by a
runtime change or by weakening an existing expectation.

An initial unprivileged process check was incomplete. Subsequent WSL-root checks
inspected 82 then 85 Linux processes; Windows CIM inspected 350 then 357.
Executable, CLI and loop identity checks found no matches or errors: Claude was
confirmed stopped. There were no browser/shared implementation edits, local C++
builds, pushes or history rewrites.

Commands, focused selectors, generated artifacts and checksums are preserved in
`../test-results/2026-09-23-postselector-close/` relative to the monorepo. Full
CTest/compiler lit, complete iterator fixtures, unaffected native replays, broad
corpus/matrices, full Bootstrap, browser/WPT/test262, Windows and sanitizer runs
were skipped under the focused policy.

## Next boundary

Retained `unsupported-third-postselector-result` source
`8038c1f85c56ca2ee4800089c7e3379f9a29643668ede6bb7405f58d0bd686de`
still refuses **DOM protected helper needs an independent inert-body proof**
under both policies. It saves `matches('[data-closed]')` into a Boolean used by the
following attribute write. Prove that result transport without losing source
order, selector validation or the original saved body exception.

The extra post-selector read source
`fcad547a0a87a8302831d843ef17a6cf52bcbfa20c0b5e7b3511d4079e09178f`
also retains that refusal. Single-write selector cleanup, nonterminal exceptional
state, multiple protected regions, implicit cleanup, nested custom iterators,
unguarded Bootstrap defaults, the application driver, full native Bootstrap and
general powers remain unfinished. No full-Bootstrap coverage gain is claimed.
