# Own-key clearing loops and signed-shift bands — 2026-09-20 UTC

Continued clean `ff9b3b95` after the required history, protocol and claim reads.
No interrupted dirty draft or September 7 WIP remained. Service interruptions
resumed these same tasks. Independent agents supplied escape implementation,
native fixtures and proof review; root reconciled, validated and committed them.

Linux executable/argv checks found no Claude identity among 72 processes, but
56 unread executable links prevented confirming availability. Windows
Get-CimInstance succeeded with 344 processes and no Claude identity. Concurrent
restrictions remained; no browser/runtime changes or WPT/test262 measurements.

## Landed

- `03fcd7dd`: signed right-shift indices reuse the existing ToInt32 band
  classifier and bounded bitwise transfer. Exact numeric endpoints, one
  conversion band, divisible stride, complete reload/alias checks and proof
  budgets remain required. Thirteen CFG/SCF positives, eight refusals and
  thirteen source cases accompany the proof; the original 33 source bodies
  remain unchanged.
- `9f367dc5`: fixed own-key snapshots support original for-of null clearing.
  A bounded proof evaluates only the snapshot's scalar iterator machinery using
  existing compiler primitive semantics, recording null stores in source order.
  The complete source census checks both arms before evaluation; runtime
  dependencies, unknown effects and roots inside the eliminated segment refuse.
  Original Array/helper identities are explicit obligations and pass the
  original module's binding census. Scalar replacements preserve cached local
  producers. Generated code contains direct field assignments, with no iterator
  object, snapshot container or VM dependency.

Six new positives and two unchanged historical loop promotions add **64 native
executions**. They cover shared inherited methods, conditional fixed shapes,
empty snapshots, surrounding effects, repeated clearing and a break after one
store. The break source was initially a proposed refusal; the existing bounded
trace proves its exact completion without an implementation change.
Return, throw, escaped snapshots and unknown unused effects remain refused.
All original class source sections 01–14 remain unchanged.

The Array iterator replacement control records **Node 0 / ctbrowser 11**.
The VM's array fast path ignores that replacement; native preparation refuses
the changed identity. The source and runtime were preserved. Authentic Data+B
still refuses `class own-key snapshot requires fixed constructor fields`.

## Focused validation

All devbox commands held `/tmp/ctbrowser-devbox-build.lock`. Commands, logs,
hashes and inspected C++ samples: `/tmp/ctcompile-own-field-loops-0630/`.

- Built explicit targets `ctjs-opt`, `ctjs-translate`,
  `ctcompile-test-host-contract`, `ctcompile-test-native-reference`,
  `ctcompile-test-escape-analysis-arrays`, `ctcompile-test-escape-claims` and
  `ctcompile-test-type-oracle`.
- Exact `ctcompile_escape_analysis_arrays`: **1/1**, 1.39s (1.40s total).
- Lit filter `^ctcompile :: Analysis/Escape/escape-claims/(right-shift|bitnot|left-shift)-index-overwrite[.]test$`:
  **3/3**, 0.15s. Right shift: **138 observed sites / 27 sound / 27 of 35
  confined precision**, zero violations, partial, pending or unclaimed sites.
- Exact `ctcompile_host_contract`: **1/1**, 0.50s (0.51s total).
- Selected class probe, names beginning `own-fields-`, `inherited-own-fields-`
  or `bootstrap-base`: **60 source observations / 200 main native executions /
  120 unprepared refusals / 53 preparation refusals**. GCC/Clang, explicit and
  deduced output, and both optimization settings are covered. Complete budgets:
  loop **1,333**, ordered fields **763**, shared inherited fields **2,702**,
  conditional inherited arguments **4,050**. Missing iterator identities,
  forged inputs, rooted loop/super controls and bounded partial-proof refusals
  pass. Ancillary checks: 16 constructed-method executions / 20 refusals and
  eight original-r executions / four refusals. This is not a whole class-lit pass.
- All ten native and four escape tested hashes match. Inspected ordinary,
  repeated and break output: stack records, borrowed receiver pointers, direct
  assignments and existing nullable primitive scalars; no Script/VM symbol,
  runtime iterator, handle table, prototype storage or collector.
- Required `tools/format.sh --check`: **20 pre-existing diagnostics in six
  HEAD-identical files**, unchanged from the starting baseline. Changed C++
  formatting, Python Black/AST, Node fixture checks and `git diff --check` pass.

The first build caught a local C++ variable redeclaration, corrected before the
passing build. An interrupted initial probe produced no claimed result. Later
probes exposed the Array oracle difference and proved the proposed break
refusal; their expectations were corrected with sources unchanged. The final
60-source probe passes, including missing-authority, root and budget controls.

Skipped: full CTest/compiler lit, whole class-initialization lit, DOM replay,
broad native/corpus matrices, full Bootstrap, WPT/test262, Windows and sanitizers.
The new loop proof is restricted to closed-source fixed-field classes; shared
contract changes were covered by the exact host test. No push.

## Next boundary

The original Data+B constructor still fails the fixed-field source census.
Its helper/config/registration operations fail before this diagnostic can
isolate variable field presence; do not infer that conditional presence alone
is the remaining blocker. Prove those complete effects and per-instance own-field
presence, then connect the now-supported clearing loop without removing
`e.remove`, `P.off` or other original disposal effects.

Shared nested Map ownership, stored receiver lifetime and duplicate-registration
effects remain unproved. Inherited getters/DOM, static construction,
config/selectors/events/Popper, broader ownership and the application driver
also remain unfinished. Cross-band shifts and nondivisible index strides retain
their conservative escape refusals.
