# Complete Data holders and division String indices, 2026-09-21 UTC

Continued clean `29057f08` from the recorded conflict-observer boundary.
No interrupted code remained; `codex-wip-20260907` was already an ancestor.
Linux cmdline/comm checks read 69 processes without unreadable records; Windows
CIM read 357. Neither found a Claude executable, Node CLI or loop. No browser or
shared implementation changed. Parallel agents supplied String work and proof
review; root completed the fixture task after its agent hit service limits.

## Landed

`a96d7d2a` recognizes the complete `console.error` expression containing
`Array.from(child.keys())[0]` in a branch. Recognition grants neither builtin
identity nor permission to execute it. Captured helper expansion retains that
arm, and ordered nested-Map analysis must prove it unselected at every invocation.
The complete child/cell/alias census and record-owner proof remain required.
Reachable diagnostics, extra effects, unknown keys and escaped owners refuse.
Three admissions cover direct calls, Data holders and constructor publication;
eight controls retain real conflicts and hidden observers.

`cd8fb905` proves one division of two direct Number literals used only as
`charAt`/`slice` bounds. Ordinary numeric division needs no coercion machinery;
NaN becomes zero before unsigned conversion, while infinities retain the existing
clamp. Two former refusal bodies execute unchanged, alongside Unicode and captured
Symbol-description witnesses. Dynamic/coercing or nested arithmetic and raw NaN
globals remain refused. First-unit casing and dataset-tail authority still require
the original direct Number constants.

`fb5b8173` finishes the original Data arrow getter. Saving lexical `this` is inert
when the existing receiver/new-target checks and complete body/callee census prove
it unread. Only the redundant undefined-enclosing-receiver guard was removed.
Live receiver reads and unused holder observers still refuse. Two fixtures retain
the **complete vendor Data declaration**, including its arrow getter, conflict
message and early-return removal; the harness checks that declaration against the
vendor source. Both ordinary calls and constructor publication execute natively
with literal String element keys and unreachable conflicts. Saved child/record
aliases, nullable reads and cleanup/recreation retain their owners.

## Focused validation

| Check | Measured result |
| --- | --- |
| Final CTest `ctcompile_host_contract` | 1/1; 0.57 s, 0.58 s total |
| `CTNative/Lowering/Objects/class-captured-map-helpers.test` | PASS; 397.61 s |
| `CTNative/Lowering/Objects/class-terminal-publication.test` | PASS; 148.26 s |
| `CTNative/Browser/native-dom-strings.test` | PASS; 165.08 s |
| Final vendor Data selector | 6 observations, 32 main native executions, 12 unprepared and 11 preparation refusals |
| Final division String selector | 48 native executions, 678 proof refusals, 106 Node/VM agreements and 11 known differences |
| Final original B/Data+B check | Two preparation refusals and four native refusals |

The two class lit cases passed together in **397.62 s** before the arrow-only
follow-up. Captured helpers measured **235 source observations, 464 main native
executions, 470 unprepared refusals and 311 preparation refusals**; the new conflict
budget was **8452**. The final vendor selector then checked its four new bodies
and two previously admitted method/getter sentinels. Two vendor admissions account
for 16 of its 32 executions; first complete vendor-holder budget is **5952**.
Each class invocation also ran the existing constructed-method controls (16
executions/20 refusals) and original helper guards (eight/four).
The whole captured/terminal selection was not replayed after the arrow extension.

The String selector checked four new admissions and two existing index sentinels
in eight modes each, with all proof controls. It skipped the whole export execution
fixture and its existing executable mutations. DOM Strings retained 809 Node/VM
observations and eight native binaries. All eight final code/test hashes match
the devbox. All 48 pre-session class source files are unchanged; the arrow follow-up
also preserved file 49. Fifteen new class Node observations, 117 String Node
observations, Python AST/import checks and scoped C++/Python formatting passed.
The required repository formatter still reports 16 existing diagnostics in
untouched `ctdrive.cpp`, `HostContract/ProviderPaths.h`, `PartialEvaluation/Heap.h`
and `Symbolic/Facts.cpp`.

All builds used explicit targets `ctjs-opt`, `ctjs-translate`,
`ctcompile-test-host-contract` and `ctcompile-test-native-reference`, under the
shared devbox lock. The conflict implementation compiled three steps, String six,
and the arrow follow-up three; other syncs required no compilation.

## Corrected probes

The first conflict selector completed its three admissions before the reference
runner reported that `console` is absent on actual conflicts. Those two refused
controls now explicitly retain that interpreter exception; Node observes the
conflict while the compiler refuses the reachable diagnostic. The corrected
11-case selector passed 24 main executions, 22 unprepared and 16 preparation
refusals. No runtime behavior or native expectation was changed to match an oracle.

Review caught an analysis-to-lowering dependency in the initial String draft.
It was removed before the build: exact Number division uses primitive arithmetic.
A subsequent probe incorrectly expected early-return removal to refuse and stopped
the launcher when preparation succeeded. No compiler failure occurred. The corrected
probe, complete vendor source and a variant changing only its arrow getter to a
method isolated the redundant capture guard; the final native checks validate its
removal. The earlier proposed early-return limitation was not a measured blocker. A final
B/Data+B probe also corrected its function count to use raw import assembly
instead of generic structured assembly; this was a probe setup error.

Evidence: `/tmp/ctcompile-conflict-build{1,2,3,4}.log`,
`/tmp/ctcompile-conflict-lit.{log,json}`, `/tmp/ctcompile-division-build{1,2}.log`,
`/tmp/ctcompile-division-dom.json`, `/tmp/ctcompile-vendor-build1.log`,
`/tmp/ctcompile-vendor-format.log`, `/tmp/ctcompile-conflict-final.sha256` and
`/tmp/ctcompile-conflict-final-check.log` and
`/tmp/ctcompile-original-base-final2.log`. Temporary selectors are
`/tmp/ctcompile-conflict-focused.py`, `/tmp/ctcompile-string-division-focused.py`
and `/tmp/ctcompile-vendor-focused.py`.

No full CTest/compiler lit, broad corpus or native matrix, full Bootstrap,
WPT/test262, Windows, new sanitizers, local native build or push ran.

## Exact next boundary

Use the complete vendor Data holder with actual DOM element identities and public
DOM/HostContract ownership. Literal String outer keys are still required here;
reachable conflict reporting still refuses. The full original B/Data+B specimen
remains refused, so neither full-Bootstrap admission nor a corpus coverage gain is
claimed. Broader String arithmetic/methods, conditional callees, mutable captured
cells, String ordering, document views and the application driver remain open.
