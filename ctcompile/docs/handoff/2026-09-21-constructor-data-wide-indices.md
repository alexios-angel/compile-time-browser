# Constructor Data publication and wide String indices, 2026-09-21 UTC

Continued clean `d5631824` from its recorded constructor-time registration boundary.
No native draft was left uncommitted; `codex-wip-20260907` was already merged.
The initial process census found no Claude session (72 Linux, 358 Windows processes).
Parallel agents supplied String work, fixtures and proof review; root continued
their saved work across interruption and owned all devbox validation and commits.

`f5b9fa07` moves an exact terminal three-argument registration call immediately
after each ordinary construction. Both literal key arguments retain their own
origins, and the receiver is the completed instance. The original callable,
fixed-cell, symbol and immutable holder-slot census precedes mutation. Every
moved helper must disappear through the existing captured Map-only expander;
generic helper admission cannot satisfy that obligation. Nested routing and the
concrete record-owner proof then account for all child and record aliases.
No publication crosses a constructor suffix with observable work; inherited
constructors, replacement returns and hidden callers remain refused.

Three new admissions cover repeated and distinct keys, replacement, called
set/get/remove holder slots, saved aliases through deletion/recreation, and
short-circuit allocation. The old file-44 constructor source executes unchanged.
Eleven complete-source refusals cover observers, exceptions, replacement returns,
unsupported keys, escaping child/record owners, reentry, an unused unsafe slot,
inheritance and a helper that cannot pass complete Map expansion. Two symbol
mutations reference the original constructor/helper; stale fingerprints, forged
authority and exact budget rollback use the existing harness. All 44 earlier
class source files remain byte-identical.

`cf37fb24` removes the uint32 bound for Number-literal String indices, including
overflow infinities. A branch guards the unsigned cast itself before length
clamping in `size_t`; signed fractional truncation and UTF-16 indexing remain
unchanged. NaN, dynamic and coercing origins still refuse. Exact `charAt(0)`
first-unit and `slice(1)` dataset-tail authority stays separate.
Sixteen former refusal programs execute unchanged; two additional witnesses
cover the size_t boundary, maximum finite Number, signed infinities and a saved
Symbol description. Native output continues to use public Core conversion and
ordinary C++ values, with no Script or VM dependency.

## Focused validation

| Check | Final result |
| --- | --- |
| Exact CTest `ctcompile_host_contract` | 1/1, 0.56 s; 0.57 s total |
| `CTNative/Lowering/Objects/class-captured-map-helpers.test` | PASS, 305.06 s |
| `CTNative/Lowering/Objects/class-terminal-publication.test` | PASS, 205.58 s |
| `CTNative/Exports/native-intrinsic-symbols.test` | PASS, 577.34 s |
| `CTNative/Browser/native-dom-strings.test` | PASS, 251.87 s |

These are four distinct lit cases, not a full-suite pass. The first completed
selection passed 4/4 in 577.35 s, with captured classes taking 389.10 s.
Review found that the new inherited refusal lacked its heritage identities;
the manifest and generic-assembly selection now include them, and the source
uses explicit `super(n)`. The corrected captured-only run passed 1/1 in
305.07 s. No compiler implementation change was needed after the initial pass.

Captured class checks measure **169 source observations, 360 main native
executions, 338 unprepared refusals and 219 preparation refusals**. Four admissions
add 32 executions; the new direct constructor budget is **3619**. The adjacent
terminal fixture measures 58 observations, 160 main executions, 116 unprepared
and 70 preparation refusals. Both class selections also run constructed-method
controls (16 executions, 20 refusals) and original helper controls (eight
executions, four refusals).

Intrinsic exports measure **648 native executions, 536 refusals and two mutations**,
with **89 Node/VM agreements and seven known casing/indexing differences**.
Eighteen admissions add 144 executions. DOM Strings measure **809 Node/VM
observations and eight GCC/Clang binaries**, plus 1,000 source refusals,
44 provenance/depth refusals, 24 method provenance checks, 241 capture
provenance/budget checks, 101 replacement checks, 22 branch checks and 31
completion checks.

All C++ builds and native executions ran on the devbox under the shared lock.
Explicit targets were `ctjs-opt`, `ctjs-translate`,
`ctcompile-test-host-contract` and `ctcompile-test-native-reference`.
The initial build compiled 14 steps and the captured-cell guard compiled three;
two final source syncs needed no rebuild. A heredoc launcher consumed its trailing
SSH command: that invocation counts only as a successful build, with no lit run.
The corrected launchers close build stdin and preserve logs on the devbox.

All **ten final code/test SHA-256 hashes** match the devbox. Five C++ and four
Python files pass scoped formatting; Python syntax/import and whitespace checks
pass. Local source-only checks pass 14 final class Node observations and 96
intrinsic Node observations. Required `tools/format.sh --check` retains **16
existing diagnostics** in untouched `ctbrowser/tools/ctdrive/ctdrive.cpp`,
`HostContract/ProviderPaths.h`, `PartialEvaluation/Heap.h` and `Symbolic/Facts.cpp`.
Full CTest/compiler lit, broad corpus/native matrices, full Bootstrap, WPT/test262,
Windows, new sanitizers, local native builds and push were skipped.

Evidence uses `/tmp/ctcompile-constructor-data`: `-build1.log` through
`-build4.log`, `-final.json`, `-final.log`, `-captured.json`,
`-captured.log`, `-ctest-final.log`, `-local.sha256` and `-format-final.log`.

## Exact next boundary

Compose three-argument Data registration with base-to-leaf completion.
`sinkCapturedPublication` currently admits only an ordinary constructor;
the older base publication path still expects a direct two-argument Map set.
Preserve the selected child owner, both key origins, all holder observers and
exception/reentry order when joining those paths. Original Data also needs
element-identity keys, conflict reporting, nullable lookup and conditional
child/empty-parent cleanup. Original B/Data+B remains refused with complete
sources. Dynamic/coercing String indices, NaN origins, broader String methods,
conditional callees, branch-mutated boxed locals, String ordering, document views
and the application driver remain. No full-Bootstrap admission or coverage gain
is claimed.
