// GENERATED by tools/gen/unicode_properties.py - do not edit.
// Unicode 17.0.0: UnicodeData.txt, DerivedCoreProperties.txt, PropList.txt,
// emoji/emoji-data.txt, DerivedNormalizationProps.txt, Scripts.txt,
// ScriptExtensions.txt, PropertyAliases.txt, PropertyValueAliases.txt,
// CaseFolding.txt, emoji-sequences.txt and emoji-zwj-sequences.txt.
// clang-format off

// Every range of every property, [lo, hi] inclusive, sorted and disjoint
// within one property; the indexes below say where a property starts.
constexpr rx_range rx_ranges[] = {
#include "regex_properties/rx_ranges-01.inc"
#include "regex_properties/rx_ranges-02.inc"
#include "regex_properties/rx_ranges-03.inc"
#include "regex_properties/rx_ranges-04.inc"
#include "regex_properties/rx_ranges-05.inc"
#include "regex_properties/rx_ranges-06.inc"
#include "regex_properties/rx_ranges-07.inc"
};

// General_Category, every value and grouping under every name.
constexpr rx_property rx_general_category[] = {
#include "regex_properties/rx_general_category-01.inc"
};

// Script, under the long and the four-letter name.
constexpr rx_property rx_script[] = {
#include "regex_properties/rx_script-01.inc"
};

// Script_Extensions: a code point with no entry extends its Script.
constexpr rx_property rx_script_extensions[] = {
#include "regex_properties/rx_script_extensions-01.inc"
};

// The binary properties of table 67, under every alias.
constexpr rx_property rx_binary[] = {
#include "regex_properties/rx_binary-01.inc"
};

// The properties of strings (table 69, `v` only): lone code points as
// ranges, and the sequences as {length, code points...} runs.
constexpr rx_string_property rx_strings[] = {
#include "regex_properties/rx_strings-01.inc"
};
constexpr char32_t rx_sequences[] = {
#include "regex_properties/rx_sequences-01.inc"
#include "regex_properties/rx_sequences-02.inc"
#include "regex_properties/rx_sequences-03.inc"
};

// CaseFolding.txt, statuses C and S: Canonicalize(ch) under `iu`,
// {code point, its simple fold} sorted by code point ...
constexpr char32_t rx_folds[][2] = {
#include "regex_properties/rx_folds-01.inc"
};
// ... and the same pairs as {fold, code point} sorted by fold, so a
// class can ask which code points share one.
constexpr char32_t rx_unfolds[][2] = {
#include "regex_properties/rx_unfolds-01.inc"
};
// clang-format on
