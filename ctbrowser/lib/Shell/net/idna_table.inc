// GENERATED by tools/gen/idna_table.py - do not edit.
// clang-format off

// IdnaMappingTable.txt, collapsed to the four statuses the URL
// Standard's parameters leave. A code point in no range is disallowed.
constexpr idna_range idna_ranges[] = {
#include "idna_table/idna_ranges-01.inc"
#include "idna_table/idna_ranges-02.inc"
#include "idna_table/idna_ranges-03.inc"
#include "idna_table/idna_ranges-04.inc"
#include "idna_table/idna_ranges-05.inc"
#include "idna_table/idna_ranges-06.inc"
#include "idna_table/idna_ranges-07.inc"
#include "idna_table/idna_ranges-08.inc"
#include "idna_table/idna_ranges-09.inc"
#include "idna_table/idna_ranges-10.inc"
};

// The mapped code points themselves, indexed by idna_range::map_at.
constexpr char32_t idna_mappings[] = {
#include "idna_table/idna_mappings-01.inc"
#include "idna_table/idna_mappings-02.inc"
};

// General_Category=M: a label may not BEGIN with one (4.1 criterion 4).
constexpr char32_t combining_mark_ranges[][2] = {
#include "idna_table/combining_mark_ranges-01.inc"
};

// Canonical_Combining_Class=Virama, the first ContextJ rule.
constexpr char32_t virama_ranges[][2] = {
#include "idna_table/virama_ranges-01.inc"
};

// Joining_Type L, D, R and T; anything else is U (the second
// ContextJ rule: (L|D) T* ZWNJ T* (R|D)).
constexpr joining_range joining_ranges[] = {
#include "idna_table/joining_ranges-01.inc"
};

// Bidi_Class as CheckBidi (RFC 5893) reads it: R, AL='A', AN='N',
// EN='E', ES='S', CS='C', ET='T', ON='O', BN='B', NSM='M'. A code
// point in no range is L - the default - and so is every class a
// valid label cannot carry.
constexpr bidi_range bidi_ranges[] = {
#include "idna_table/bidi_ranges-01.inc"
};
// clang-format on
