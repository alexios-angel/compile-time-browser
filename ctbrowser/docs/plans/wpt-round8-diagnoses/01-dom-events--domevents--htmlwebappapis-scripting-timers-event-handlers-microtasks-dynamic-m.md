[Back to wpt-round8-diagnoses.md](../wpt-round8-diagnoses.md)

## dom-events — dom/events + html/webappapis (scripting, timers, event handlers, microtasks, dynamic markup insertion)

Baseline: At 9f8da347 (from the JSON): dom/events (/tmp/m-9f8da347/dom-events.json) 176 files = 78 PASS / 9 FAIL / 4 TIMEOUT / 85 SKIP; subtests 660 PASS / 39 non-PASS. html/webappapis (/tmp/w-9f8da347/html.json filtered to html/webappapis/**) 349 files = 151 PASS / 86 FAIL / 64 TIMEOUT / 1 CRASH / 7 HARNESS_ERROR / 40 SKIP; subtests 790 PASS / 230 FAIL / 96 TIMEOUT / 41 NOTRUN (367 non-PASS). Combined [..]

### No realm per browsing context: a frame's scripts never run, its window is a proxy over the page's globals, errors are reported to the page's window

- **158 subtests / 76 files**, size L, overlaps REALM, confidence high
- Root cause: ctbrowser/lib/Shell/bindings/frames.cpp header still says it at HEAD ('THE REALM IS SHARED', 'NO SCRIPTS RUN IN A FRAME'); `rg run_script_ lib/Shell/bindings/frames.cpp` finds nothing. load_frame() builds contentWindow as a small object_object behind a proxy whose miss reads cx.global() (frames.cpp:340-372), so frame.contentWindow.Function === Function, `frames[0].onerror`/`frames[0].event` are plain props on that object, and every helper an iframe's <script> defines (`run`, `doParse`, `callDocumentMethod`, `runWindowOpenVeryIndirectly`, `frameState`, `listener`, `hey`) is undefined (17 subtests carry exactly that message). dispatch_error_value (events/input.cpp:85) always fires at the PAGE [..]
- Spec: HTML 8.1.3 realms/settings objects, 8.1.3.3 report an exception (relevant global of the callback), 8.1.3.5 entry/incumbent; ECMA-262 9.3, 10.2.3 step 4 ([[Realm]]), 10.2.1.1 PrepareForOrdinaryCall [..]
- Engine files: ctbrowser/lib/Shell/bindings/frames.cpp; ctbrowser/lib/Shell/browser/nested.cpp; ctbrowser/lib/Shell/browser/scripts.cpp; ctbrowser/include/ctbrowser/script/vm.hpp; ctbrowser/lib/Script/vm/run_loop.cpp; ctbrowser/lib/Shell/bindings/events/input.cpp (dispatch_error_value: report to the callback's realm's window); ctbrowser/lib/Shell/bindings/window/callbacks.cpp
- Evidence: html/webappapis/scripting/processing-model-2/integration-with-the-javascript-job-queue/promise-job-incumbent.html (11: '`runWindowPostMessageVeryIndirectly` is undefined'); html/webappapis/dynamic-markup-insertion/opening-the-input-stream/ignore-opens-during-unload.window.js (12 TIMEOUT); html/webappapis/dynamic-markup-insertion/opening-the-input-stream/no-new-global.window.js (8: 'expected "You" [..]
- Fix: Per round8-brief-REALM.md. For this slice the observable deliverables are: (1) run a frame's inline and src <script>s and inline handlers under the frame's realm at load_frame/settle_frame time; (2) closure_object records its realm; context::call switches globals for the call; (3) dispatch_error_value takes the realm of the throwing callback (listener/timer/rAF/microtask) and fires `error` at THAT realm's window object, with `window.event` set on that window only; (4) document.open()/write in a frame runs against the frame's bindings and realm (already routed via `[this]` in install_dynamic_markup; only the scripts are missing). Measure html/webappapis after batch 2 of the brief: ~76 files here are gated on it.

### structuredClone cannot serialize platform objects and has no transfer list

- **48 subtests / 1 files**, size M, overlaps none, confidence high
- Root cause: The cloner in ctbrowser/lib/Script/builtins/text/globals.cpp:150-410 says it: 'Platform objects (Blob, File, ImageData) are the host's: this engine cannot tell one from a plain object and copies it as one', so `structuredClone(new Blob())` returns a plain object (23 'instanceof Blob/File/FileList expected true got false'). There is no options.transfer (`rg transfer globals.cpp` finds nothing) so 'Resizable ArrayBuffer is transferable' etc. see byteLength 16 not 0 and detached-buffer transfer never rejects (6). A detached/OOB DataView throws the VM's RangeError instead of DataCloneError (2). MessagePort/ImageBitmap/OffscreenCanvas/ReadableStream/Response do not exist (8). 'A subclass [..]
- Spec: HTML 2.7.3 StructuredSerializeInternal (platform objects with [Serializable]), 2.7.6 StructuredSerializeWithTransfer / 2.7.7 StructuredDeserializeWithTransfer, [..]
- Engine files: ctbrowser/lib/Script/builtins/text/globals.cpp (cloner, install_structured_clone); ctbrowser/include/ctbrowser/script/vm.hpp (a host serialize/deserialize hook beside set_rejection_tracker); ctbrowser/lib/Shell/bindings/window/window.cpp (Blob/File/FileList/ImageData/DOMException as [Serializable]); ctbrowser/lib/Shell/bindings/exceptions.cpp
- Evidence: html/webappapis/structured-clone/structured-clone.any.js (48 of 152: 23 Blob/File/FileList instanceof, 11 createImageBitmap/OffscreenCanvas/MessageChannel/ReadableStream/Response not defined, 6 transfer, 2 DataCloneError type, 1 BigInt, 5 misc)
- Fix: Add context::set_platform_serializer(std::function<bool(value, serialized&)>, deserializer) that the cloner consults for any object with a host brand before falling to the plain-object walk; lib/Shell registers Blob (bytes+type), File (+name,lastModified), FileList, ImageData, DOMException, DOMPoint/DOMRect/DOMMatrix, keyed by the prototype chain's nearest registered interface (gives the subclass and deleted-global cases for free). Parse `options.transfer` in structuredClone: ArrayBuffer -> new buffer, source detached (byteLength 0, later access throws TypeError); throw DataCloneError (via the existing throw_data_clone_error) for a detached or non-transferable value, and map the VM's OOB DataView RangeError to DataCloneError inside the cloner. MessagePort/ImageBitmap/OffscreenCanvas stay long tail.

### setHTMLUnsafe ignores its options: runScripts, template contents target, HTML parser in XML documents

- **23 subtests / 3 files**, size M, overlaps none, confidence high
- Root cause: set_html_unsafe (ctbrowser/lib/Shell/bindings/shadow_dom.cpp:324-350) takes only the markup; the binding at :505 passes arg 0 and never reads args[1]. The fragment is parsed into a scratch document with `doc_->scripting()` and copied, so a <script> in it is inert even with {runScripts:true} (18 subtests: 'assert_true: expected true got false' + TIMEOUT/NOTRUN waiting for the external script's load; ordering vs custom element reactions 2). For a <template> target the children go under the template element instead of its content fragment (1: 'template should not have any child nodes'). In an XML/SVG document `parse_html_fragment(..., txn.element_ns(target))` yields '' because the context [..]
- Spec: HTML 8.6.2 setHTMLUnsafe(html, options) and 'run scripts' steps (whatwg/html#12495), 4.12.1.1 prepare the script element, 4.12.1.2 execute, 8.6 'template' target
- Engine files: ctbrowser/lib/Shell/bindings/shadow_dom.cpp (set_html_unsafe, install_shadow_dom setHTMLUnsafe binding); ctbrowser/lib/Shell/bindings/document/entry.cpp (run_inserted_scripts / execute_script_element - reuse for 'run scripts' in tree order); ctbrowser/lib/DOM/treebuilder.cpp (fragment parsing with an XML context element)
- Evidence: html/webappapis/dynamic-markup-insertion/html-unsafe-methods/setHTMLUnsafe-runScripts.html (20: Element 10 + ShadowRoot 10); html/webappapis/dynamic-markup-insertion/html-unsafe-methods/setHTMLUnsafe-xml.html (2); html/webappapis/dynamic-markup-insertion/html-unsafe-methods/setHTMLUnsafe.html (1, template case)
- Fix: Read `SetHTMLUnsafeOptions {runScripts}` from args[1]. Parse the fragment with the scripting flag = runScripts (so <noscript> etc. behave); after the copy, if runScripts: collect every <script> in the inserted subtree in tree order, mark them 'already started' cleared, then run prepare-the-script-element for each (inline: execute now with currentScript rules; src: fetch via assets_ and run in a later task with load event) - the existing execute_script_element does the run; do it BEFORE custom element reactions are flushed (test expects 'run-scripts' before 'element-1-ctor'). Template target: if target is <template>, the copy destination is its content fragment's node (the same as innerHTML on a template). XML documents: always parse as HTML fragment with context element name only (HTML 8.6.2 'HTML fragment parsing algorithm' regardless of document type) - pass node_ns::html for the [..]

### Only one activation target must run: <area href> never follows its link, and a nested form's submit reaches the outer form's onsubmit

- **18 subtests / 1 files**, size S, overlaps none, confidence medium
- Root cause: browser::follow_link (ctbrowser/lib/Shell/browser/actions.cpp:132-145) walks ancestors looking for `atoms_.intern_lower("a")` only, so activate() for an <area> (actions.cpp:23 'tag == a || tag == area' -> follow_link) finds no href and no hashchange fires: all 10 '<AREA> child of ...' cases get [] instead of the '#testN_link' URL. The 8 nested-form cases (an inner <form> appended inside the outer form's <input type=submit>/<button>) record BOTH forms: the engine dispatches `submit` at the inner form with bubbles=true and the outer form's onsubmit content attribute runs with `this` = outer form; browsers record only the inner one (unverified whether they fire submit non-bubbling across a [..]
- Spec: HTML 4.8.14 the area element (activation behaviour = follow the hyperlink), DOM 2.9 dispatch steps 5.5-5.9.11 (one activation target), HTML 4.10.21.3 form submission algorithm
- Engine files: ctbrowser/lib/Shell/browser/actions.cpp (follow_link: accept `area`; submit/activate); ctbrowser/lib/Shell/bindings/events/dispatch.cpp (has_activation_behavior / run_activation_behavior)
- Evidence: dom/events/Event-dispatch-single-activation-behavior.html (18 of 132: 10 AREA cases 'expected ["...#testNN_link"] got []', 8 nested FORM submit/reset cases 'expected length 1 got 2')
- Fix: follow_link: match `a` OR `area` (HTML 4.8.14: an <area> with href is a hyperlink with the same activation behaviour); the fragment branch then fires hashchange as today. For the form cases, measure what happens with `<form onsubmit="log(this)"><input type=submit><form onsubmit=log(this)><input type=submit id=x></form></input></form>` in Chrome; likely fix is in browser::submit: the form submission algorithm's 'submit' event must not be observed by an outer form - i.e. the engine must not treat the outer form as a second activation - rather than a change to bubbling.

### document.open() on a secondary document (frame / createHTMLDocument) does not rebuild the tree and does not clear the frame's listeners

- **16 subtests / 2 files**, size S, overlaps REALM, confidence medium
- Root cause: Not a realm issue: the createHTMLDocument case fails identically. document_open (ctbrowser/lib/Shell/bindings/document/write.cpp:293-345) removes the children then `parser_->begin({}, true, !secondary_)` and a following `write("<!DOCTYPE html>")` leaves childNodes.length 0 ('after write: doctype node in document expected 1 but got 0', 6 cases) - tree_builder::begin's `start(input, {}, node_ns::html)` + the doctype insert `builder_->insert_before(doc_->document_node(), doctype, root_)` (lib/DOM/treebuilder.cpp:999-1007) do not produce a document child on a document whose element was removed and not eagerly recreated (eager_root=false for a secondary). Steps 7-8 of the open steps erase [..]
- Spec: HTML 8.4.2 document open steps (steps 7-8 remove listeners on document, nodes and window; step 9 replace all; steps 13-16 new parser), 13.2.6.4.1 'initial' insertion mode
- Engine files: ctbrowser/lib/Shell/bindings/document/write.cpp (document_open); ctbrowser/lib/DOM/treebuilder.cpp (begin/pump for an open stream on an empty document; doctype insertion at :999-1014); ctbrowser/lib/Shell/bindings/events/dispatch.cpp (which bindings own a listener added on a secondary document's nodes); ctbrowser/lib/Shell/bindings/frames.cpp (frame window focus())
- Evidence: html/webappapis/dynamic-markup-insertion/opening-the-input-stream/active.window.js (7: 6x 'after write: doctype node in document expected 1 but got 0' + 1 TIMEOUT); html/webappapis/dynamic-markup-insertion/opening-the-input-stream/event-listeners.window.js (9: 3x 'window event listener not removed', 1x 'assert_not_equals: got disallowed value null', 3x '`focus` is undefined', 2 TIMEOUT)
- Fix: (a) In tree_builder::begin for open=true with no eager root, make sure `start()` resets root_/insertion-mode to 'initial' so the first written doctype/element becomes a child of the Document (unit test: createHTMLDocument().open(); write('<!DOCTYPE html>'); childNodes.length==1; close(); ==2). (b) document_open must erase listeners for this document's nodes/window/document wherever they are stored: erase_if over primary().listeners_ (and this->listeners_) by owner-of-node == this, plus this document's window bucket. (c) frame_window.focus/blur as no-op natives (HTML 6.6.5) so the standard-listener cases reach their assertions. Batch with REALM's batch 3 (document.open in a frame).

### Setting the details `open` attribute must queue the toggle task (the WPT queueTask() idiom)

- **13 subtests / 2 files**, size S, overlaps none, confidence high
- Root cause: `toggle` is dispatched only from browser::toggle_details (ctbrowser/lib/Shell/browser/actions.cpp:97) on a click. HTML 4.11.1 says the details toggle event task is queued whenever the `open` attribute is added/removed. support/promise-rejection-events.js:885-891 implements queueTask(f) as `d = createElement('details'); d.ontoggle = f; d.setAttribute('open','')` on a DISCONNECTED element, so at HEAD - where round seven's unhandledrejection landed - the 7+ subtests that use queueTask still never resume.
- Spec: HTML 4.11.1 the details element ('queue a details toggle event task' whenever the open attribute is added or removed; details toggle task tracker)
- Engine files: ctbrowser/lib/Shell/bindings/events/dispatch.cpp (settle_attribute_writes: add an `open` attribute-change hook); ctbrowser/lib/Shell/browser/actions.cpp (toggle_details: share the task); ctbrowser/lib/Shell/bindings/document/entry.cpp (mutated(): attribute write notes)
- Evidence: html/webappapis/scripting/processing-model-2/unhandled-promise-rejections/promise-rejection-events.html (7 subtests naming queueTask: 'delayed handling: a nested-queueTask ...' x3, 'mutationObserverMicrotask vs. queueTask', 'queueTask ordering ... (1)' and '(2)', 'a nested-task delay' - all TIMEOUT at baseline, and gated on this at HEAD); [..]
- Fix: In settle_attribute_writes (or a new `details` hook in mutated()): when a write note is an attribute edit of `open` on an HTML <details>, if no toggle task is pending for that element, queue a task (add_timer 0 / the task queue) that fires a non-bubbling ToggleEvent {oldState, newState} at the element - connected or not. toggle_details then just sets/removes the attribute and lets the hook fire. Unit test: disconnected details, ontoggle, setAttribute('open') -> fires next tick, exactly once for two attribute flips.

### No postMessage / MessageChannel / MessagePort

- **7 subtests / 4 files**, size M, overlaps none, confidence high
- Root cause: window.cpp:928-936 says `postMessage` is 'deliberately still absent' and there is no MessageChannel anywhere (`rg MessageChannel lib` = 0 hits outside that comment). Every test that uses the standard 'queue a task' idioms fails on the first line: tasks.window.js (4), event-handler-handleEvent-ignored (1), EventListener-incumbent-global-1/2 (2, also need the www1 host), structured-clone MessagePort (2), promise-job-incumbent's sanity checks (REALM). With frames now real (contentWindow exists synchronously) the 2020 argument for leaving it out no longer holds.
- Spec: HTML 9.3.3 window.postMessage (steps 6-8: queue a global task on the posted message task source), 9.4 MessageChannel/MessagePort (port message queue, 9.4.4 message port post message steps)
- Engine files: ctbrowser/lib/Shell/bindings/window/window.cpp (window.postMessage); ctbrowser/lib/Shell/bindings/frames.cpp (frame_window.postMessage); ctbrowser/lib/Shell/bindings/events/interfaces.cpp (MessageEvent exists? add MessageChannel/MessagePort interfaces); ctbrowser/lib/Shell/bindings/window/callbacks.cpp (the task that delivers)
- Evidence: html/webappapis/dynamic-markup-insertion/opening-the-input-stream/tasks.window.js (4: 2x '`postMessage` is undefined', 2x 'MessageChannel is not defined'); html/webappapis/scripting/events/event-handler-handleEvent-ignored.html (1); dom/events/EventListener-incumbent-global-1.sub.html + -2 (2, '`postMessage` is undefined'); html/webappapis/structured-clone/structured-clone.any.js (2 MessagePort [..]
- Fix: window.postMessage(message, targetOrigin|options, transfer): structuredClone the message, queue a task (add_timer 0 on the primary) that fires a MessageEvent {data, origin, source: sender window proxy, ports} at the target window (page or frame). MessageChannel = two MessagePort EventTargets; port.postMessage queues the same task to the other port; port.start()/close(); onmessage setter enables. ~150 lines in a new bindings/messaging.cpp. Also unblocks the WPT harness's cross-frame paths and 10+ files outside this slice (html/browsers, dom/events).

### lineno/colno are always 0 in error reports; a compile error's position is thrown away

- **6 subtests / 4 files**, size L, overlaps none, confidence high
- Root cause: dispatch_error_value (ctbrowser/lib/Shell/bindings/events/input.cpp:85-115) sets lineno=colno=0 with the comment 'No position yet - the VM keeps none for a throw'. `rg lineno include/ctbrowser/script/vm.hpp` is empty: the bytecode carries no line table. The compiler DOES know positions for syntax errors (abort.sub.window.js reports 'parse error: } - at 64:20') but compile_handler_attribute / run_due_callbacks / execute_script_element pass only the text. reporterror.any.js (3) additionally wants `filename` = the .any.js file, not the runner's .ctwpt.html wrapper.
- Spec: HTML 8.1.3.3 report an exception (line/column), ErrorEvent lineno/colno
- Engine files: ctbrowser/lib/Shell/bindings/events/input.cpp (dispatch_error_value); ctbrowser/lib/Script/compile/*.cpp (emit a pc->line table per function; parse-error position already in the message); ctbrowser/include/ctbrowser/script/vm.hpp (record throw position in throw_value/raise); ctbrowser/lib/Shell/bindings/document/entry.cpp (execute_script_element: pass the script element's source line offset [..]
- Evidence: html/webappapis/scripting/processing-model-2/window-onerror-runtime-error.html (1: 'expected 36 but got 0'); html/webappapis/scripting/processing-model-2/window-onerror-runtime-error-throw.html (1); html/webappapis/scripting/processing-model-2/window-onerror-parse-error.html (1: 'expected 34 but got 0'); html/webappapis/scripting/reporterror.any.js (3: filename is the .ctwpt.html wrapper)
- Fix: S half: parse 'at L:C' out of compiler::compile's error text (or add line/col fields to program) and pass line + the inline <script>'s start line (the tree builder has the token span) to dispatch_error; runner: the .any.js wrapper should report the .js URL as filename (tools/wpt/run-wpt.py wrapper or bindings script_src). L half: a per-function line table in lib/Script/compile (ctcompile pins the bytecode SHAPE - a side table keyed by pc is additive) and `context::last_throw_position()` consulted by dispatch_error_value.

### A document.write()n external classic script runs inside write() instead of after the writing script returns

- **5 subtests / 5 files**, size M, overlaps none, confidence high
- Root cause: prepare_parser_script (ctbrowser/lib/Shell/bindings/document/write.cpp:180-215) fetches an external non-defer script synchronously and runs it immediately (`run_script_(prepared)`) even when the parser's script nesting level is > 0 - i.e. from inside document.write() called by the outer script. HTML: an external classic script is the 'pending parsing-blocking script'; 13.2.6.4.8 sets the parser pause flag when nesting != 0 and the script runs only once the outer script's `</script>` handling unwinds. Observed order [1,3,2,4] vs [1,2,3,4] (script_005/006), and the written <meta> after the script is tokenised too early (script_010/011/012: 'expected 0 but got 1/2').
- Spec: HTML 4.12.1.1 prepare the script element step 33 ('pending parsing-blocking script'), 13.2.6.4.8 the 'text' insertion mode, </script> end tag steps 12-20, 13.2.5 parser pause flag
- Engine files: ctbrowser/lib/Shell/bindings/document/write.cpp (prepare_parser_script step 31/33); ctbrowser/lib/DOM/treebuilder.cpp (script nesting level, parser pause flag, 13.2.6.4.8 steps 13-20)
- Evidence: html/webappapis/dynamic-markup-insertion/document-write/script_005.html (1); html/webappapis/dynamic-markup-insertion/document-write/script_006.html (1); html/webappapis/dynamic-markup-insertion/document-write/script_010.html (1); html/webappapis/dynamic-markup-insertion/document-write/script_011.html (1); html/webappapis/dynamic-markup-insertion/document-write/script_012.html (1)
- Fix: When `parser_->script_nesting() > 0` and the prepared script is external+classic, store it as `pending_parsing_blocking_script_` and set the tree builder's pause flag (stop pumping); in the `</script>` end-tag step, after the outer script returns and nesting drops to 0, run the pending script (and any it writes, in a loop per 13.2.6.4.8 step 20), then resume the pump. Inline written scripts keep running immediately (that path is right today).

### Special error-event handling is keyed on (type=='error' && the page window) instead of (ErrorEvent instance && Window target)

- **4 subtests / 4 files**, size S, overlaps none, confidence high
- Root cause: fire_handler_property (ctbrowser/lib/Shell/bindings/events/dispatch.cpp, block starting 'window.onerror IS THE ONE HANDLER THAT IS NOT HANDED ITS EVENT') tests `type == "error" && target.bits() == page window`. HTML 8.1.8.1 step 4 says: five arguments and 'return true cancels' only when the handler is a Window/WorkerGlobalScope onerror AND the event is an ErrorEvent. So `window.dispatchEvent(new Event('error'))` hands the handler 5 args ('expected 1 but got 5', window-synthetic-event, body-element-synthetic-event, frameset-element-synthetic-event) and a frame window (a different object) never gets the special path (eventhandler-cancellation: 'Returning true from frames[0]'s onerror ... [..]
- Spec: HTML 8.1.8.1 the event handler processing algorithm steps 4-5 (special error event handling)
- Engine files: ctbrowser/lib/Shell/bindings/events/dispatch.cpp (fire_handler_property; forwards_to_window/body_or_frameset_of); ctbrowser/lib/Shell/bindings/events/interfaces.cpp (error_event_prototype_ for the instanceof check)
- Evidence: html/webappapis/scripting/events/event-handler-processing-algorithm-error/window-synthetic-event.html (1); html/webappapis/scripting/events/event-handler-processing-algorithm-error/body-element-synthetic-event.html (1); html/webappapis/scripting/events/event-handler-processing-algorithm-error/frameset-element-synthetic-event.html (1 TIMEOUT); [..]
- Fix: Replace the guard with: `special = type == "error" && is_window_like(target) /* page window proxy OR any frame_window built by load_frame (tag them with a hidden slot) */ && event.prototype chain contains error_event_prototype_`. When special is false the handler gets the event and `return false` cancels; when true the five args and `return true` cancels. Keep the body/frameset forwarding as is.

### window.clientInformation is missing (an alias of navigator)

- **4 subtests / 2 files**, size S, overlaps none, confidence high
- Root cause: window.cpp:671 defines only `navigator`; `clientInformation` (HTML: `[Replaceable] readonly attribute Navigator clientInformation`, same object) is undefined.
- Spec: HTML 7.2.2.1 Window interface: clientInformation; 8.9.1 Navigator
- Engine files: ctbrowser/lib/Shell/bindings/window/window.cpp (navigator block, :626-672)
- Evidence: html/webappapis/system-state-and-capabilities/the-navigator-object/clientinformation.window.js (1); html/webappapis/system-state-and-capabilities/the-navigator-object/per-global.window.js (3: 'window.clientInformation must be implemented')
- Fix: `window->set("clientInformation", navigator)` + define_global, and the same on a frame window in load_frame (per-global expects it to survive document.open and a frame discard - identity with that frame's navigator). One line each.

### The event loop runs all due timers / all rAF callbacks then ONE microtask checkpoint, and fires DOMContentLoaded/load ahead of tasks queued earlier in the tick

- **3 subtests / 3 files**, size M, overlaps none, confidence high
- Root cause: run_due_callbacks (ctbrowser/lib/Shell/bindings/window/callbacks.cpp:115-215) loops every due timer calling call_fenced, then drains microtasks once; same for animation frames. HTML's 'clean up after running a callback' performs a checkpoint after EACH callback: promise-resolution-order expects raf1,resolve1,raf2,resolve2 and gets raf1,raf2,resolve1,resolve2. browser::tick (browser/frame.cpp:107-119) fires DOMContentLoaded/complete/load BEFORE advance_clock/run_due_callbacks, so the 'notify about rejected promises' task queued during parsing (promise_rejections.cpp uses add_timer 0 as its task queue) runs after readystatechange:complete instead of between interactive and complete. There is [..]
- Spec: HTML 8.1.7.1 task queues / 8.1.7.3 event loop processing model step 3-7 (one task, then a microtask checkpoint), 8.1.3.4 clean up after running a callback, 8.6 timer initialisation steps (nesting [..]
- Engine files: ctbrowser/lib/Shell/bindings/window/callbacks.cpp (run_due_callbacks); ctbrowser/lib/Shell/browser/frame.cpp (tick: load-event ordering vs queued tasks); ctbrowser/lib/Shell/bindings/promise_rejections.cpp (its task lives on the timer list); ctbrowser/lib/Shell/bindings/document/write.cpp (parser_finished's 'the end' task); ctbrowser/lib/App/app/run.cpp (fixed_dt / next_wakeup granularity for the [..]
- Evidence: html/webappapis/scripting/processing-model-2/unhandled-promise-rejections/promise-resolution-order.html (1: got raf1,raf2,resolve1,resolve2,...); html/webappapis/scripting/processing-model-2/unhandled-promise-rejections/promise-rejection-event-during-parse.html (1: unhandledrejection after readystatechange:complete); html/webappapis/timers/timer-nesting-not-inherited-in-microtask.html (1: [..]
- Fix: Introduce a real task queue in dom_bindings: `std::deque<task>` with a source tag; setTimeout/rAF/frame-load/fetch/'the end'/rejection notification all become tasks (timers stay a due-list that ENQUEUES a task when due). run_due_callbacks pops one task, runs it, drains microtasks, repeat until the queue is empty for this tick; rAF callbacks each get a checkpoint. browser::tick: the DOMContentLoaded/load task must be queued through the same queue behind whatever the parser's scripts queued (parser_finished already does this for a script-created parser - make `initial` do the same instead of load_event_pending_). For the headless driver, let next_wakeup_ms honour sub-frame timers (a 1ms timer wakes in 1ms; run.cpp already computes `due`). Ordering-sensitive: run dom/events + html/webappapis + the unit suite after each step.

### Timer arguments are not WebIDL-converted: long overflow and non-callable handlers

- **3 subtests / 3 files**, size S, overlaps none, confidence high
- Root cause: install_timers (ctbrowser/lib/Shell/bindings/window/callbacks.cpp:301-306) passes `arg_number(args,1)` straight into add_timer: a delay of 2**32 stays 4294967296 ms instead of wrapping to 0 through `long` (ToInt32) as WebIDL requires, so `setTimeout(done, 2**32)` never fires and `assert_unreached` at 100ms does. A handler that is an object (evil-spec-example) is neither a string nor callable, so run_due_callbacks calls call_fenced on a non-callable: HTML says a non-callable handler is ToString'd at INVOCATION time and run as a script (its toString may itself call setTimeout).
- Spec: HTML 8.6 timer initialisation steps (step 2 'if timeout < 0 set 0', handler is a string -> classic script at run time), WebIDL 3.2.7 long conversion, IDL `setTimeout(TimerHandler handler, optional [..]
- Engine files: ctbrowser/lib/Shell/bindings/window/callbacks.cpp (install_timers, run_due_callbacks, add_timer)
- Evidence: html/webappapis/timers/type-long-settimeout.any.js (1); html/webappapis/timers/type-long-setinterval.any.js (1); html/webappapis/timers/evil-spec-example.any.js (1 TIMEOUT)
- Fix: delay = ToInt32(to_number(arg1)) (modulo 2^32 into signed), then max(0,·); keep the callback VALUE (any) and at fire time: callable -> call; otherwise ToString then compile+run as a classic script (the string path already exists - move the coercion to the run site). Also pass extra args (setTimeout(f, 0, a, b)) which run_due_callbacks drops today.

### No History interface on window (history is not defined)

- **2 subtests / 4 files**, size S, overlaps none, confidence high
- Root cause: `rg pushState|"history"|history_ lib include` finds nothing except frames.cpp's exclusion list: window.history does not exist on the page or on a frame window. Files that touch history.state/pushState/replaceState/back throw at the top level (3 HARNESS_ERROR files) or fail on `.state` (history-state.window.js, 2).
- Spec: HTML 7.2 History interface (7.2.1-7.2.4: pushState/replaceState 'shared history push/replace state steps'), 8.4.2 document open steps (history entry untouched)
- Engine files: ctbrowser/lib/Shell/bindings/window/window.cpp (History interface object + window.history); ctbrowser/lib/Shell/bindings/frames.cpp (a frame's own history); ctbrowser/lib/Shell/browser/lifecycle.cpp (a session-history list per browsing context: url + state)
- Evidence: html/webappapis/dynamic-markup-insertion/html-unsafe-methods/Document-parseHTMLUnsafe-url-pushstate.html (HARNESS_ERROR: 'history is not defined'); html/webappapis/dynamic-markup-insertion/html-unsafe-methods/Document-parseHTMLUnsafe-url-base-pushstate.html (HARNESS_ERROR); html/webappapis/dynamic-markup-insertion/opening-the-input-stream/history.window.js (HARNESS_ERROR, also window.open); [..]
- Fix: A minimal History per bindings: {length, state, scrollRestoration, pushState(data,unused,url), replaceState, back/forward/go -> no-op or fragment-only}, pushState/replaceState structuredClone the state and update location_href_ (same-origin, resolve against the document URL) and `document.URL`; document.open keeps `history.state` (8.4.2 step 15 does not touch it). ~120 lines in a new bindings/window/history.cpp; the interface object via the existing interface_object helper.

### Inline event handler scope omits the form owner for <img> and form-associated custom elements

- **2 subtests / 1 files**, size S, overlaps CE2, confidence high
- Root cause: compile_handler_attribute (ctbrowser/lib/Shell/bindings/events/dispatch.cpp:1418-1430) computes `with_form` only for button/fieldset/input/object/output/select/textarea; HTML's 'form-associated elements' list also has img and form-associated custom elements, so `<img onclick="elements">` resolves to the global (got "global_elements").
- Spec: HTML 8.1.8.1 event handler content attributes step 10.3 (form owner scope), 4.10.2 form-associated elements (includes img and form-associated custom elements)
- Engine files: ctbrowser/lib/Shell/bindings/events/dispatch.cpp (compile_handler_attribute with_form list); ctbrowser/lib/Shell/bindings/custom_elements.cpp (formAssociated flag lookup)
- Evidence: html/webappapis/scripting/events/compile-event-handler-lexical-scopes-form-owner.html (2: '<img> has a form owner', 'form-associated <x-foo> has a form owner')
- Fix: Add `img` to the list and, for a custom element, consult the registry's definition `formAssociated`; both then read `form` (the img needs a `form` owner accessor - HTML 4.10.17 form owner for img is the nearest ancestor form).

### A throw inside a microtask is swallowed as a VM failure: the value is lost and the rest of the queue is dropped

- **1 subtests / 1 files**, size S, overlaps none, confidence high
- Root cause: context::drain_microtasks (ctbrowser/include/ctbrowser/script/vm.hpp:855-868) calls `call(job.fn, job.args)`, not call_fenced: an uncaught throw sets `failed_`, the loop stops ('!failed_'), the remaining jobs stay queued/dropped (run.cpp:86 'A FAILED SCRIPT'S QUEUE DIES WITH IT'), and the host only ever sees the text via note_callback_fault('microtask') -> dispatch_error(message) with error=undefined, so `ev.error === error` fails. HTML says each microtask's exception is reported (to the callback's global) and the checkpoint continues.
- Spec: HTML 8.1.7.3 perform a microtask checkpoint step 2.6 (report the exception), 8.1.3.3 report an exception
- Engine files: ctbrowser/include/ctbrowser/script/vm.hpp (drain_microtasks); ctbrowser/lib/Shell/bindings/window/callbacks.cpp (report through dispatch_error_value with the thrown value); ctbrowser/lib/Script/vm/call/run.cpp
- Evidence: html/webappapis/microtask-queuing/queue-microtask-exceptions.any.js (1: 'expected Error: boo but got undefined'); html/webappapis/microtask-queuing/queue-microtask-cross-realm-callback-report-exception.html (1, needs REALM too)
- Fix: drain_microtasks: run each job through call_fenced(fn, args, undefined, threw, thrown); on threw (not on a VM fault) invoke a host hook `report_exception_(thrown)` (a std::function beside rejection_tracker_, set by dom_bindings to dispatch_error_value(describe_thrown, thrown)) and CONTINUE the drain; only a genuine `failed_` (resource ceiling) stops it. Promise reaction jobs already catch their own throws, so this only changes queueMicrotask/MutationObserver jobs.

**Long tail:** Cross-origin www1 host tests (8 files / 12 subtests: runtime-error-cross-origin*, compile-error-cross-origin*, allow/disallow-crossorigin - need a second origin and CORS the file: runner cannot serve; muted 'Script error.' reporting would follow once it can). Worker/SharedWorker absent (5 files / 10 subtests + 2 HARNESS_ERROR .dedicatedworker files). window.open (5 files: document-write/047, document.open-02, reload, history.window.js, promise-job-entry sanity). window.print/stop/focus (user-prompts 3, update-the-rendering-resize-autofocus 1). navigator.oscpu/userAgent 'value' TIMEOUT (2) and [..]

**Skipped (already fixed at HEAD):** Already addressed at HEAD (b722aa41), do not re-count: (1) unhandledrejection/rejectionhandled - cced0f19 added lib/Shell/bindings/promise_rejections.cpp + PromiseRejectionEvent (interfaces.cpp:724) after the baseline: promise-rejection-events.html (22), -many-promises (6), -onerror (1), -attached-in-event (1), tasks.window.js 'Promise rejection' (2) - BUT the subtests that use the file's queueTask() helper (a disconnected <details> with `open` set) stay TIMEOUT until the toggle-task cluster above lands. (2) requestAnimationFrame: cancelAnimationFrame, cancel-during-run and exception [..]

## html-dom — html/dom + html/semantics/forms (the WPT checkout only carries html/semantics/forms; interactive/embedded/tabular/text-level are not in the corpus)

Baseline: /tmp/m-9f8da347/html-dom.json: 376 files = 153 PASS / 71 FAIL / 13 TIMEOUT / 2 HARNESS_ERROR / 137 SKIP; subtests 60,524 PASS / 191 non-PASS. /tmp/w-9f8da347/html.json filtered to html/dom/** + html/semantics/**: 1,024 files = 316 PASS / 212 FAIL / 34 TIMEOUT / 10 HARNESS_ERROR / 452 SKIP; subtests 63,976 PASS / 1,409 non-PASS, of which forms 1,218, render-blocking 59, global-attributes 54, [..]

### Echo-server (.py handler) dependent submissions: no wptserve, POST never navigates, no multipart/text-plain body serialisers

- **202 subtests / 5 files**, size L, overlaps none, confidence medium
- Root cause: tools/wpt/run-wpt.py has no server (its own header: 'there is no server here'); `/` paths are read off CTBROWSER_DOC_ROOT (assets.hpp set_document_root), so a POST to /FileAPI/file/resources/echo-content-escaped.py or /fetch/api/resources/echo-content.py can never echo the entity body - the frame document is empty and every assert reads ''. navigate_form_target additionally refuses method=post (frames.cpp:455 `if (method == "post" || method == "dialog") return false;`) and only serialize_form_urlencoded exists - no multipart/form-data or text/plain encoder (4.10.21.7/8).
- Spec: HTML 4.10.21.3 steps 21-22, 4.10.21.7 multipart/form-data encoding, 4.10.21.8 text/plain encoding
- Engine files: tools/wpt/run-wpt.py; ctbrowser/include/ctbrowser/shell/page/assets.hpp; ctbrowser/lib/Shell/bindings/frames.cpp; ctbrowser/lib/Shell/bindings/element/control_methods.cpp
- Evidence: html/semantics/forms/form-submission-0/multipart-formdata.window.js (62/62); html/semantics/forms/form-submission-0/text-plain.window.js (62/62); html/semantics/forms/form-submission-0/urlencoded2.window.js (62/62); html/semantics/forms/form-submission-0/submit-entity-body.html (13, TIMEOUT); html/semantics/forms/form-submission-0/form-data-set-empty-file.window.js (3 of 4)
- Fix: Two halves. Engine: in navigate_form_target accept POST and build the request body with the enctype's serialiser (multipart/form-data per 4.10.21.7 with a random boundary, text/plain per 4.10.21.8, urlencoded via URLSearchParams' rules) and hand {url, method, body, content-type} to load_frame. Infrastructure (pick one): (a) run-wpt.py launches `wpt serve` and the registry fetches http://web-platform.test:8000 for anything under the doc root that is a .py handler; or (b) the cheap in-engine emulation - when the planned navigation's path is one of the three echo handlers (echo-content.py, echo-content-escaped.py with its \xNN escaping, form-submission.py), the frame's document is the request body as text/plain. 33 of these subtests ALSO need the DataTransfer cluster.

### <input type=color>: colorspace/alpha attributes not threaded through sanitisation nor reflected

- **182 subtests / 1 files**, size S, overlaps none, confidence high
- Root cause: d69dee50 (round seven) added sanitize_color(value, display_p3, alpha) but input_types.cpp's colour branch calls it for the limited-sRGB opaque form only, and reflection.cpp has no `colorSpace`/`alpha` on HTMLInputElement - the commit body says so explicitly. Every color.window.js case with color space 'display-p3' or 'with alpha' therefore serialises `#rrggbb` instead of `color(srgb r g b / a)` / `color(display-p3 ...)`, and re-sanitisation on attribute change does not happen.
- Spec: HTML 4.10.5.1.15 Color state (colorspace, alpha, 'update a color well control color'), CSS Color 4 serialisation
- Engine files: ctbrowser/lib/Shell/page/input_types.cpp; ctbrowser/lib/Shell/bindings/element/reflection.cpp; ctbrowser/lib/Style/css/properties/color.cpp; ctbrowser/lib/Shell/page/forms.cpp
- Evidence: html/semantics/forms/the-input-element/color.window.js (182/196: 'Actual: #000000 Expected: color(srgb 0 0 0)', 'Expected: color(display-p3 0 0 0)', 'value is hex expected #ffffff but got #000000')
- Fix: input_types sanitized(): read the element's `colorspace` (enumerated limited-srgb|display-p3, default limited-srgb) and `alpha` (boolean) and pass them to sanitize_color; add enum_attr("HTMLInputElement","colorSpace","colorspace","limited-srgb display-p3","limited-srgb","limited-srgb") and bool_attr("HTMLInputElement","alpha") in the reflection table; re-run sanitisation when either attribute changes (the same hook settle_types uses for `type`). The unit test in css_values_color.cpp already covers the serialiser.

### Form submission algorithm: no planned navigation, submitter dropped, SubmitEvent/FormDataEvent absent, form* attributes ignored

- **127 subtests / 14 files**, size L, overlaps none, confidence high
- Root cause: HEAD (456b2a5b) navigates a GET-to-named-frame SYNCHRONOUSLY inside build_submission (control_methods.cpp:2733-2755 -> frames.cpp:443-480 load_frame), so there is no 'planned navigation' that a later submit()/click supersedes (HTML step 24: 'v4' wins; the engine loads the frame three times and the first load resolves loadPromise). browser::submit (actions.cpp:214-230) fires `submit` as a plain Event via dispatch("submit") with no `submitter` and calls submit_form_(form, node_id{}) - the clicked button's formaction/formtarget/formmethod/formenctype and its name=value entry are lost (navigate_form_target reads only the form's attributes, its own comment says so). No SubmitEvent or [..]
- Spec: HTML 4.10.21.3 (steps 5-7, 17-20, 24 'plan to navigate'), 4.10.21.4, 4.10.22 SubmitEvent, 4.10.21.8 FormDataEvent, 4.10.6 button command
- Engine files: ctbrowser/lib/Shell/bindings/element/control_methods.cpp; ctbrowser/lib/Shell/bindings/frames.cpp; ctbrowser/lib/Shell/browser/actions.cpp; ctbrowser/lib/Shell/bindings/events/dispatch.cpp; ctbrowser/lib/Shell/bindings/element/interfaces.cpp; ctbrowser/include/ctbrowser/shell/bindings.hpp
- Evidence: html/semantics/forms/form-submission-0/form-double-submit-requestsubmit.html (36, HARNESS_ERROR; 'expected v2 but got v1', 'Invalid URL: undefined'); html/semantics/forms/form-submission-0/form-double-submit-input-type-change.html (20); html/semantics/forms/the-input-element/input-type-change-submit.html (20); html/semantics/forms/form-submission-0/form-submit-action.html (10: formaction/form= [..]
- Fix: (1) Carry the submitter: on_activate_ -> browser::activate -> browser::submit(form, submitter); submit_form_(form, submitter). (2) Define SubmitEvent (submitter) and FormDataEvent (formData) constructors in interfaces.cpp/events and fire `submit` as a SubmitEvent from both requestSubmit and the activation path. (3) In navigate_form_target take action/method/enctype/target from the submitter's form* attributes first (4.10.21.3 steps 17-20), choose the navigable per 'rules for choosing a navigable' incl. empty/_self = the form's own navigable (a frame's dom_bindings navigates its own element via the parent's load_frame). (4) Replace the immediate load_frame with a `planned_navigation{form, navigable, url}` member; a new plan for the same form overwrites it; drain it from the task loop (where timers run) - that alone fixes every double-submit file. (5) Keep the form's [..]

### Constraint-validation flags are zeroed when the control is barred (disabled/readonly)

- **106 subtests / 10 files**, size S, overlaps none, confidence high
- Root cause: validity_flags (control_methods.cpp:2107-2111) returns early: `if (!will_validate(txn, b, id)) { return flags; }` with a comment claiming the browsers agree. They do not: validator.js's iterate_over clones each control with disabled=true and expects the same flags ('expectedImmutable' defaults to expected); only validationMessage/checkValidity/willValidate depend on being barred.
- Spec: HTML 4.10.20.1 definitions ('suffering from ...' are defined on the value; 'barred from constraint validation' only excludes the element from checkValidity/reportValidate/willValidate)
- Engine files: ctbrowser/lib/Shell/bindings/element/control_methods.cpp
- Evidence: html/semantics/forms/constraints/form-validation-validity-valid.html (24: 'validity.valid should be false, when control is disabled'); html/semantics/forms/constraints/form-validation-validity-patternMismatch.html (22 of 43); html/semantics/forms/constraints/form-validation-validity-rangeOverflow.html (16 of 19), -rangeUnderflow.html (16 of 18); [..]
- Fix: Delete the early return in validity_flags; keep `will_validate` for willValidate, checkValidity/reportValidity (skip barred controls) and validationMessage (empty when barred, which validator.js also asserts). One-line root-cause fix; the per-type message counts follow.

### Render-blocking: the parser is not incremental, so rendering opportunities never interleave with parsing

- **59 subtests / 38 files**, size L, overlaps none, confidence medium
- Root cause: Every render-blocking test parses a document whose inline scripts spin (generateParserDelay) and asserts from a requestAnimationFrame that later markup (#last) has NOT been parsed yet, or that rendering is blocked until a `blocking=render` script/link or `<link rel=expect>` settles. The engine parses the whole document before the first frame, so rAF callbacks see the complete DOM ('assert_false: expected false got true' in 35 files) and nothing implements the blocking attribute / rel=expect at all (rg 'blocking' hits only views.cpp/performance.cpp).
- Spec: HTML 8.1.7.3 event loop processing model (rendering opportunities), 4.2.7 blocking attribute + render-blocking mechanism, 4.6.7 rel=expect
- Engine files: ctbrowser/lib/Shell/browser/frame.cpp; ctbrowser/lib/Shell/bindings/document/entry.cpp; ctbrowser/lib/DOM/treebuilder.cpp; ctbrowser/lib/Shell/bindings/window/callbacks.cpp
- Evidence: html/dom/render-blocking/element-render-blocking-002..036.html (35 files x 1: 'blocking defers until needed element is parsed', 'removing blocking makes it no longer blocking'); html/dom/render-blocking/remove-attr-unblocks-rendering.optional.html (9, TIMEOUT); html/dom/render-blocking/non-render-blocking-scripts.optional.html (7, TIMEOUT), remove-element-unblocks-rendering.optional.html (7); [..]
- Fix: Make the parser resumable: after each parser-inserted script executes, return to the event loop (run microtasks, due timers, and - if the document's render-blocking element set is empty - a rendering opportunity with rAF callbacks) before continuing tokenisation. Track the render-blocking set: `<script blocking=render>`/`<link blocking=render>` in <head>, `<link rel=expect href=#id>` until #id is parsed; removal of the element/attribute or a non-matching media unblocks. This is the html/syntax agent's parser-driven-document work more than forms; listed because the files sit in html/dom.

### DataTransfer missing and input.files is always null (no FileList)

- **46 subtests / 8 files**, size M, overlaps none, confidence high
- Root cause: `accessor("HTMLInputElement","files", ...)` at control_methods.cpp:1964 returns value::null() unconditionally and has no setter; `DataTransfer` is not defined anywhere in lib/Shell (rg), so every file-submission test that builds its File via `new DataTransfer(); dt.items.add(file); input.files = dt.files` rejects with ReferenceError. The entry list also omits the empty File an empty <input type=file> must contribute.
- Spec: HTML 4.10.5.1.18 File Upload state (files getter/setter), 4.10.21.4 constructing the entry list step 5.7, HTML 6.11.2 DataTransfer / 6.11.3 DataTransferItemList
- Engine files: ctbrowser/lib/Shell/bindings/element/control_methods.cpp; ctbrowser/lib/Shell/bindings/element/interfaces.cpp; ctbrowser/lib/Shell/bindings/xhr.cpp
- Evidence: html/semantics/forms/form-submission-0/{multipart-formdata,text-plain,urlencoded2}.window.js (11 each = 33: 'ReferenceError: DataTransfer is not defined'; these also need the echo-server cluster); html/semantics/forms/form-submission-0/newline-normalization.html (4), constructing-form-data-set.html (3), submit-file.sub.html (1); html/semantics/forms/the-input-element/files.html (3: 'got [..]
- Fix: A FileList wrapper (array-like, item(), length) held in a per-input slot; `files` getter returns it (empty, not null) for type=file and null otherwise; setter accepts only a FileList (TypeError otherwise, ignored when not type=file). DataTransfer constructor with `items` (DataTransferItemList.add(File)/add(string,type), remove, clear) and `files` (a FileList of the File items), dropEffect/effectAllowed/types. Entry list: a type=file with no files appends File("", type application/octet-stream). Reuse the File/Blob objects xhr.cpp already builds.

### Text control selection is seeded at the end of the value, and moved by things that must not move it

- **38 subtests / 4 files**, size S, overlaps none, confidence high
- Root cause: form_store::state_of seeds `seeded.caret = seeded.value.size()` (forms.cpp:123) and, when the value attribute is re-read for a non-dirty control, sets the caret to the end again (forms.cpp:92); a type change clamps instead of resetting (forms.cpp:55); textarea child mutations re-seed the raw value the same way; set_value (forms.cpp:159) compares the un-normalised text so setting the same CRLF value counts as a change and moves the cursor. The spec's selection is 0/0 until the user or the `value` IDL setter moves it.
- Spec: HTML 4.10.19.5 text control selection (selectionStart/End defaults), 4.10.5.1 'value' mode setter (move the text entry cursor to the end), 4.10.11 textarea 'relevant value' / child text content [..]
- Engine files: ctbrowser/lib/Shell/page/forms.cpp; ctbrowser/include/ctbrowser/shell/page/forms.hpp; ctbrowser/lib/Shell/bindings/element/control_methods.cpp
- Evidence: html/semantics/forms/the-input-element/type-change-state.html (25 of 380: 'selectionStart should be 0 expected 0 but got 2/6/7'); html/semantics/forms/textfieldselection/selection-start-end-extra.html (6: 'Setting textContent in a textarea should NOT move selection', 'expected 0 but got 6'); html/semantics/forms/textfieldselection/defaultSelection.html (4: 'expected 0 but got 3'); [..]
- Fix: Seed caret = selection = 0; on attribute re-seed and textarea child changes clamp the existing selection to the new length instead of moving it (the spec only says the value changed, not the cursor); on a type change set the selection to 0/0 (what every browser and type-change-state expect); in set_value compare against the normalised API value (LF-only for textarea) and only then move the cursor to the end. control_methods' selection_of already reads through code units, so nothing else moves.

### Scripts do not run in frames (REALM)

- **28 subtests / 7 files**, size L, overlaps REALM, confidence high
- Root cause: Tests whose assertion is made by a document loaded into an <iframe> (form-with-action.sub.html calling parent.success, the currentScript frame, the self-origin sub-frames, show-picker cross-origin frames) never get there because a frame shares the page realm and its scripts never execute.
- Spec: HTML 7.1 browsing contexts / realms, 7.7 postMessage
- Engine files: ctbrowser/lib/Shell/bindings/frames.cpp
- Evidence: html/semantics/forms/the-form-element/form-action-submission.html (3, TIMEOUT), form-action-submission-with-base-url.html (3, TIMEOUT); html/dom/self-origin.sub.html (10, HARNESS_ERROR: postMessage undefined), self-origin.any.js (1); html/dom/documents/dom-tree-accessors/Document.currentScript.html (3, TIMEOUT); html/semantics/forms/the-input-element/show-picker-cross-origin-iframe.html (4), [..]
- Fix: Covered by round8-brief-REALM.md (one realm per browsing context, frame scripts run, postMessage).

### dir=auto ignores form control values, non-HTML script/style, CDATA, and text mutations do not restyle

- **27 subtests / 9 files**, size M, overlaps none, confidence high
- Root cause: engine::direction_is_rtl/first_strong (lib/Style/engine.cpp:2297-2340) read only child text nodes: an <input dir=auto> / <textarea dir=auto> must use its value (the store's, or the value attribute) and does not; the skip of script/style/textarea does not check element_ns (non-HTML ones must count); is_text_kind may exclude CDATA (cdata-dir_auto TIMEOUT); and a text insertion into a dir=auto subtree does not invalidate the ancestor's :dir()/direction. control_methods.cpp:2523-2524 hard-codes the dirname entry to "ltr" instead of the element's directionality.
- Spec: HTML 3.2.6.4 auto-directionality (text-form-control directionality, contained-text auto directionality), 4.10.18.2 dirname
- Engine files: ctbrowser/lib/Style/engine.cpp; ctbrowser/lib/Shell/bindings/element/control_methods.cpp; ctbrowser/lib/Shell/bindings/mutation.cpp
- Evidence: html/dom/elements/global-attributes/dir-auto-form-associated.window.js (11 of 22: '<input dir=auto type=text> directionality'); html/dom/elements/global-attributes/cdata-dir_auto.html (3, TIMEOUT), dir-assorted.window.js (3: 'non-html style element text contents influence dir=auto'); html/dom/elements/global-attributes/dir-auto-dynamic-changes.window.js (3 non-slot: 'CSSdirection after insertion [..]
- Fix: In first_strong: for an HTML input in a text-ish type or a textarea with dir=auto, run first_strong_in over the control's value (a Style->Shell hook, or the value attribute + a store callback); skip bdi/script/style/textarea only when element_ns==html; include CDATA via is_text_kind. Mark the nearest dir=auto ancestor's style dirty on any text/child mutation and on control value writes (the wrote_to_control_ path). dirname: replace the literal "ltr" with direction_is_rtl(element) ? "rtl" : "ltr" and apply dirname only where it applies (text/search/tel/url/email/password/hidden/submit/reset/button + textarea).

### pattern attribute falls back to the `u` flag when `v` fails, so invalid-under-v patterns still constrain

- **23 subtests / 2 files**, size S, overlaps none, confidence high
- Root cause: compile_pattern (control_methods.cpp:2074-2095) tries `new RegExp('^(?:'+p+')$','v')` and then FALLS BACK to the 'u' flag; the spec compiles with v only and an error means 'no constraint'. `[(]` and `a)(b` are errors under v (ClassSetSyntaxCharacter / stray paren) but compile under u, so the value mismatches; the JavaScript-specific cases (`\cx[5-\[]{2}`) suggest ctjs's v-mode class parsing (round seven's J2) also diverges.
- Spec: HTML 4.10.5.3.6 pattern (compiled pattern: 'v' flag, ^(?: ... )$), ECMA-262 22.2.1 ClassSetCharacter
- Engine files: ctbrowser/lib/Shell/bindings/element/control_methods.cpp
- Evidence: html/semantics/forms/constraints/form-validation-validity-patternMismatch.html (21 of 43: 'Invalid `v` regular expression gets ignored', 'The pattern attribute tries to escape a group', 'matches JavaScript-specific regular expression' -> 'patternMismatch should be false'); html/semantics/forms/the-input-element/pattern_attribute_v_flag.html (2: '<input pattern="[(]" value="foo"> should be valid')
- Fix: Remove the 'u' fallback in the compiled helper (v or nothing). Then check ctjs's v-mode class parser rejects unescaped ClassSetSyntaxCharacters `( ) [ ] { } / - \ |` inside [...] and accepts `\cx`, `[5-\[]` and `\q{...}` string literals; whatever still fails is a ctjs regexp bug for the JS agent.

### cloneNode drops `indeterminate` (a wrapper slot, not store state)

- **22 subtests / 1 files**, size S, overlaps none, confidence high
- Root cause: tree_ops.cpp:100-113 clones the form store's control_state (value, checkedness, dirty flags - b8ec8864/d813ab52) but `indeterminate` lives in the wrapper slot `__indeterminate` (control_methods.cpp:1966-1980), which no clone step copies.
- Spec: HTML 4.10.5 input element cloning steps (browsers and the test include indeterminate; whatwg/html#3567)
- Engine files: ctbrowser/lib/Shell/bindings/document/tree_ops.cpp; ctbrowser/lib/Shell/bindings/element/control_methods.cpp; ctbrowser/include/ctbrowser/shell/page/forms.hpp
- Evidence: html/semantics/forms/the-input-element/cloning-steps.html (22 of 68: 'input[type=X] element's indeterminateness should be cloned' for every type)
- Fix: Move `indeterminate` into control_state (bool) so clone_state carries it for free, or copy the `__indeterminate` slot in the clone path next to the store copy.

### dir/lang through slots and shadow hosts

- **21 subtests / 3 files**, size S, overlaps FLAT, confidence high
- Root cause: direction_is_rtl walks txn.parent() and first_strong walks txn.children() - the light tree. A slot's directionality comes from its assigned nodes and a slotted element's from the host/slot chain, i.e. the flat tree; language_of has the same gap (lang on host).
- Spec: HTML 3.2.6.4 (uses the flat tree: 'assigned nodes', 'host'), 3.2.6.2 lang
- Engine files: ctbrowser/lib/Style/engine.cpp
- Evidence: html/dom/elements/global-attributes/dir-slots-directionality.html (9 of 17: 'Expect rtl for slot with assigned rtl content'); html/dom/elements/global-attributes/dir-auto-dynamic-changes.window.js (10 slot cases: 'dir=auto changes for slot reassignment', 'Slotted content affects multiple dir=auto slots'); html/dom/elements/global-attributes/lang-attribute-shadow.window.js (2: 'lang only on host')
- Fix: Once the FLAT brief's flat-tree parent/children accessors exist, point direction_is_rtl/first_strong/language_of at them (assigned nodes for a slot, the host for a shadow root).

### Radio button group: the checked setter never unchecks the group, and toggle walks the whole document by name only

- **12 subtests / 2 files**, size S, overlaps none, confidence high
- Root cause: The HTMLInputElement `checked` setter (control_methods.cpp:1950-1961) writes state.checked and nothing else; form_store::toggle (forms.cpp:248-266) walks from txn.root() (so a disconnected tree or a different form owner is either missed or wrongly swept) and matches only by name attribute. The group is: same tree root (incl. disconnected subtrees and fragments), same form owner, same non-empty name.
- Spec: HTML 4.10.5.1.16 Radio Button state (radio button group definition, checkedness rules)
- Engine files: ctbrowser/lib/Shell/page/forms.cpp; ctbrowser/lib/Shell/bindings/element/control_methods.cpp
- Evidence: html/semantics/forms/the-input-element/radio.html (7 of 12: 'only one control of a radio button group can have its checkedness set to true', 'Radio buttons in an orphan tree should make a group'); html/semantics/forms/the-input-element/radio-disconnected-group-owner.html (5 of 8: 'Removed elements are moved into separate radio groups', 'Disconnected document fragments can serve as radio group [..]
- Fix: One form_store::uncheck_group(txn, atoms, id, owner) walking from the element's own root (root_of_tree) and comparing name + form owner; call it from toggle, from the `checked` setter when the new value is true, and from the name/form-attribute change and insertion steps (4.10.5.1.16's 'when the element's checkedness becomes true'). validity_flags' radio walk (control_methods.cpp:2169-2190) already has the right group test - lift it into the store.

### HTMLFormElement has no named getter

- **11 subtests / 1 files**, size M, overlaps none, confidence high
- Root cause: form.button / form.l1 / form['addEventListener'] are undefined: rg finds no named-property hook on the form wrapper (wrapper.cpp / control_methods.cpp only define `elements`, `length`, submit/reset); document/named_access.cpp has the document-level pattern but nothing for forms, and there is no RadioNodeList for the multi-match case.
- Spec: HTML 4.10.3 The form element (named getter, past names map), 4.10.3.1 RadioNodeList
- Engine files: ctbrowser/lib/Shell/bindings/element/wrapper.cpp; ctbrowser/lib/Shell/bindings/element/control_methods.cpp; ctbrowser/lib/Shell/bindings/document/named_access.cpp
- Evidence: html/semantics/forms/the-form-element/form-nameditem.html (11 of 17: 'Name for a single element should work', 'Named elements should override builtins', 'Calling item() on the NodeList returned from the named getter')
- Fix: On the form wrapper's property-miss path (the same hook named_access.cpp uses for the document): collect elements_of_form(form) plus <img> descendants whose id or name matches (past-names map for a removed element); one match -> the element, several -> a RadioNodeList (a live NodeList with `value` = the checked radio's value). Named properties shadow the prototype ('override builtins').

### Form pseudo-classes parsed but never matched; :disabled ignores optgroup/fieldset ancestry

- **11 subtests / 3 files**, size M, overlaps none, confidence high
- Root cause: selector.cpp:40-58 structural_bit_of maps only root/empty/*-child/*-of-type/disabled/enabled/checked/link/visited/scope/defined; :valid, :invalid, :required, :optional, :in-range, :out-of-range, :indeterminate, :placeholder-shown are in known_pseudo_class (parse OK) but have no bit, so they never match. engine.cpp:647 `is_disabled = own disabled attribute` - an <option> inside <optgroup disabled> (even through a wrapper <div>) and a control inside a disabled <fieldset> are not :disabled.
- Spec: Selectors 4 §13 input pseudo-classes; HTML 4.10.5.1.16 / 4.10.9 / 4.10.15 'actually disabled' (optgroup, fieldset)
- Engine files: ctbrowser/lib/Style/css/selector.cpp; ctbrowser/lib/Style/engine.cpp; ctbrowser/lib/Shell/bindings/element/control_methods.cpp
- Evidence: html/semantics/forms/the-input-element/pattern_attribute.html (4: 'basic <input pattern> support' asserts input.matches(':invalid')); html/semantics/forms/the-select-element/customizable-select/option-disabled-optgroup.html (4: 'options should check ancestor optgroup for disabled state'); html/semantics/forms/the-select-element/customizable-select/option-disabled-optgroup-wrapper.html (3: 'Option [..]
- Fix: Add state bits for the eight pseudo-classes and fill them from a Shell hook that calls validity_flags/will_validate (control_methods.cpp:2093) - the same route state_checked already takes; :disabled = own attribute, or an <optgroup disabled> ancestor for option, or a disabled <fieldset> ancestor outside its first <legend>. Invalidate on value/attribute writes (wrote_to_control_).

### Textarea raw value vs API value: no newline normalisation, no wrap=hard, defaultValue uses textContent

- **9 subtests / 2 files**, size S, overlaps none, confidence high
- Root cause: state_of seeds a textarea from `txn.text(child)` of direct children (forms.cpp:110-113) and the `value` getter hands that raw value back - the API value must have CRLF and CR normalised to LF, and the 'wrapping transformation' (wrap=hard + cols) is not applied at all. `defaultValue` (control_methods.cpp:1697-1712) is text_content()/set_text() - the spec says child text content (direct Text children only) and 'string replace all'.
- Spec: HTML 4.10.11 The textarea element (raw value, API value, wrapping transformation, defaultValue)
- Engine files: ctbrowser/lib/Shell/page/forms.cpp; ctbrowser/lib/Shell/bindings/element/control_methods.cpp
- Evidence: html/semantics/forms/the-textarea-element/value-defaultValue-textContent.html (6: 'The value property should normalize CRLF and CR to LF expected foo\nbar but got foo\r\nbar', 'defaultValue should reflect the child text content expected foo bar but got foo barbaz'); html/semantics/forms/the-textarea-element/wrapping-transformation.window.js (3: 'Newlines should be normalized to LF', [..]
- Fix: Keep the raw value in control_state; add an `api_value()` that normalises newlines (and the submission path applies the wrapping transformation for wrap=hard with cols); the `value` getter and selection maths use the API value. defaultValue getter = concatenation of direct Text children; setter = string replace all (one Text child). Reuse core/algorithms' whitespace helpers.

### email value sanitisation: multiple not split/trimmed, IDN domain not punycoded

- **7 subtests / 2 files**, size S, overlaps none, confidence high
- Root cause: input_types.cpp:424-428 trims the whole value only; with `multiple` the spec splits on ',', strips whitespace from each address and rejoins with ',' (the comment there defers it to 'the caller that knows multiple', which never rewrites the value). The domain part must be converted to ASCII (url.cpp's domain_to_ascii/punycode exist and are unused here).
- Spec: HTML 4.10.5.1.5 E-mail state (value sanitization algorithm, valid e-mail address list), UTS #46 domain to ASCII
- Engine files: ctbrowser/lib/Shell/page/input_types.cpp; ctbrowser/lib/Shell/net/url.cpp
- Evidence: html/semantics/forms/the-input-element/email.html (3: 'expected user@example.com,user2@example.com but got user@example.com , user2@example.com'); html/semantics/forms/the-input-element/email-value-idn.html (4: 'expected test@xn--exmle-hra.com but got test@exämle.com')
- Fix: In the email branch: split on ',' when the element has `multiple`, trim each with html_whitespace, punycode the part after the last '@' via url.cpp's domain_to_ascii, join with ','; re-sanitise when `multiple` is added/removed (settle_types-style).

### Form owner ignores the parser's form element pointer and `form=` in reset

- **5 subtests / 2 files**, size S, overlaps none, confidence high
- Root cause: form_owner (control_methods.cpp:702-723) is an ancestor walk plus the `form` attribute; the tree builder records no form element pointer (rg 'form element pointer' in lib/DOM: none), so a control inside a <table> whose <form> self-closed has no owner; browser::reset -> forms_.reset_form walks the form's descendants only, so `<input form=form5>` is not reset.
- Spec: HTML 4.10.17.3 form owner (parser-inserted association), 13.2.6.4.7 'in table' insertion mode for form, 4.10.21.4 reset
- Engine files: ctbrowser/lib/DOM/treebuilder.cpp; ctbrowser/lib/Shell/bindings/element/control_methods.cpp; ctbrowser/lib/Shell/page/forms.cpp; ctbrowser/lib/Shell/browser/actions.cpp
- Evidence: html/semantics/forms/the-input-element/reset.html (3 of 6: 'reset button resets controls associated with a form using the form attribute' expected foobar got barfoo); html/semantics/forms/form-control-infrastructure/form.html (2: 'label-in-table-with-control.form expected form3 but got null')
- Fix: Tree builder: when the form element pointer is set and the element is form-associated with no `form` attribute, record the association (an internal attribute or a document-side map the DOM keeps); form_owner consults it before the ancestor walk. reset(): iterate elements_of_form (which already resolves `form=`) instead of the descendant walk.

**Long tail:** customizable <select> (48 subtests / 15 files: <selectedcontent>, base-appearance UA sheet, select.value with non-option parts - a new spec, needs the element + UA styles); form-submission-target rel-*-target.html (28: BroadcastChannel + window.open/opener); partial-updates/tentative (34, tentative spec); aria-attribute-reflection-enumerated.tentative (26: the tentative test expects the enumerated defaults \"none\"/\"false\", HEAD's cb3d3f88 returns null to satisfy the stable aria-attribute-reflection.html - a deliberate conflict); innerText getter (7: ::first-line/::first-letter [..]

**Skipped (already fixed at HEAD):** select-event.html (186 subtests, the slice's largest file): d813ab52 makes a cloned control's selection collapse at 0 (the 'disconnected node' / 'twice in disconnected node' cases, 'expected 0 but got 6') and 35ea1702 stops setRangeText moving the caret before the range is set (the 'a second time (must not fire)' cases); with the clone no longer fully selected the remaining timer ordering looks right, so not counted - verify in the round-seven measurement. color.html (12: 'Valid simple color' / 'leading space' cases): d69dee50 parses the value as a CSS <color> and serialises the colour well - [..]

## html-parsing — html/syntax + domparsing + selection + url (html/browsers, html/infrastructure, html/editing and encoding/ were NOT in the wide run: /tmp/w-9f8da347/html.json holds only html/{semantics,dom,webappapis,syntax}; no encoding.json exists)

Baseline: 615 files in slice at 9f8da347: 303 PASS, 155 FAIL, 133 SKIP (reftests/crashtests/testdriver), 16 HARNESS_ERROR, 8 TIMEOUT; 54,308 subtests, 3,011 failing. Per suite (files pass/total, failing subtests/total): domparsing 14/72, 1350/1612; html/syntax 209/311, 255/8695; selection 41/183, 740/34066; url 39/49, 666/9935.

### streamHTML/streamAppendHTML/... (WritableStream-fed streaming parser) does not exist

- **754 subtests / 29 files**, size L, overlaps none, confidence high
- Root cause: The same tentative proposal's streaming half: `element.streamHTML()` returns a WritableStream whose chunks drive an incremental HTML parser at a position, with sanitizer, custom-element reaction, parser-blocking-script and declarative-shadow-root semantics. The engine has no WritableStream at all (rg WritableStream in lib/Script and lib/Shell: nothing), no incremental tokenizer entry point (parse_html_fragment is one-shot) and no Sanitizer.
- Spec: WICG declarative-partial-updates streamHTML; Streams Standard §4 WritableStream; HTML §13.2 parser re-entrancy
- Engine files: ctbrowser/lib/Shell/bindings/shadow_dom.cpp; ctbrowser/lib/DOM/tokenizer.cpp; ctbrowser/lib/DOM/treebuilder.cpp; ctbrowser/lib/Script/builtins/
- Evidence: domparsing/tentative/stream-*.html: 754 failing subtests across 29 files (stream-html-sanitizer 192, stream-positional 82, stream-sanitizer-img-ctor 65, stream-fragment-context 54, stream-insertion-logic 52, stream-html-with-declarative-shadow-root 36, stream-tokenizer-context 32, ...), every one "`streamHTML` is undefined, not a function"; 6 more stream-html-with-trusted-types-*.html files are [..]
- Fix: Only worth it as a whole: (1) WritableStream/WritableStreamDefaultWriter in the VM builtins (Streams §4), (2) a resumable tokenizer+tree-builder (the document.write path already re-enters the parser at </script>; expose 'feed(chunk)' / 'close()' on a tree_builder held by the element), (3) the positional insertion of the previous cluster, (4) the Sanitizer default config. L, and tentative - recommend deferring; note it in wpt.md as 'tentative, not planned'.

### URL parse failure never throws at XMLHttpRequest.open, window.open (missing) and Location assignment

- **570 subtests / 1 files**, size M, overlaps none, confidence high
- Root cause: url/failure.html feeds 190 unparseable absolute URLs (file://example:1/, http://user:pass@/, https://%EF%BF%BD ...) to three APIs and expects SyntaxError from each. (1) xhr.cpp:186-189 `open` bypasses the parser whenever the input contains "://" (`given.find("://") != npos ? given : resolve(...)`) and net/url.cpp:1702 `resolve` is deliberately lenient - it returns the reference unchanged on failure - so `url.empty()` at xhr.cpp:193 never fires. (2) `window.open` does not exist (no "open" global in lib/Shell/bindings/window/*, subtest message "`open` is undefined"). (3) A frame window's `location` is a plain data property (frames.cpp:379 `frame_window->set("location", made.location_)`, same [..]
- Spec: XHR §4.5.6 open() steps 7-8; HTML §7.2.2.1 window.open() steps; HTML §7.10.1 Location href setter; URL §4.4 basic URL parser failure
- Engine files: ctbrowser/lib/Shell/bindings/xhr.cpp; ctbrowser/lib/Shell/bindings/frames.cpp; ctbrowser/lib/Shell/bindings/window/window.cpp; ctbrowser/lib/Shell/net/url.cpp
- Evidence: url/failure.html: 570/1175 failing = 190 "XHR: ... should throw" (assert_throws_dom did not throw) + 190 "Location's href: ... should throw" (did not throw) + 190 "window.open(): ... should throw" (TypeError `open` is undefined)
- Fix: xhr.cpp open: replace the "://" shortcut with `parse_url(given, location_href_)` and throw DOMException SyntaxError on nullopt (XHR 4.5.6 steps 7-8). Add `window.open(url='', target, features)` (HTML 7.2.2.1 window open steps): parse url against the document base URL, failure -> SyntaxError; return null (engine cannot open windows) or a stub with `close()` - the test only needs the throw. Make `location` an accessor on both the page window and the frame window (frames.cpp:379, window install) whose setter runs Location's href setter (HTML 7.10.1 step 3-5: parse against the entry base, failure -> SyntaxError, else navigate the frame - frames.cpp already has a navigate path from 456b2a5b). sendBeacon passes by accident (undefined -> TypeError); leave it.

### Positional HTML methods (appendHTML/prependHTML/beforeHTML/afterHTML/replaceWithHTML/replaceChildrenHTML + Unsafe) do not exist

- **360 subtests / 4 files**, size M, overlaps none, confidence high
- Root cause: WICG declarative-partial-updates (tentative). `object[method_name]` is undefined on Element, ShadowRoot and CharacterData ("`a computed member` is undefined, not a function" x336; the 12 assert_throws cases expected HierarchyRequestError for a comment/template-child target and got the TypeError). The engine already has both halves: setHTMLUnsafe/getHTML with declarative shadow roots (shadow_dom.cpp:317) and insertAdjacentHTML's position logic (node_methods.cpp:163-230, `adjacent_place`).
- Spec: WICG declarative-partial-updates (https://github.com/WICG/declarative-partial-updates); HTML setHTMLUnsafe; DOM Parsing insertAdjacentHTML; Sanitizer API default config
- Engine files: ctbrowser/lib/Shell/bindings/element/node_methods.cpp; ctbrowser/lib/Shell/bindings/shadow_dom.cpp; ctbrowser/lib/Shell/bindings/element/interfaces.cpp
- Evidence: domparsing/tentative/positional-methods.html 348/348; domparsing/tentative/positional-template-child.html 12/12 (HierarchyRequestError on a template's direct child); domparsing/tentative/positional-methods-with-parser-options.html + -with-trusted-types.html: HARNESS_ERROR (`trustedTypes` is not defined)
- Fix: One installer over the six positions: parse markup as a fragment with context = the target's parent for before/after/replaceWith (the target itself for append/prepend/replaceChildren), attach declarative shadow roots for the Unsafe variants, then reuse insertAdjacentHTML's placement (beforebegin/afterbegin/beforeend/afterend + replace). The 'safe' variants apply the Sanitizer default config (drop <script>, on* attributes, javascript: URLs) and never run scripts; Unsafe runs scripts only with `{runScripts: true}` in the second argument. Throw HierarchyRequestError when the target's parent is a template content / null for the sibling positions. Tentative spec - decide whether it earns a session before spending one; the trustedTypes files stay HARNESS_ERROR without a Trusted Types stub.

### Frame scripts run in the page's realm: iframe-driven tests in this slice (Selection, DOMParser, parser reparenting, html5lib scripted variants)

- **202 subtests / 32 files**, size L, overlaps REALM, confidence high
- Root cause: frames.cpp header says it: a frame runs none of its own scripts and contentWindow is a proxy over the page's globals. So `iframe.contentWindow.DOMParser` is undefined (DOMParser-parseFromString-url*: "Cannot read properties of undefined (reading 'parseFromString')", "`doParse` is undefined"), a made-in-frame document cannot get the frame document's URL ("Child window ... expected file:///common/blank.html?1 but got about:blank"), deleteFromDocument.html's iframe defines `run` ("`run` is undefined, not a function - in `initializeIframe`"), getSelection.html wants a distinct Selection per frame window, adoption-agency-reparenting*/custom-element-reparenting*/foster-parenting-* use [..]
- Spec: HTML §8.1.3 realms/settings objects; HTML §8.6.2 DOMParser (URL = relevant global's associated Document's URL); Selection API §getSelection per window
- Engine files: ctbrowser/lib/Shell/bindings/frames.cpp; ctbrowser/lib/Shell/browser/nested.cpp; ctbrowser/lib/Shell/browser/scripts.cpp; ctbrowser/lib/Shell/bindings/selection.cpp; ctbrowser/lib/Shell/bindings/document/second_document.cpp
- Evidence: selection/deleteFromDocument.html 58 (`run` is undefined in initializeIframe); domparsing/DOMParser-parseFromString-url-base.html 27 + -url.html 27 + -url-moretests.html 43 realm/iframe subtests (of 45/45/31): parseFromString on iframe DOMParser, Child window URL; selection/getSelection.html 6 (iframe's document.getSelection() null / same object as the page's); [..]
- Fix: Covered by round8-brief-REALM: a realm per frame document whose window IS the global object; frames.cpp runs the frame's inline/src scripts under it. Once that lands, also give each frame realm its own Selection instance (install_selection runs per realm) and make DOMParser-made documents take the URL of the realm's document (second_document.cpp:206-219 already reads maker.location_href_).

### selectAllChildren (and Range node arguments) refuse a Document because documents are proxy objects

- **185 subtests / 2 files**, size S, overlaps none, confidence high
- Root cause: selection.cpp:305-311 `selectAllChildren` does `if (!node.is_object()) throw TypeError("parameter 1 is not of type 'Node'")`. `document`, a createHTMLDocument and a DOMParser document are `proxy_object`s - value.hpp:141 `is_object()` is `is_kind(heap_kind::object)` and false for them; value.hpp:150 `is_object_like()` is the check that admits a proxy. The very next line already handles the document case (`is_the_document(node) ? doc_->document_node()`), it is just never reached. The same shape rejects an iframe's contentDocument in Range methods ("Range: the argument is not a Node" in move-selection-range-into-different-root's sub-document cases, range.cpp node-argument check).
- Spec: Selection API §selectAllChildren(node) steps 1-6; DOM §5.5 Range set the start/end
- Engine files: ctbrowser/lib/Shell/bindings/selection.cpp; ctbrowser/lib/Shell/bindings/document/range.cpp; ctbrowser/include/ctbrowser/script/value.hpp
- Evidence: selection/selectAllChildren.html: 177/2242 failing, all with node 11 document / node 14 foreignDoc / node 16 xmlDoc (59 ranges x 3 documents): "parameter 1 is not of type 'Node'"; selection/move-selection-range-into-different-root.tentative.html: 8 sub-document subtests "Range: the argument is not a Node"
- Fix: selection.cpp:307: `if (!node.is_object_like() || (!is_a_document(node) && owner_of(node) == nullptr))`; resolve a document via owner_of(node)->doc_->document_node() rather than only `is_the_document`. Audit the Range node-argument checks in range.cpp for the same `is_object()` test and use `is_a_document`/`owner_of` there too (the sub-document case then falls into the 'root is not this document -> abort' path of the next cluster).

### Selection.modify() is not implemented

- **168 subtests / 15 files**, size L, overlaps none, confidence high
- Root cause: selection.cpp:25 says so ("ponytail: ... no `modify()`"). Every bidi/contenteditable/modify-* file calls getSelection().modify(alter, direction, granularity) and dies with "`modify` is undefined, not a function". Needs caret positions from layout: character/word/line/lineboundary/paragraph/paragraphboundary movement, 'left'/'right' resolved through bidi/visual order, and editing-host limits (modify must not leave the focused editing host).
- Spec: Selection API §modify() (legacy, WebKit-defined: https://w3c.github.io/selection-api/#dom-selection-modify); CSS Writing Modes for bidi visual order
- Engine files: ctbrowser/lib/Shell/bindings/selection.cpp; ctbrowser/lib/Shell/browser/selection.cpp; ctbrowser/lib/Shell/browser/input.cpp; ctbrowser/lib/Layout/
- Evidence: selection/bidi/modify.tentative.html 36, modify-extend-by-character 28, modify-move-by-character 28; selection/caret-position-should-be-correct-while-moveup-movedown.html 24; selection/contenteditable/modify-around-non-editable-span.html 16, modify.tentative.html 15, selection/modify.tentative.html 7; selection/modify-line-{flex-column,flex-row,grid-basic}.tentative 2 each, [..]
- Fix: Bridge the two selections (bindings' Range vs browser/selection.cpp's visual selection) so modify() moves a caret position: map (node, offset) -> layout position via the existing caret hit-testing in browser/input.cpp, step by granularity (character = next grapheme in visual order; word = UAX#29 boundaries the engine's line breaker may already expose; line = same x on the adjacent line box; lineboundary/paragraphboundary = line box / block edges), clamp to the editing host when the focus is inside one, then write back with store_range. Start with character/word/lineboundary forward/backward in LTR (covers modify.tentative, modify-around-*, ~40 subtests), leave bidi 'left/right' and line movement for later. Whole-session work.

### focus() on a contenteditable host does not place the caret at its first caret position

- **149 subtests / 2 files**, size M, overlaps none, confidence high
- Root cause: control_methods.cpp:108 `focus` only calls `on_focus_(id)` (browser focus + visual selection_focus_); the bindings' Selection (the Range in selection.cpp) is never collapsed into the editing host, so after `editor.focus()` the test's bracket-serialisation finds no caret ("Only one caret should be in the editor expected 1 but got 0", 99 of 100 subtests in each variant). Expected (Gecko/Blink, tentative): if the selection is not already inside the host, collapse it at the first caret position - (host,0) when empty, start of the first text node skipping collapsible leading whitespace (not under white-space: pre*), before the first <br>, before an empty inline, inside the first <p>.
- Spec: HTML §6.6.3 focusing steps / §6.6.5 focus update steps (editing host caret placement is UA-defined; tests are tentative and match Gecko+Blink)
- Engine files: ctbrowser/lib/Shell/bindings/element/control_methods.cpp; ctbrowser/lib/Shell/bindings/selection.cpp; ctbrowser/lib/Shell/browser/selection.cpp
- Evidence: selection/contenteditable/initial-selection-on-focus.tentative.html?div 99/100 (+1 `addBrackets is not defined`: editing/include/tests.js is not in the fetched corpus); selection/contenteditable/initial-selection-on-focus.tentative.html?span 50/51
- Fix: In the focusing steps for an element that is an editing host (contenteditable attr, not inside another host): if the selection's range is not inside the host, compute the first caret position by a DOM walk (first text node -> offset past leading collapsible whitespace unless the computed white-space preserves it; first <br>/element -> (parent, index); nothing -> (host, 0)) and store_range a collapsed range there. Ties in with cluster 'selectionchange' (the change must queue the event). Also selection/contenteditable/collapse.html (3): Selection.collapse into an editing host should move focus (activeElement) to that host - the reverse direction, same hook.

### Request.formData()/Response missing (urlencoded body parsing surface)

- **70 subtests / 1 files**, size S, overlaps none, confidence high
- Root cause: resources.cpp:296 defines `Request` and a fetch response object with only text()/json() (resources.cpp:452-455); there is no `Response` constructor ("Response is not defined" x35) and no `formData()` on either ("`formData` is undefined" x35). The test builds Request/Response with an application/x-www-form-urlencoded body and expects formData() to run the URL standard's urlencoded parser (the engine already has that parser for URLSearchParams).
- Spec: Fetch §5.5 Body mixin formData(); URL §5.1 application/x-www-form-urlencoded parser
- Engine files: ctbrowser/lib/Shell/bindings/resources.cpp; ctbrowser/lib/Shell/net/url.cpp
- Evidence: url/urlencoded-parser.any.js 70/105 (35 request.formData(), 35 response.formData())
- Fix: Add a `Response(body, init)` constructor sharing the fetch response prototype (status/ok/headers/text/json), and `formData()` on Request and Response: if Content-Type is application/x-www-form-urlencoded (charset parameter ignored, as Fetch says), run the urlencoded byte parser over the body bytes (UTF-8 decode with replacement) into a FormData; multipart can throw TypeError for now. Reuse URLSearchParams' parser in net/url.cpp.

### No selectionchange event is ever fired (document or text controls)

- **58 subtests / 7 files**, size S, overlaps none, confidence high
- Root cause: rg selectionchange in lib/Shell finds only the onselectionchange attribute name (dispatch.cpp:1721) and the ponytail note in selection.cpp:25. Selection API §'scheduling a selectionchange event' and HTML's 'set the selection range' for input/textarea (control_methods.cpp:2965 set_selection) never queue a task; the four TIMEOUT files wait for the event forever and textcontrols/selectionchange.html counts 0 events after selectionStart=/selectionEnd=/setSelectionRange/select/setRangeText.
- Spec: Selection API §5 'scheduling a selectionchange event' (has scheduled selectionchange event flag); HTML §4.10.19.9 'set the selection range' step 5 (queue an element task to fire selectionchange, [..]
- Engine files: ctbrowser/lib/Shell/bindings/selection.cpp; ctbrowser/lib/Shell/bindings/element/control_methods.cpp; ctbrowser/lib/Shell/bindings/events/dispatch.cpp
- Evidence: selection/textcontrols/selectionchange.html 44/60 (expected 1 but got 0); selection/onselectionchange-on-document.html 4 TIMEOUT, selection/textcontrols/selectionchange-bubble.html 4 TIMEOUT, selection/textcontrols/onselectionchange-content-attribute.html 2 TIMEOUT; selection/onselectionchange-on-distinct-text-controls.html 2, selectionchange-on-shadow-dom.html 1, [..]
- Fix: store_range (selection.cpp:65) -> if the document's 'has scheduled selectionchange event' flag is false, set it and queue a task that clears it and fires `selectionchange` (bubbles=false, not cancelable) at the document. control_methods.cpp set_selection (and select/setRangeText/selectionStart/End setters) -> queue an element task to fire `selectionchange` at the control with bubbles=true (HTML 2023: it bubbles from text controls; selectionchange-bubble.html) - one flag per element so several writes in one task coalesce. Use the existing task queue the timers use (run_due_callbacks) so `await new Promise(setTimeout)` sees it; the onselectionchange content attribute on the body/document already resolves through the attribute list.

### Selection cannot point into a shadow tree; no composed-range rescoping

- **45 subtests / 10 files**, size M, overlaps none, confidence high
- Root cause: selection.cpp:143-152 `rooted_here` requires root_of_tree(id) == the document root, so setBaseAndExtent/collapse/extend on a shadow-tree node silently abort (spec since 2023: the check is the SHADOW-INCLUDING root). When anchor and focus are in different trees the engine routes through Range.comparePoint, which throws WrongDocumentError (range.cpp:495 "the node is in another tree") - the spec sets start then end with 'set the start/end' which collapses instead, and keeps anchor/focus separately so getComposedRanges still answers. getComposedRanges (selection.cpp:362) ignores `options.shadowRoots`, never rescopes a boundary out of an unlisted shadow root to (host's parent, host index), and [..]
- Spec: Selection API §setBaseAndExtent / §extend (shadow-including root, 2023+); §getComposedRanges(options) rescoping; §containsNode; DOM §4.2.2 shadow-including root
- Engine files: ctbrowser/lib/Shell/bindings/selection.cpp; ctbrowser/lib/Shell/bindings/document/range.cpp; ctbrowser/lib/Shell/bindings/shadow_dom.cpp
- Evidence: selection/shadow-dom/tentative/Selection-getComposedRanges.html 7/12; selection/shadow-dom/tentative/Selection-getComposedRanges-dom-mutations-removal.html?mode=open 6/6 and ?mode=closed 6/6; selection/shadow-dom/tentative/Selection-getComposedRanges-slot.html 2, -collapsed.html 1, Selection-deleteFromDocument-around-shadow.html 2; [..]
- Fix: (1) rooted_here: walk shadow-including (root_of_tree then shadow host, repeat) and accept when it reaches this document. (2) setBaseAndExtent/extend: order the two points by a shadow-including compare (rescope both to the common tree first); if the trees differ, the live Range collapses at focus (spec 'set the start/end') but the selection remembers anchor+focus for getComposedRanges - store the four values in slots beside range_slot. (3) getComposedRanges(options): validate every entry is a ShadowRoot wrapper (TypeError), then for each boundary: while node's root is a shadow root not in the list, node = host, offset = index(host) (+1 for the end). (4) containsNode: false when the node is not in the flat tree (no assigned slot under a host). (5) On mutation (the live-range hook from c1110fac), if the selection's range root is no longer this document (host removed, range moved to a [..]

### Shared interface getters read a made document's node id from the primary document's tree (table.rows on createHTMLDocument)

- **42 subtests / 3 files**, size M, overlaps none, confidence medium
- Root cause: Interface prototypes are shared across documents (second_document.cpp:27-35 adopt_interfaces_of) but the getters capture `this` = the primary bindings: control_methods.cpp:257 `table_rows` walks `doc_->read()` and the `rows` getter at :355 uses `receiver(c)` (wrapper.cpp:121 - unpacks the handle, no owner lookup). Only 12 call sites redirect through owner_of (reflection.cpp:1030 'THE DOCUMENT THAT OWNS THE RECEIVER answers'). Every template clearing-stack test does `doc = newHTMLDocument(); doc.body.innerHTML = '<table id=tbl><tbody><template>...</template><tr id=tr><td>...'; table.rows.length` and gets 0 - the parse is right (html5lib ?file=template passes 113/113; treebuilder.cpp:544 [..]
- Spec: HTML §4.9.1 HTMLTableElement.rows; DOM §4.5 document ownership (ownerDocument)
- Engine files: ctbrowser/lib/Shell/bindings/element/control_methods.cpp; ctbrowser/lib/Shell/bindings/element/wrapper.cpp; ctbrowser/lib/Shell/bindings/document/second_document.cpp; ctbrowser/lib/Shell/bindings/element/reflection.cpp
- Evidence: html/syntax/parsing/template/clearing-the-stack-back-to-a-given-context/clearing-stack-back-to-a-table-body-context.html 33/33, ...-table-context.html 6/6, ...-table-row-context.html 3/3: all "Wrong number of table rows expected 1 but got 0"; probably also template/additions-to-the-in-{head,body}-insertion-mode/generating-of-implied-end-tags.html 2+2 ("Template element must be parsed got [..]
- Fix: Generalise reflection.cpp:1030's redirect into the installers: in `getter()`/`method()`/`operation()` (interfaces.cpp / control_methods.cpp) wrap each native so that `dom_bindings * owner = owner_of(c.current_this()); if (owner && owner != this) return owner->same_native(...)` - simplest is to install every prototype native as a thin trampoline that looks up the owner and calls the owner's copy of the lambda (store the lambdas in a table keyed by (interface, name) shared by all bindings). Or the cheaper first step: make `receiver()` return a (bindings*, node_id) and change the table/section/row getters (rows, tBodies, tHead, tFoot, caption, cells, sectionRowIndex, rowIndex) to read `owner->doc_`. Verify with a unit test: createHTMLDocument + innerHTML table + rows.length.

### JS strings are byte-indexed UTF-8 (the documented UTF-16 gap) - substring of a non-ASCII char returns its first byte

- **15 subtests / 1 files**, size L, overlaps none, confidence high
- Root cause: docs/script.md 'The UTF-16 gap': string_object holds std::string UTF-8 and every index method is byte-based (string.cpp:551-563 substring, :324 charAt, :461 charCodeAt use s.size()). with-inheritance.html does `content.substring(i, i+1)` on a decoded windows-1253 ζ and gets the byte 0xCE ("expected 'ζ' but got 'Î'", 15 subtests) - the decode is right, the string model is wrong. VM-owned; listed here because it is the whole remainder of that file.
- Spec: ECMA-262 §6.1.4 String type (UTF-16 code units)
- Engine files: ctbrowser/lib/Script/builtins/text/string.cpp; ctbrowser/include/ctbrowser/script/value.hpp; ctbrowser/docs/script.md
- Evidence: html/syntax/charset/with-inheritance.html: 15 'Check character' subtests (expected ζ or ж, got the first UTF-8 byte)
- Fix: Owned by the VM/test262 agents: either a UTF-16 (or latin1/UTF-16 dual) string_object, or, as a bridge, code-unit-indexed accessors (length/charAt/charCodeAt/substring/slice/indexOf) that map UTF-16 indices to byte offsets with a cached ASCII-only flag so the common case stays O(1). unittests/js/string_basics.cpp pins today's wrong answers as the acceptance list.

### Encoding: no 'change the encoding' after the 1024-byte prescan, and the prescan sees <meta> inside script/style/title

- **13 subtests / 3 files**, size M, overlaps none, confidence high
- Root cause: (a) treebuilder.cpp has no encoding hook (rg encoding: only <annotation-xml encoding>), so a <meta charset> found by the tree builder after the prescan window never triggers HTML 13.2.3.4 'changing the encoding while parsing' - after-1kb.html, after-head-in-1kb(-crlf).html and ncr.html keep the parent's windows-1253 instead of the declared windows-1251. (b) encoding.cpp:428 prescan_encoding treats `<script><meta charset=windows-1251></script>` (and <style>, <title>) as a real <meta> tag, so in-script/in-style/in-title switch to 1251 when the reftests (rel=mismatch) expect the inherited 1253. (c) inheritance-bogus-meta(-utf-8): a child with an unrecognised <meta charset> label must inherit [..]
- Spec: HTML §13.2.3.2 determining the character encoding step 8 (parent inheritance); §13.2.3.3 prescan a byte stream; §13.2.3.4 changing the encoding while parsing; §13.2.6.4.4 in head <meta>
- Engine files: ctbrowser/lib/DOM/encoding.cpp; ctbrowser/lib/DOM/treebuilder.cpp; ctbrowser/lib/Shell/bindings/frames.cpp
- Evidence: html/syntax/charset/with-inheritance.html: 7 'Check characterSet' subtests (after-1kb, after-head-in-1kb, after-head-in-1kb-crlf, ncr expect windows-1251; in-script, in-style, in-title expect windows-1253); html/syntax/charset/inheritance-bogus-meta.html 3/4 (TIMEOUT), inheritance-bogus-meta-utf-8.html 3/3
- Fix: (a) In the tree builder's in-head <meta> step, when the document's encoding confidence is 'tentative' (inherited/default) and the token declares a supported charset different from the current one, stop and re-run the parse of the same bytes with the new encoding (the loader has the bytes; frames.cpp:294-299 already re-decodes) - the navigate-again path, cheap here since the whole byte stream is in memory. (b) In prescan_encoding, when a tag start is script/style/title/textarea/xmp/noembed/noframes/iframe/noscript, skip to the matching case-insensitive `</name` before continuing (what a real tokenizer sees; matches the reftests). (c) An unknown label in the child -> keep inheriting; check decode of the utf-8 parent + bogus child gives U+FFFD for invalid bytes.

### insertAdjacentHTML on a parentless element / Document parent throws the wrong thing or nothing

- **13 subtests / 2 files**, size S, overlaps none, confidence high
- Root cause: node_methods.cpp:163-200 adjacent_place: a parentless element returns nullopt silently (spec: throw NoModificationAllowedError) and an element whose parent is the Document throws HierarchyRequestError (spec: NoModificationAllowedError). The comment cites the OLD spec text ('return null'); DOM Parsing/HTML now says step 2 'If context is null or a Document, throw NoModificationAllowedError'.
- Spec: HTML §insertAdjacentHTML(position, string) step 2 (formerly DOM Parsing §3.2)
- Engine files: ctbrowser/lib/Shell/bindings/element/node_methods.cpp
- Evidence: domparsing/insert_adjacent_html.html 6/31 (3 'parent node is null ... did not throw', 3 'parent is a document ... threw HierarchyRequestError'); domparsing/insert_adjacent_html-xhtml.xhtml 7/30 (same)
- Fix: In adjacent_place for beforebegin/afterend: if parent is null or a Document node -> throw_dom_exception("NoModificationAllowedError"). Keep the SyntaxError for bad positions. One-line change plus the comment.

### Customizable <select>: <selectedcontent>/<button> inside <select> parsed by the old 'in select' rules

- **12 subtests / 3 files**, size S, overlaps none, confidence medium
- Root cause: treebuilder.cpp has no `selectedcontent` (rg: nothing) and its 'in select' mode is the pre-2025 one; webkit02.dat's `<select><button><selectedcontent></button><option>X` cases expect the customizable-select tree (button and selectedcontent kept as children of select, option after the button).
- Spec: HTML §13.2.6.4.16 'in select' insertion mode (current text); HTML §4.10.7 the select element, selectedcontent
- Engine files: ctbrowser/lib/DOM/treebuilder.cpp
- Evidence: html/syntax/parsing/html5lib_url.html?file=webkit02 4/48, html5lib_write.html?file=webkit02 4/44, html5lib_write_single.html?file=webkit02 4/44 - all the <select><button><selectedcontent> cases
- Fix: Port the current spec's 'in select' insertion mode (the 2025 customizable select change: start tags button/selectedcontent/div etc. inside select are inserted rather than ignored; <option>/<optgroup>/<hr> keep their pops; <select> inside <select> pops) and add `selectedcontent` to the tag tables. Re-run the whole html5lib set for regressions (1,723 fixtures pass today).

### Engine-fired load/DOMContentLoaded/pageshow have the wrong flags and pageshow never fires

- **5 subtests / 2 files**, size S, overlaps none, confidence high
- Root cause: dispatch.cpp:682 make_event_object(cx, type, true, true) makes every engine event bubbles=true, cancelable=true; the-end.html asserts DOMContentLoaded cancelable=false ("cancelable should be false expected false got true"), load bubbles=false with target=document, and a pageshow (PageTransitionEvent, bubbles+cancelable, persisted=false) after load - never dispatched (rg pageshow: nothing), so 'load'/'pageshow'/'order' time out. DOMContentLoaded-defer.html: DOMContentLoaded fires before a task queued by a defer script (frame.cpp:111 fires it synchronously after parse, before run_due_callbacks).
- Spec: HTML §13.2.7 'the end' steps 3-6; HTML §7.4.6 completely finish loading (pageshow); PageTransitionEvent
- Engine files: ctbrowser/lib/Shell/browser/frame.cpp; ctbrowser/lib/Shell/bindings/events/dispatch.cpp
- Evidence: html/syntax/parsing/the-end.html 4/4 (1 FAIL + 3 TIMEOUT); html/syntax/parsing/DOMContentLoaded-defer.html 1/1
- Fix: Give the engine-event maker per-type flags: DOMContentLoaded {bubbles:true, cancelable:false}, load {false,false} targeted at the document but dispatched at the window; fire pageshow (PageTransitionEvent with persisted=false) right after load (HTML 7.4.6 'completely finish loading' step). Run defer scripts and then DOMContentLoaded as tasks in order (HTML 13.2.7 'the end' steps 3-4: 'spin the event loop until scripts that will execute when the document has finished parsing is empty', then queue a global task for DOMContentLoaded).

### The page parser never attaches declarative shadow roots (<template shadowrootmode> in loaded markup)

- **3 subtests / 5 files**, size S, overlaps none, confidence high
- Root cause: attach_declarative_shadow_roots (shadow_dom.cpp:231) is called only from setHTMLUnsafe (shadow_dom.cpp:342) and the parseHTMLUnsafe/DOMParser path (:366). treebuilder.cpp has no shadow attachment (rg shadow: nothing), and neither browser/frame load nor frames.cpp calls the helper, so in a normally loaded page `host.shadowRoot` is null and `root.getElementById(...)` throws before any test registers (HARNESS_ERROR). document.hpp:262's 'THE PARSER ATTACHED THIS ONE' flag exists for exactly this and is set only by the two fragment paths.
- Spec: HTML §13.2.6.4.9 'in head' end tag template: declarative shadow root attachment steps; HTML §4.12.2 template shadowrootmode
- Engine files: ctbrowser/lib/Shell/bindings/shadow_dom.cpp; ctbrowser/lib/Shell/browser/frame.cpp; ctbrowser/lib/Shell/bindings/frames.cpp; ctbrowser/lib/DOM/treebuilder.cpp
- Evidence: HARNESS_ERROR: selection/shadow-dom/tentative/Selection-isCollapsed.html, selection/shadow-dom/tentative/Selection-getComposedRanges-range-update.html ("Cannot read properties of null (reading 'getElementById')"), selection/shadow-dom/cross-shadow-boundary-extend.html ("reading 'querySelector'"); selection/shadow-dom/tentative/Range-isPointInRange.html 2/2 (same null shadowRoot); [..]
- Fix: After the page (and each frame document, frames.cpp ~line 300) finishes parsing, call attach_declarative_shadow_roots(*doc_, document root) exactly as setHTMLUnsafe does - or better, do it from the tree builder's template end-tag step (HTML 13.2.6.4.9 'in template' </template>: if the template has shadowrootmode and its parent can host, attach and move the contents) so document.write and streaming parses get it too. Hits the shadow-dom suite (other slice) at least as hard - four files here hide whole test files.

**Long tail:** Not engine: editing/include/{editor-test-utils,tests}.js are not in the fetched corpus (49 subtests: modify-around-inline-element-boundary 36, setBaseAndExtent-start-or-end-in-anonymous-shadow-container 11, deleteFromDocument-HTMLDetails HARNESS_ERROR, 2x addBrackets) - fix tools/wpt/fetch-wpt.sh; server-only files (55: innerhtml-mxss.sub.html {{host}} substitution 30, percent-encoding.window.js .py 16, xmldecl-2 .headers 6, the-input-byte-stream-001/016/018 HTTP charset 3), url/historical.any.js (self.GLOBAL harness), 2 HARNESS_ERROR on `history` undefined. Engine singles: IdnaTestV2 V7 2, [..]

**Skipped (already fixed at HEAD):** Already landed in round seven (b722aa41), not counted: (1) e8407cca + 9a7423e6 - the XML serialization algorithm's prefix map and the HTML-namespace end-tag rule (only non-HTML empty elements self-close; HTML void ' />'; <div></div> otherwise) and the XML parser's null namespace: domparsing/XMLSerializer-serializeToString.html 29 (baseline messages are the xhtml-xmlns and xmlns-redeclaration shapes the commit names), html/syntax/serializing-xml-fragments/outerHTML.html 97 (every failure is `<a xmlns=...></a>` vs `/>`), DOMParser-parseFromString-xml-internal-subset 1 (xmlns=xhtml on <foo>) - [..]

## css-om — css/css-values, css/cssom, css/css-cascade, css/css-conditional, css/css-variables, css/css-typed-om, css/css-syntax, css/css-nesting, css/mediaqueries

Baseline: At 9f8da347 (/tmp/w-9f8da347/css.json; the m-9f8da347 css-css-values/css-cssom files carry identical numbers): css/css-values 508 files = 171 PASS / 89 FAIL / 7 TIMEOUT / 4 HARNESS_ERROR / 237 SKIP, 7,611 of 10,346 subtests PASS. css/cssom 221 files = 156 PASS / 36 FAIL / 29 SKIP, 3,168 of 3,559. css/css-cascade 154 = 48 PASS / 45 FAIL / 1 TIMEOUT / 60 SKIP, 1,005 of 1,152. css/css-conditional [..]

### calc-size() calculations and interpolate-size: allow-keywords are not interpolated (2,453 subtests in 18 files)

- **2453 subtests / 18 files**, size L, overlaps none, confidence medium
- Root cause: Two missing pieces in lib/Style/easing.cpp interpolate_pair (lines 1140-1154, the pairwise calc-size port that landed in round seven as 84aa2ef1): (1) the CALCULATION half `B` of calc-size(A, B) is interpolated by recursing interpolate_pair('size', 'size * 2'), which reaches numeric_of (easing.cpp:198-219) -> css::evaluate_math without the size symbol enabled (evaluator.cpp:549 only accepts `size` when size_symbol_ && basis_ == symbolic), so the pair falls to the list path, is declared discrete, and the harness reads back '0px' at every progress ('from [calc-size(auto, size)] to [calc-size(auto, size * 2)] at (0) should be [100px]' got '0px'; width rows get '400px'/'200px'). CSS Values 5 [..]
- Spec: CSS Values 5 §10.2 interpolate-size, §10.3 'Interpolation of calc-size()' (basis compatibility, `any`, linear mix of the calculation), CSS Sizing 3 §5.1; interpolation-testcommon.js
- Engine files: /home/alex/Downloads/claude/wt/ctbrowser-wpt/ctbrowser/lib/Style/easing.cpp; /home/alex/Downloads/claude/wt/ctbrowser-wpt/ctbrowser/lib/Style/css/calc/evaluator.cpp; /home/alex/Downloads/claude/wt/ctbrowser-wpt/ctbrowser/lib/Shell/bindings/animations/css.cpp; /home/alex/Downloads/claude/wt/ctbrowser-wpt/ctbrowser/include/ctbrowser/layout/values.hpp; [..]
- Evidence: css/css-values/calc-size/animation/calc-size-height-interpolation.html 770/1266 FAIL ('expected "100px" but got "0px"' for every calc-size(auto,size)->calc-size(auto,size*2) row, all four runners); css/css-values/calc-size/animation/calc-size-width-interpolation.html 530/842; css/css-values/calc-size/animation/interpolate-size-height-interpolation.html 212/318 ('from [initial] to [90px] at (0) [..]
- Fix: In easing.cpp: (a) add `interpolate_size_allows_keywords` to css::length_context (or a bool argument threaded from bindings/animations/css.cpp:239 interpolable_text and the transition starter, taken from the element's computed `interpolate-size`; the property is already in the table); (b) in interpolate_pair before the calc-size arm, for the sizing properties (width/height/min-*/max-*/inline-size/block-size/flex-basis/...): if allow-keywords, rewrite `auto|min-content|max-content|fit-content|stretch|content` to `calc-size(kw, size)` and a plain length/percentage L to `calc-size(any, L)`; (c) bases pair when textually equal or either is `any` (the other wins); otherwise discrete; (d) emit the calculation as text: `calc-size(<basis>, calc((1 - p) * (B1) + p * (B2)))`, never through numeric_of - layout's size_calc_reader (values.hpp:362) folds linear expressions in `size` (extend it for [..]

### getComputedStyle insets: position:fixed resolves against the nearest positioned ancestor instead of the transformed ancestor/viewport; relative percentages are not absolutized (316 subtests)

- **316 subtests / 4 files**, size S, overlaps none, confidence high
- Root cause: computed_style/entries.cpp:233-245 picks `at.containing` by walking to the first ancestor with `is_positioned()`, for fixed boxes too; the test's #container-for-fixed is `position:absolute; transform:scale(1)` and CSS Transforms 1 §2 makes a transformed box the containing block for FIXED descendants (the abspos container is 200px tall, the transformed one 300px: 'top' expected 30px got 20px for 10%). The layout pass has the same gap: lib/Layout/position.cpp:72-104 walk() carries `fixed_cb` unchanged from apply_positioning (line 230, the viewport) and never replaces it when `box->transformed` (box.hpp:168 already computes that flag). Relative (72 subtests): entries.cpp:783-795 returns the [..]
- Spec: CSSOM §6.7.2 resolved value special case for top/right/bottom/left; CSS Transforms 1 §2 (transform establishes a containing block for fixed descendants); CSS Position 3 §3.5 relative positioning, [..]
- Engine files: /home/alex/Downloads/claude/wt/ctbrowser-wpt/ctbrowser/lib/Shell/bindings/computed_style/entries.cpp; /home/alex/Downloads/claude/wt/ctbrowser-wpt/ctbrowser/lib/Layout/position.cpp; /home/alex/Downloads/claude/wt/ctbrowser-wpt/ctbrowser/include/ctbrowser/layout/box.hpp
- Evidence: css/cssom/getComputedStyle-insets-fixed.html 216/324 (180 x 'top' expected Npx got Npx, 36 x 'bottom'; e.g. 'Percentages are absolutized into pixels' top expected "30px" got "20px"); css/cssom/getComputedStyle-insets-relative.html 72/252 ('top' expected "10px" got "10%", expected "9px" got "calc(10% - 1px)"); css/cssom/getComputedStyle-insets-absolute.html 24/324 (rtl / vertical-lr / vertical-rl [..]
- Fix: position.cpp walk(): after computing `inner` for a positioned box, also `if (box->transformed) fixed_inner = padding_box_of(...)` and pass it as fixed_cb to the children (CSS Transforms §2; extend to filter/perspective/contain:paint later). entries.cpp ancestor loop: for `at.position == fixed` skip ancestors that are merely positioned and stop at the first `transformed` one (else keep the viewport rect). Relative arm: drop the over-constrained early return; `if (!mine.is_auto()) return px_text(mine.resolve(relative_basis, font_size))` for every given side (the opposite-auto negation stays). Absolute rtl/vertical: derive the static position from the CB's writing-mode/direction (the test's staticPositionX/Y formulas at support/getComputedStyle-insets.js:69-79 are the spec's). Unit test: a fixed box under a transformed abspos ancestor answers top against the transformed box's padding box.

### CSS.supports("prop:value") one-argument form without parentheses answers false (the 172 css-conditional HARNESS_ERRORs and every revert-rule file)

- **233 subtests / 182 files**, size S, overlaps none, confidence high
- Root cause: The one-argument CSS.supports binding (lib/Shell/bindings/css.cpp:52-58) hands the string to style::css::supports_condition -> condition() in lib/Style/css/properties/declaration.cpp:71-92, which returns false unless the body starts with '(' (line 89: `if (body.front() != '(' || body.back() != ')') return false;`). CSS Conditional 4 §6.1 step 2 says: if conditionText does not parse as a <supports-condition>, try it WRAPPED IN PARENTHESES. cq-testcommon.js probes `CSS.supports("container-type:size")` / `"container-type:scroll-state"` (110 + 41 HARNESS_ERROR files: 'assert_implements: Basic support for size container queries required'), and every revert-rule test opens with [..]
- Spec: CSS Conditional 4 §6.1 'supports(conditionText)' step 2 (wrapped in parentheses); CSS Conditional 5 §4.2 container-type
- Engine files: /home/alex/Downloads/claude/wt/ctbrowser-wpt/ctbrowser/lib/Shell/bindings/css.cpp; /home/alex/Downloads/claude/wt/ctbrowser-wpt/ctbrowser/lib/Style/css/properties/declaration.cpp; /home/alex/Downloads/claude/wt/ctbrowser-wpt/ctbrowser/include/ctbrowser/style/css/properties.hpp
- Evidence: css/css-conditional/container-queries/* : 110 files HARNESS_ERROR 'Basic support for size container queries required' + 41 files 'scroll-state container queries required' (e.g. nested-query-containers.html 32 NOTRUN subtests, scroll-state/scroll-state-query-with-var.html 6); css/css-cascade/revert-rule-basic.html 4/4, revert-rule-revert-layer.html 5/5, revert-rule-layer.html 2/2, [..]
- Fix: In the one-argument arm of the `supports` method in bindings/css.cpp: `const std::string text = c.to_string(args[0]); return value::boolean(supports_condition(text) || supports_condition("(" + text + ")"));` - exactly what substitute.cpp:1218-1219 does. Do NOT put the fallback inside supports_condition itself: `@supports color:red {}` (parser.cpp:605) must stay invalid. 151 HARNESS_ERROR files then RUN; most will fail on real container-query gaps (cluster 'Container query evaluation') - that is the point, they were hiding hundreds of subtests. Unit test: CSS.supports('color:red') true, CSS.supports('(color:red)') true, CSS.supports('color:nonsense') false.

### @scope without a prelude has no root: the engine is never told a sheet's owner node (69 subtests, 21 files)

- **69 subtests / 21 files**, size M, overlaps FLAT, confidence high
- Root cause: lib/Style/engine.cpp:522-524 says it in a comment: 'An implicit scope - @scope { } with no prelude - is rooted at the sheet's owner node's parent, which this engine is not told; it has no roots here and matches nothing.' engine::add_sheet(css, origin) (engine.hpp:185) takes text and an origin only, and browser/styles.cpp collect_author_styles concatenates every <style>/<link> into one string, so a rule cannot be traced back to its owner element. CSS Cascade 6 §3.1: 'if <scope-start> is omitted the scope root is the parent element of the owner node of the stylesheet (or the shadow host for a shadow tree sheet)'. Every scope-implicit* / scope-style-sharing / scope-declarations subtest reads [..]
- Spec: CSS Cascade 6 §3.1 (@scope prelude omitted: parent of the owner node), §3.2 scoped selectors and :scope, §3.3 nested scopes; CSSOM §6.1 owner node
- Engine files: /home/alex/Downloads/claude/wt/ctbrowser-wpt/ctbrowser/lib/Style/engine.cpp; /home/alex/Downloads/claude/wt/ctbrowser-wpt/ctbrowser/include/ctbrowser/style/engine.hpp; /home/alex/Downloads/claude/wt/ctbrowser-wpt/ctbrowser/lib/Style/css/parser.cpp; /home/alex/Downloads/claude/wt/ctbrowser-wpt/ctbrowser/include/ctbrowser/style/css/value.hpp; [..]
- Evidence: css/css-cascade/scope-implicit.html 9/11 ('@scope without prelude implicitly scopes to parent of owner node' expected "1" got "auto"); css/css-cascade/scope-nesting.html 11/24, scope-shadow.html 9/9 (shadow: FLAT), at-scope-relative-syntax.html 8/8 (nested relative selector), scope-invalidation.html 5/29; css/css-cascade/scope-style-sharing-001.html 4/13, scope-style-sharing-002.html 4/13, [..]
- Fix: (1) css::scope_block gets `bool implicit`; parser.cpp scope_block_of sets it when the prelude has no `(<scope-start>)`. (2) engine::add_sheet(css, origin, node_id owner = {}) records the owner per sheet (sheet index -> owner) and remaps it into scope_entry as `owner`; browser/styles.cpp and nested.cpp stop concatenating - add each <style>/<link>/@import result as its own sheet with its owner element (the CSSOM's author_style_text path in bindings/stylesheets must do the same, one sheet per CSSStyleSheet record; a constructed/adopted sheet's owner is the document or shadow root). (3) engine.cpp each_root: for an implicit scope the single root depth is that of `parent(owner)` (or the shadow host, once FLAT lands) - no selector match needed; is_root becomes 'd == depth of that node'. (4) parser: compile the <scope-end> with the scope's own root as the nesting parent (`&` and implicit [..]

### A frame's media environment (viewport size) is set only during the page's rendering-tick frame layout: matchMedia in an iframe, viewport units in a frame's @keyframes, @media in srcdoc frames (66 subtests)

- **66 subtests / 10 files**, size M, overlaps none, confidence medium
- Root cause: nested.cpp:99-106 sets the frame engine's media_environment viewport_width/height from the frame box's content size inside browser::layout_frames(), which runs from the rendering update (frame.cpp:227-233: `if (dirty_ >= layout) run_layout(); else if (frames_stale()) layout_frames();`) - not from a script-forced layout. media-query-matches-in-iframe.html appends a 100x100 iframe, forces layout with getBoundingClientRect(), then reads `iframe.contentWindow.matchMedia('(max-width: 150px)').matches`, which is evaluated live by dom_bindings::media_query_matches against `selector_engine().environment()` (media_queries.cpp:57-60; the frame's own matchMedia is wired in frames.cpp:384-387) - the [..]
- Spec: CSSOM View §14 'update the rendering' / forced layout; Media Queries 4 §4.1 width/height are the viewport's; CSS Values 4 §6.1.2 viewport-percentage lengths (the frame's viewport); HTML 4.8.5 iframe [..]
- Engine files: /home/alex/Downloads/claude/wt/ctbrowser-wpt/ctbrowser/lib/Shell/browser/nested.cpp; /home/alex/Downloads/claude/wt/ctbrowser-wpt/ctbrowser/lib/Shell/browser/frame.cpp; /home/alex/Downloads/claude/wt/ctbrowser-wpt/ctbrowser/lib/Shell/bindings/media_queries.cpp; /home/alex/Downloads/claude/wt/ctbrowser-wpt/ctbrowser/lib/Shell/bindings/frames.cpp; [..]
- Evidence: css/mediaqueries/media-query-matches-in-iframe.html 22/22 ('matchMedia('(max-width: 150px)').matches should update immediately' assert_true: expected true got false); css/css-values/viewport-units-keyframes.html 24/24 ('Interpolation from 0px to 100vw is 100px at 50%' got '0px'); css/css-cascade/layer-media-query.html 8/8 TIMEOUT (srcdoc frame, 'A1 Basic expected rgb(255, 0, 0) got undefined'), [..]
- Fix: (1) Make the forced-layout entry the bindings use for getBoundingClientRect/getComputedStyle/matchMedia (the `ensure_layout` path in frame.cpp) run `layout_frames()` whenever frames_stale() - and mark frames stale synchronously when a frame element's style/width/height attribute changes, not on the next tick. (2) In nested.cpp, set the frame's environment from the frame box BEFORE the first resolve_all of the frame document (today it happens in the same `moved` block, but the frame's initial parse/keyframe registration may run under the page's env: check bindings/animations/css.cpp keyframe folding uses the frame engine's environment, not the page's). (3) media_query_matches for a frame document must consult that frame's engine (owner_of_media_query_list already routes the list to the right bindings; verify secondary bindings' selector_engine() is the frame's engine after [..]

### Tree-counting functions in @keyframes: sibling-index()/sibling-count() in a keyframe value are copied verbatim into the animated value instead of being computed on the target (59 subtests, 18 files)

- **59 subtests / 18 files**, size M, overlaps none, confidence medium
- Root cause: The cascade folds sibling-index() for declared values (engine.cpp:88-95 marks initial values that use them as per-element, engine.cpp:734-735 supplies sibling facts), but keyframe values are taken by bindings/animations/css.cpp (line 433 area: property name kept, value text stored as written) and interpolated/applied without the substitute+math pass, so `--time: calc(2s * sibling-index())` reads back 'calc(2s * sibling-index())' instead of '6s' (registered properties, 20/20) and the length/percent/transform keyframe files answer the unresolved text (2 each). CSS Values 5 §14: tree counting functions are resolved at computed-value time against the element the value applies to - a keyframe's [..]
- Spec: CSS Values 5 §14 tree counting functions (computed-value time, on the element); CSS Animations 1 §4 (keyframe values computed on the target); CSS Properties & Values 1 §3.2 registered property [..]
- Engine files: /home/alex/Downloads/claude/wt/ctbrowser-wpt/ctbrowser/lib/Shell/bindings/animations/css.cpp; /home/alex/Downloads/claude/wt/ctbrowser-wpt/ctbrowser/lib/Style/css/substitute.cpp; /home/alex/Downloads/claude/wt/ctbrowser-wpt/ctbrowser/lib/Style/engine.cpp; /home/alex/Downloads/claude/wt/ctbrowser-wpt/ctbrowser/lib/Style/css/keyframes.cpp
- Evidence: css/css-values/tree-counting/sibling-index-keyframe-registered-properties-dynamic.html 20/20 ('Initially, the sibling-index() is 3 for --time' expected "6s" got "calc(2s * sibling-index())"); css/css-values/tree-counting/sibling-index-keyframe-{font-style,font-variation-settings,font-weight,length-value,palette-mix,percent,rotate,scale,transform}-dynamic.html 2 each; [..]
- Fix: When an animation's keyframes are computed for a target (the point where custom properties and var() are substituted into keyframe values - bindings/animations/css.cpp around 420-440), run the same per-element fold the cascade runs for a declared value: expose `engine::compute_value_for(node, property, text)` (substitute -> tree-counting facts -> math fold -> registered-property syntax check) and use it for every keyframe value, re-running when the target's sibling facts change (the '-dynamic' files remove #rm and expect the value to follow: the style-change event must invalidate running animations' computed keyframes). Allow the functions in @container size/style queries (media.cpp's `feature` gets the facts from chain_styles_) and refuse them in descriptors (parser.cpp font-face/counter-style blocks).

### var() is not yet an arbitrary substitution function: name from a substitution, any <declaration-value> in the arguments, CSS-wide keywords as fallback (51 subtests, 9 files)

- **51 subtests / 9 files**, size M, overlaps none, confidence high
- Root cause: lib/Style/css/substitute.cpp validates var() at parse time against the Variables 1 grammar `var( <custom-property-name> , <declaration-value>? )`: `var(var(--myvar))` (name produced by an inner substitution), `var(--x ())`, `var({--x})` and `var(prop)` are refused at parse time (el.style stores '' - variable-reference.html expects the specified text to round-trip, var-parsing.html expects the property to be set), while css-variables-2 §2.2/§3.1 make the first argument a <declaration-value> that is substituted first and then must parse as a <custom-property-name> (guaranteed-invalid -> fallback, 'invalid substituted name falls back' expected 20px got 1008px). A CSS-wide keyword produced as [..]
- Spec: CSS Variables 2 §2.2 var() syntax (<declaration-value> name), §3.1 'replace a var() function'; CSS Values 5 §2.2 arbitrary substitution functions (CSS-wide keyword result); CSS Values 5 §2.3 attr() [..]
- Engine files: /home/alex/Downloads/claude/wt/ctbrowser-wpt/ctbrowser/lib/Style/css/substitute.cpp; /home/alex/Downloads/claude/wt/ctbrowser-wpt/ctbrowser/include/ctbrowser/style/css/substitute.hpp; /home/alex/Downloads/claude/wt/ctbrowser-wpt/ctbrowser/lib/Style/css/properties/declaration.cpp; /home/alex/Downloads/claude/wt/ctbrowser-wpt/ctbrowser/lib/Style/engine.cpp
- Evidence: css/css-variables/variable-reference-name-substitution.html 13/24 ('invalid substituted name falls back' expected "20px" got "1008px"; 'unset name-providing var() falls back'); css/css-variables/var-parsing.html 8/18 ("var(--x ())" should set the property value), variable-reference.html 8/18 (width: var(prop) expected "var(prop)" got ""); css/css-variables/variable-css-wide-keywords.html 7/30 [..]
- Fix: Parse-time (declaration.cpp value_check + substitute.cpp's may_substitute scan): a var() whose first argument is a balanced <declaration-value> and whose optional second part follows a top-level comma is VALID at parse time (store the text as written; CSSOM serializes it back). Computed-value time (substituter::run / the var arm): substitute the name argument first (recursively, depth-limited), trim, require a single ident token starting with `--` (else guaranteed-invalid -> use fallback or the whole declaration invalid at computed-value time), then look it up; carry attr-taint through the name. After substitution of the whole value, if the result is exactly one CSS-wide keyword (initial/inherit/unset/revert/revert-layer/revert-rule), hand it to the cascade's keyword resolver (engine.cpp:1300-1345 `put`) instead of the grammar check. Unit tests are the four test names above.

### Presentational hints live in the box builder, not in the cascade: revert/revert-layer cannot roll back <img width>, and SVG presentation attributes are not mapped at all (39 subtests)

- **39 subtests / 2 files**, size M, overlaps none, confidence high
- Root cause: include/ctbrowser/layout/box.hpp:511-520 and 666-680 read `<table width>`, `<td width>`, `<img width/height>`, `<iframe width>` attributes as layout fallbacks (`if (b.width.is_auto()) ... attribute`), so they exist only after the cascade: `.revert-1 { width: revert }` (CSS Cascade 5 §6.5: presentational hints are author-origin declarations that precede all author sheets; `revert` rolls them back too, `revert-layer` treats them as a layer below the author's) still lays the image out at width=44 (presentational-hints-rollback: 12/16, expected client width 60). SVG presentation attributes (`stroke-width="var(--stroke1)"`, fill, stroke, opacity...) are mapped nowhere (rg '"stroke-width"' finds [..]
- Spec: CSS Cascade 5 §6.5 presentational hints (author origin, before author sheets; revert-layer treats them as a separate layer); HTML 15.3.x rendering 'maps to the dimension property'; SVG 2 §... [..]
- Engine files: /home/alex/Downloads/claude/wt/ctbrowser-wpt/ctbrowser/include/ctbrowser/layout/box.hpp; /home/alex/Downloads/claude/wt/ctbrowser-wpt/ctbrowser/lib/Style/engine.cpp; /home/alex/Downloads/claude/wt/ctbrowser-wpt/ctbrowser/include/ctbrowser/style/engine.hpp; /home/alex/Downloads/claude/wt/ctbrowser-wpt/ctbrowser/lib/Style/css/properties/table.cpp
- Evidence: css/css-cascade/presentational-hints-rollback.html 12/16 ('#tests > * 1' img.revert-1 width=44 data-expected-client-width=60); css/css-variables/variable-presentation-attribute.html 27/48 ('Testing stroke-width on #box1' expected "10px" got "1px")
- Fix: Give the engine a per-element presentational-hint source the way the style attribute already is: in engine.cpp's match collection, before the author sheets, synthesize declarations from HTML's mapped attributes (img/iframe/video/table/td width,height -> px or %; bgcolor, align, hidden...) and the SVG presentation attribute list (SVG 2 'presentation attributes': every property in the table whose name matches an attribute on an SVG-namespace element), tagged origin=author, layer index below every real layer, specificity 0, so `revert` strikes them (origin == author) and `revert-layer` strikes only the layers above them (matches_.origin/layer are what rolled_back keys on, engine.cpp:1310-1340). Values go through the same var()/substitute pass as any declaration. Then delete the box.hpp attribute fallbacks (they become dead: keep `<table border>` which is paint). Net: css/css-tables and the [..]

### calc-size()/keyword flex-basis and calc-size(min-content, size/2) widths resolve against an inflated intrinsic measure (39 subtests, 3 files)

- **39 subtests / 3 files**, size M, overlaps none, confidence medium
- Root cause: flex.cpp:537-600 (keyword_main / calc_over_content for flex-basis) and algorithm.cpp:20-45 intrinsic_border_width measure a keyword basis with a size that is 25% too large in the column case (flex-basis: min-content expected 20px got 25px for every keyword; calc-size(min-content, size / 0.25) expected 80px got 100px) and 20% too large for widths (calc-size(min-content, size / 2) expected 200px got 240px: the min-content measure is 480 instead of 400, i.e. the box's padding/border is counted in `size` and again by border_box_size, or the measure is taken in the wrong axis for a column container). `calc-size(stretch, size * 2 - 10%)` is refused by the grammar ('flex-basis value is accepted' [..]
- Spec: CSS Values 5 §10.1 calc-size() (size in the box-sizing box); CSS Sizing 3 §5.1; CSS Flexbox 1 §9.2.3 flex base size
- Engine files: /home/alex/Downloads/claude/wt/ctbrowser-wpt/ctbrowser/lib/Layout/flex.cpp; /home/alex/Downloads/claude/wt/ctbrowser-wpt/ctbrowser/lib/Layout/algorithm.cpp; /home/alex/Downloads/claude/wt/ctbrowser-wpt/ctbrowser/include/ctbrowser/layout/algorithm.hpp; /home/alex/Downloads/claude/wt/ctbrowser-wpt/ctbrowser/lib/Style/css/properties/grammar.cpp
- Evidence: css/css-values/calc-size/calc-size-flex-basis-on-column.html 18/33 ('resolved value for height resulting from flex-basis: min-content' expected "20px" got "25px"); css/css-values/calc-size/calc-size-flex-basis-on-row.html 16/33; css/css-values/calc-size/calc-size-width.html 5/20 ('calc-size(min-content, size / 2)' expected "200px" got "240px")
- Fix: Read the three tests' fixtures (they state the content sizes) and trace calc_over_content's `content` argument for a column flex item and for a block with padding: make `size` the basis in the box-sizing box (content box unless border-box), subtract the inner edges exactly once, and for a column container measure the BLOCK intrinsic size (lay the child out at the cross size, take its auto height) rather than the inline measure. Accept `stretch` as a calc-size basis in grammar.cpp. Net: css/css-flexbox + css/css-sizing.

### CSS Nesting parser/serializer fidelity: a nested rule starting with a combinator is dropped, `&` is canonicalised instead of kept as written, bare declarations after a nested group rule vanish (32 subtests, 10 files; also explains at-scope-relative-syntax's 8)

- **32 subtests / 10 files**, size M, overlaps none, confidence high
- Root cause: lib/Style/css/parser.cpp's style-rule body reader (lines 289-470) decides declaration-vs-rule per CSS Syntax 3 §5.4.4 but a prelude beginning with `>`, `+`, `~` is not recognised as a rule (parsing.html: '.foo { > .bar { } }' Inner rule should exist expected 1 got 0 - CSS Nesting 1 §2.1 allows a <relative-selector-list>, the same anchoring selector.cpp:806-818 already does for :has()). selectorText is regenerated from the compiled selector (`.class&` -> `&.class`, `&&` -> `&`, `:is(.bar,.baz)` loses the implied `& `), where CSSOM serializes the selector as parsed with `&` in place. Declarations that follow a nested rule inside a nested @media are dropped ('Implicit like rule not in first [..]
- Spec: CSS Nesting 1 §2.1 (relative selectors, &), §2.3 (nested group rules), §3 CSSNestedDeclarations; CSSOM §6.4.3 selectorText serialization
- Engine files: /home/alex/Downloads/claude/wt/ctbrowser-wpt/ctbrowser/lib/Style/css/parser.cpp; /home/alex/Downloads/claude/wt/ctbrowser-wpt/ctbrowser/lib/Style/css/selector.cpp; /home/alex/Downloads/claude/wt/ctbrowser-wpt/ctbrowser/lib/Shell/bindings/stylesheets/rules.cpp; /home/alex/Downloads/claude/wt/ctbrowser-wpt/ctbrowser/lib/Shell/bindings/stylesheets/serialize.cpp
- Evidence: css/css-nesting/parsing.html 11/32 ('.foo { > .bar { color: green; }}' Inner rule should exist expected 1 got 0; '.class&' expected ".class&" got "&.class"); css/css-nesting/serialize-group-rules-with-decls.html 6/15, nested-declarations-cssom.html 3/12, cssom.html 2/14, invalid-inner-rules.html 2/2 (@font-face inside a style rule must be rejected), nested-declarations-cssom-whitespace 2/2, [..]
- Fix: (1) In the body reader: a run that starts with a combinator token (`>`, `+`, `~`) or with `&`/ident/`.`/`#`/`[`/`:`/`*` followed by a `{` block at depth 0 is a nested rule; compile its prelude as a relative selector list anchored on `&` (selector.cpp's :has() relative parse, with the nesting parent instead of :scope). (2) Keep the selector's source text (whitespace-collapsed) on the rule record for selectorText/cssText; the compiled form stays for matching. (3) Every maximal run of declarations that is not the first in a nested block (style rule or nested group rule) files a CSSNestedDeclarations record with the parent's selector, in source order (§3.1); an empty one serializes nothing. (4) Reject `&` immediately followed by a type selector and non-nestable at-rules (@font-face, @keyframes...) inside style rules. Net: css/css-nesting + css/css-cascade/scope-*.

### supports() feature functions missing: at-rule(), font-tech(), font-format(), and @import supports() is parsed but never evaluated (31 subtests)

- **31 subtests / 6 files**, size M, overlaps none, confidence high
- Root cause: declaration.cpp leaf() (lines 40-70) knows only `(declaration)`, nested groups and `selector()`. CSS Conditional 5 §6.5 `at-rule(@name)` / `at-rule(@name; descriptor: value)` and §6.4 `font-tech()` / `font-format()` are refused (`CSS.supports(at-rule(@supports))` false; conditionText must serialize `at-rule(@supports)` with the whitespace normalised). parser.cpp:170-230 strips a `supports(...)` off an @import prelude (and rules.cpp:501-515 keeps it in the record) but nothing evaluates it: `@import url() supports()` (empty - must make the import invalid) applies, `supports(font-tech(color-COLRv1))` does not (import-conditions 7/29).
- Spec: CSS Conditional 5 §6.4 font-tech()/font-format(), §6.5 at-rule(); CSS Cascade 5 §5.1 <import-conditions> supports(); CSS Conditional 3 §7 conditionText
- Engine files: /home/alex/Downloads/claude/wt/ctbrowser-wpt/ctbrowser/lib/Style/css/properties/declaration.cpp; /home/alex/Downloads/claude/wt/ctbrowser-wpt/ctbrowser/lib/Style/css/parser.cpp; /home/alex/Downloads/claude/wt/ctbrowser-wpt/ctbrowser/lib/Shell/browser/styles.cpp; /home/alex/Downloads/claude/wt/ctbrowser-wpt/ctbrowser/lib/Shell/bindings/stylesheets/rules.cpp
- Evidence: css/css-conditional/js/supports-at-rule.html 13/25 ('CSS.supports(at-rule(@supports)) expected true but got false'); css/css-cascade/import-conditions.html 7/29 ('supports() is not a valid import condition' got green; 'supports(at-rule(@import)) is a valid import condition' got red); css/css-conditional/at-supports-at-rule-serialization.html 3/26, js/CSS-supports-L5.html 2/9 [..]
- Fix: leaf(): add arms for `at-rule(` (name in parser.cpp's at_kind table + font-face/property/counter-style/page descriptors when a `; descriptor: value` follows - reuse check_declaration with the descriptor grammars), `font-tech(` (a fixed keyword list: features-opentype, features-aat, features-graphite, color-COLRv0/1, color-SVG, color-sbix, color-CBDT, variations, palettes, incremental), `font-format(` (woff, woff2, truetype, opentype, collection, embedded-opentype, svg, plus strings). @import: evaluate the stripped `supports(...)` through supports_condition (wrapped-in-parentheses fallback, as §6.1 says for the import form too) at parser.cpp:206-230 / browser/styles.cpp:38-45 and drop the import when false or empty. conditionText: keep the author's `<general-enclosed>` text, normalise only around the parentheses.

### Constructed/adopted stylesheets on shadow roots do not style shadow content (31 subtests, 10 files) - the flat tree / scoped sheets item

- **31 subtests / 10 files**, size L, overlaps FLAT, confidence high
- Root cause: adoptedStyleSheets on a ShadowRoot are stored (sheets.cpp:131-140, 474-540) but lib/Style has no notion of a shadow tree's scoped stylesheet list; the shadow's children get no computed style at all ('expected rgb(0, 0, 0) got ""'). This is exactly FLAT batch (2) 'scoped stylesheets and flat-tree inheritance' (CSS Scoping 1 §3.2, CSSOM §... adoptedStyleSheets order after the tree's own sheets).
- Spec: CSS Scoping 1 §3.2 scoped stylesheets, §3.3 shadow cascading; CSSOM 'adoptedStyleSheets', CSSStyleSheet.replace/replaceSync
- Engine files: /home/alex/Downloads/claude/wt/ctbrowser-wpt/ctbrowser/lib/Style/engine.cpp; /home/alex/Downloads/claude/wt/ctbrowser-wpt/ctbrowser/lib/Shell/bindings/stylesheets/sheets.cpp; /home/alex/Downloads/claude/wt/ctbrowser-wpt/ctbrowser/lib/Shell/browser/styles.cpp
- Evidence: css/cssom/CSSStyleSheet-constructable.html 15/32 ('Constructed style sheets can be applied on shadow root' expected rgb(0, 0, 0) got ""); css/cssom/adoptedstylesheets-cascade-order.html 3/13, CSSStyleSheet-constructable-duplicate 2/4, CSSStyleSheet-constructable-invalidation 2/3, CSSStyleSheet-constructable-disallow-import.tentative 3/4, selectorText-modification-restyle-002 2/2, [..]
- Fix: Covered by round8-brief-FLAT.md batch (2): per tree-scope sheet lists (the tree's <style>/<link>, then its adoptedStyleSheets in list order), a rule matches only within its tree except :host/::slotted/::part, and cascade order = tree-scope order per CSS Scoping §3.3. The cssom files above are FLAT's cssom net; the disallow-import (3) row is separate: replaceSync must drop @import rules (CSSOM §6.1 replace()).

### Container query evaluation gaps behind the HARNESS_ERRORs: named containers, cq units against auto-height containers, font-relative style() range queries not re-evaluated (30 subtests today; the 151 files cluster 1 unblocks land here)

- **30 subtests / 16 files**, size L, overlaps FLAT, confidence medium
- Root cause: engine.cpp:551-580 container_holds walks the containers_ chain per rule and asks the layout hook for a size; `@container --foo (...)` must select the NEAREST ancestor whose container-name list contains --foo AND whose container-type answers the axis (CSS Conditional 5 §4.2: name filters first, then type) - query-container-name.html 'match closest named container' answers 'no' (4/5) and -dynamic 2/3; cqh against a size container with auto height must be 0 (§4.4 / CSS Values 4 §6.1.3 - container-units-auto expected 0px got 768px: falls back to the viewport); style() range queries with rem/em/lh are not re-evaluated when the root or container font-size changes (style-range-dynamic 6/7, [..]
- Spec: CSS Conditional 5 §4.2 container-type/-name and 'query container' selection, §4.3 size features, §4.4 style() queries, CSS Values 4 §6.1.3 container-percentage lengths
- Engine files: /home/alex/Downloads/claude/wt/ctbrowser-wpt/ctbrowser/lib/Style/engine.cpp; /home/alex/Downloads/claude/wt/ctbrowser-wpt/ctbrowser/lib/Style/css/media.cpp; /home/alex/Downloads/claude/wt/ctbrowser-wpt/ctbrowser/lib/Style/css/calc/units.cpp; /home/alex/Downloads/claude/wt/ctbrowser-wpt/ctbrowser/lib/Shell/browser/frame.cpp; [..]
- Evidence: css/css-conditional/container-queries/style-range-dynamic.html 6/7 ('After increasing root font-size' expected green got red); css/css-conditional/container-queries/query-container-name.html 4/5 ('match closest named container' expected "yes" got "no"), query-container-name-dynamic.html 2/3; css/css-conditional/container-queries/container-units-auto.html 1/1 ('Resolving cqh against a auto height [..]
- Fix: After cluster 1 lands, re-measure css-conditional and rank; expected shape: (1) container selection: for each rule's container condition, walk chain_styles_ from the parent upward, skip ancestors whose container-name (a space-separated list) lacks the query's name, then require the type for the queried axis (size: both, inline-size: inline only; a style() query needs no type); (2) container-relative units (cqw/cqh/cqi/cqb/cqmin/cqmax) resolve against the selected container's content box from the same hook, 0 for an axis the container does not size; (3) style-range queries: fold the query's lengths with the CONTAINER's font-size/line-height (chain_styles_) and mark the rule set dirty on any font-size change up the chain (the invalidation is what the -dynamic files test); (4) the size loop: layout -> container sizes -> restyle the subtree -> relayout, bounded (CSS Conditional 5 §4.1 'the [..]

### CSSContainerRule.containerName/containerQuery split the prelude on whitespace, so a style()/scroll-state() function is taken as a name (20 HARNESS_ERROR files + container-rule-cssom)

- **25 subtests / 21 files**, size S, overlaps none, confidence high
- Root cause: prototypes.cpp:852-870 `container_part` calls next_component (source.cpp:79-84: skip whitespace, scan to the next whitespace) and treats the first component as the name whenever it does not start with '(' and is not not/and/or. `@container STYLE(--foo: bar){}` therefore yields containerName 'STYLE(--foo:' and containerQuery 'bar)'. cq-testcommon.js's assert_implements_style_container_queries probes exactly `sheet.cssRules[0].containerQuery === "style(--foo: bar)"` -> 'assert_implements: Basic support for style container queries required' on 20 files (the lowercase rewrite at line 862-866 never runs on the right string). CSS Conditional 5 §4.1: <container-name> is a <custom-ident>; a [..]
- Spec: CSS Conditional 5 §4.1 @container syntax (<container-name> = <custom-ident>), §4.5 CSSContainerRule containerName/containerQuery; CSSOM §6.6 serialization
- Engine files: /home/alex/Downloads/claude/wt/ctbrowser-wpt/ctbrowser/lib/Shell/bindings/stylesheets/prototypes.cpp; /home/alex/Downloads/claude/wt/ctbrowser-wpt/ctbrowser/lib/Shell/bindings/stylesheets/source.cpp; /home/alex/Downloads/claude/wt/ctbrowser-wpt/ctbrowser/lib/Style/css/parser.cpp
- Evidence: 20 css/css-conditional/container-queries/* HARNESS_ERROR 'Basic support for style container queries required' (at-container-style-parsing, at-container-style-serialization, custom-property-style-queries, custom-property-style-query-change, display-contents-dynamic-style-queries, registered-color-style-queries, style-container-for-shadow-dom, style-query-change-with-ancestor-zoom, ...); [..]
- Fix: Tokenize the prelude (style::css::tokenize) and set `named` only when the first significant token is an ident token (not a function token, not not/and/or); name = that ident, query = the rest. Serialize the query per CSSOM: collapse whitespace, lowercase the function names `style(`/`scroll-state(`/`anchored(` (line 862-866 already tries), keep `<general-enclosed>` as written. parser.cpp:636-655 has the same split and is correct (token_at + type == ident) - share it by exposing a `split_container_prelude(text) -> {name, query}` in style/css and calling it from both.

### Custom-property substitution joins tokens without the CSS Syntax §9.1 comment table: `var(--t1)var(--t2)` merges `foo` + `()` into `foo()` (19 subtests)

- **19 subtests / 2 files**, size S, overlaps none, confidence high
- Root cause: substitute.cpp:500-507 `substituter::join` inserts `/**/` only when both sides end/begin with a name character (`is_name || '.' || '%'`). CSS Syntax 3 §9.1's table also requires a comment between an ident/at-keyword/hash/dimension/number and a following `(` (function), `-`, `-->`, url(, number/dimension/percentage, and between `#`/`-`/`@` and an ident, etc. serialize-consecutive-tokens.html 16/72 ('Result must have a comment between foo and () got disallowed value "foo()"', '# and bar'), variable-substitution-basic 3/13 ('23px 59px' got '23px59px' - two substitutions separated by whitespace in the source lose it: '"0px" got "20px"' - the opposite bug, a token built from two var() halves [..]
- Spec: CSS Syntax 3 §9.1 Serialization (the consecutive-token table); CSS Variables 1 §3 (substitution is a token-stream operation)
- Engine files: /home/alex/Downloads/claude/wt/ctbrowser-wpt/ctbrowser/lib/Style/css/substitute.cpp
- Evidence: css/css-syntax/serialize-consecutive-tokens.html 16/72; css/css-variables/variable-substitution-basic.html 3/13 ('Multiple variable references in a single property (no spaces)' expected "23px 59px" got "23px59px"; "You can't build up a single token where part of it is provided by a var" expected 0px got 20px)
- Fix: Replace join()'s name-char heuristic with the §9.1 table keyed on the LAST token type of `left` and the FIRST token type of `right` (tokenize each side's boundary: ident/function/url/bad-url/'-'/number/percentage/dimension/CDC/'('/'*'/'%'/'@'/'#'): emit `/**/` for every marked pair; and re-tokenize the substituted value once at the end so `2` + `3px` stays two tokens (the whole-value re-tokenisation css-variables-1 §3 describes). Unit test: the 16 failing pairs from the WPT file.

### Stylesheet byte decoding ignores @charset / BOM / HTTP charset / the referring document's encoding (15 files)

- **15 subtests / 15 files**, size S, overlaps none, confidence high
- Root cause: css/css-syntax/charset/*: a windows-1251 page loading a sheet whose selector is `U+0418` fails 15 of 16 files ('expected hidden got visible') - the fetched CSS bytes are handed to the parser as UTF-8 regardless of the BOM, an `@charset "..."` rule, the HTTP Content-Type charset, the `<link charset>` attribute or the referring document's encoding (CSS Syntax 3 §3.2 'determine the fallback encoding' and 'decode bytes'). wpt-next.md lines 356-357 already name this ('css-syntax 15/40: @charset and byte decoding'). The Encoding Standard label table exists in the DOM half (document.characterSet work, round W).
- Spec: CSS Syntax 3 §3.2 'decode bytes' and 'determine the fallback encoding'; Encoding Standard §4.2 'get an encoding'
- Engine files: /home/alex/Downloads/claude/wt/ctbrowser-wpt/ctbrowser/lib/Shell/browser/resources.cpp; /home/alex/Downloads/claude/wt/ctbrowser-wpt/ctbrowser/lib/Shell/browser/styles.cpp; /home/alex/Downloads/claude/wt/ctbrowser-wpt/ctbrowser/lib/Shell/bindings/resources.cpp
- Evidence: css/css-syntax/charset/page-windows-1251-css-no-decl.html 1/1, page-windows-1251-css-utf8-bom.html, page-windows-1251-css-at-charset-utf16(be).html, page-windows-1251-css-http-bogus.html, ...-charset-attribute-bogus.html, xml-stylesheet-page-windows-1251-charset-attribute-windows-1250.xhtml - 15 files x 1 subtest 'expected "hidden" but got "visible"'
- Fix: In the stylesheet fetch completion: decode with (1) BOM, else (2) the HTTP charset label, else (3) `@charset "x";` in the first 1024 bytes (exact byte pattern, utf-16 labels map to utf-8), else (4) the <link charset> attribute, else (5) the referring document's encoding, else utf-8 - then feed the decoded text to the parser. Reuse the DOM's Encoding-Standard label table and windows-125x decoders (lib/DOM's meta-charset work); constructed sheets and <style> are already text and unaffected.

**Long tail:** matchContainer()/match-container-change-event (42, tentative API - skip); @custom-media (17: at-custom-media-cssom 10, -parsing 7; MQ5 draft); Typed OM computedStyleMap undefined (css-typed-om directory not run at all; in-slice calc-size-typed-om 19, random-computed 19, calc-size-interpolation-expansion 11 - a whole subsystem, L, not started); caretRangeFromPoint undefined / caretPositionFromPoint wrong (cssom 19 across 7 files); revert-rule leftovers after cluster 1 (revert-rule-shadow 10 -> FLAT, revert-rule-cycle 2, revert-rule-important 1); mediaqueries: boolean context of [..]

**Skipped (already fixed at HEAD):** Checked at HEAD b722aa41 and NOT counted as new clusters: (1) container-type/container-name/container in the property table (398de54c, keywords.cpp:64, table_modules.cpp:197-199) - already present at the 9f8da347 baseline, so it is not what the 172 HARNESS_ERRORs are waiting on; the one-argument CSS.supports form (cluster 1) is. (2) revert-rule rollback in the cascade (engine.cpp:1333, keywords.cpp:32) - present at baseline; the revert-rule-* files fail on the CSS.supports probe, not on the rollback (cluster 1), except revert-rule-shadow (FLAT) and -cycle/-important (long tail). (3) [..]

## css-layout — css layout slice: css-flexbox, css-grid, css-position, css-display, css-align, css-box, css-sizing, css-tables, css-overflow, css-writing-modes, css-text, css-fonts, css-inline, css-multicol, css-page, css-break, css-lists, css-logical (from /tmp/w-9f8da347/css.json)

Baseline: 732 files in the slice at 9f8da347: 412 PASS / 316 FAIL / 4 SKIP, 0 TIMEOUT/CRASH/HARNESS_ERROR; subtests 13,729 PASS / 8,362 FAIL. Per suite (PASS/FAIL files, FAIL subtests): css-grid 21/53 (2,096), css-fonts 52/48 (2,006), css-sizing 11/26 (1,085), css-text 65/32 (773), css-box 26/14 (477), css-multicol 25/14 (345), css-position 21/11 (239), css-flexbox 23/11 (236), css-align 43/14 (227), [..]

### CSS.supports refuses every grid value with repeat()/minmax()/fit-content(): the three names are missing from value_functions, and grid-line longhands set via CSSOM read back as `auto`

- **1270 subtests / 27 files**, size S, overlaps none, confidence high
- Root cause: lib/Style/css/properties/grid.cpp models CSS Grid 2 track lists correctly (unittests/unit/css_grammar_values.cpp:125-162 pass for `repeat(auto-fit, [three] minmax(max-content, 6em) [four])`, `span 1 i` -> `span i`, `calc(1.1) -a-`), but scan_tokens (grammar.cpp:352-360) sets `unknown_function` for any function name not in math_functions/value_functions, and value_functions (grammar.cpp:76-104: rgb, url, gradients, transforms, cubic-bezier, steps, counter, rect, format, lab/lch/oklab/oklch, color-mix, light-dark, alpha, contrast-color, color-layers, device-cmyk) has NO repeat/minmax/fit-content. supports_declaration (declaration.cpp:512) returns `checked.valid && [..]
- Spec: CSS Grid 2 §7.2 (<track-list>, repeat()), §7.2.3 (minmax(), fit-content()), §8.3 (<grid-line>); CSS Conditional 3 §6.1 CSS.supports(property, value)
- Engine files: ctbrowser/lib/Style/css/properties/grammar.cpp (value_functions 76-104, scan_tokens 352-360); ctbrowser/lib/Style/css/properties/declaration.cpp (check_declaration 385-391, supports_declaration 499-512); ctbrowser/lib/Style/css/properties/grid.cpp (track_list 417-450, grid_line 330-400, computed_grid 626-641); ctbrowser/lib/Shell/bindings/computed_style/entries.cpp (grid_property 959-1040)
- Evidence: css/css-grid/animation/grid-template-columns-interpolation.html 396 'should be supported' subtests (from [1fr repeat(2, 2fr 30px) 1fr], [10px repeat(auto-fill, minmax(25px, 1fr)) 10px], [fit-content(10px)], [minmax(10px, 1fr) ...]); css/css-grid/animation/grid-template-rows-interpolation.html 396; css/css-grid/animation/grid-template-{columns,rows}-composition.html 58+58; [..]
- Fix: (1) Add "repeat", "minmax", "fit-content" to value_functions in grammar.cpp:76 (bump the std::array size to 44) - one line, unblocks ~1,100 subtests across the four animation files and every *-computed grid file; then the interpolation files fall to cluster 'grid track-list interpolation'. (2) Run css/css-grid/parsing/grid-area-computed.html under ctdrive and dump `el.style.cssText` + `getComputedStyle(el).gridRowStart` after `el.style['grid-row-start']='AZ'`; unit tests prove grid_line is right, so trace set_declaration -> engine::resolve pass two (engine.cpp:2043-2090, does check_declaration/expand_shorthand drop it?) -> entries.cpp:959 computed_grid; fix the one hop that loses the value. Also add `repeat(`/`minmax(` unit tests through supports_declaration, not only check_declaration, so the two answers cannot drift again.

### Animated values never reach layout or the cascade: width/height (used value from the box) and font-size (at.box->font_size) ignore every animation and transition

- **794 subtests / 8 files**, size M, overlaps none, confidence high
- Root cause: animated_values() is consumed in exactly one place, entries.cpp:324 inside getComputedStyle, where it overlays the `declared` lambda (entries.cpp:349-357). But width/height resolve as USED values read from the layout box (entries.cpp:559-577, `at.box`), font-size is `at.box->font_size` (entries.cpp:185), and layout (browser::run_layout, frame.cpp:273, box_builder over `resolved_`) never sees animated text because resolve_styles (frame.cpp:244-270) hands the cascade's map to layout untouched and only afterwards to update_css_animations. So `width` from [0px] to [100px] reads 10px (the stylesheet value) at every progress for CSS Animations/Web Animations, transitions read the end value [..]
- Spec: CSS Cascade 4 §6.3 (animation origin: 'CSS Animations and CSS Transitions generate declarations in the animation origin'); Web Animations 1 §5.4.3 (the effect value applies to the computed value the [..]
- Engine files: ctbrowser/lib/Shell/bindings/computed_style/entries.cpp (185, 296-357, 559-577); ctbrowser/lib/Shell/browser/frame.cpp (resolve_styles 244-270, run_layout 272+); ctbrowser/lib/Shell/bindings/animations.cpp (animated_values 318-470); ctbrowser/lib/Shell/bindings/animations/css.cpp (update_css_animations 300-360); ctbrowser/lib/Style/engine.cpp (resolve 1231, pass two 2043-2100)
- Evidence: css/css-sizing/animation/width-interpolation.html 252/456 (`CSS Animations: property <width> from [0px] to [100px] at (-0.3) :: expected "0px" but got "10px"` for every from/to pair and every t; transitions `[0px]->[100px]` got 100px at every t); css/css-sizing/animation/height-interpolation.html 188/438; css/css-fonts/animations/font-size-interpolation-001.html 126/140 (`from [4px] to [14px] at [..]
- Fix: Give animations a cascade origin instead of a getComputedStyle overlay: in browser::resolve_styles, after bindings_->update_css_animations(txn, before, resolved_), ask bindings_->animated_values(node, font_size, underlying) for every node with a record (animations_on) and build a derived computed_style for that node whose declarations are the cascade's plus the animated physical longhands (the same mapping entries.cpp:330-348 does, moved into a helper), replacing resolved_[key]; mark layout dirty when the set of animated nodes is non-empty (the tick already re-resolves). Then delete the overlay in entries.cpp (or keep it only for the `underlying` callback) so width/height/font-size are read off a box laid out with the animated text. A cheaper interim (S) that fixes only the corpus: in entries.cpp, when `animated` names width/height/font-size, resolve from the animated text (parse_length [..]

### Level-4 text/inline/multicol/overflow properties and values absent from the property table (or typed as a scalar kind that refuses their multi-value grammar)

- **668 subtests / 51 files**, size M, overlaps none, confidence high
- Root cause: find_property returns nullptr (so CSS.supports is false and getComputedStyle does not publish) for text-spacing, text-fit, text-group-align, word-space-transform, column-wrap, scroll-axis-lock, scroll-buttons/scroll-markers (css-overflow-5), `size` (css-sizing-4 shorthand), and the display values grid-lanes/inline-grid-lanes; and rows that exist have a scalar kind that refuses the Level-4 grammar: text-indent is k::length_percentage so `0px each-line` / `50px hanging` is refused (table.cpp:244), hyphenate-limit-chars is k::integer so `5 1 2` and `auto 1 2` are refused (table.cpp:305), initial-letter's `<number> <integer>?` (grammar.cpp integer_slots_ok only), line-clamp/max-lines, [..]
- Spec: CSS Text 4 §2.2 (text-indent hanging/each-line), §5.3 (hyphenate-limit-chars), §8.3 text-spacing, §3 white-space longhands; CSS Inline 3 §3.1 initial-letter, §5 vertical-align; CSS Box 4 §3.4 [..]
- Engine files: ctbrowser/lib/Style/css/properties/table.cpp (text-indent 244, hyphenate-limit-chars 305, vertical-align 270); ctbrowser/lib/Style/css/properties/table_modules.cpp (text-wrap 228-233, margin-trim, overflow-clip-margin, display list); ctbrowser/lib/Style/css/properties/keywords.cpp; ctbrowser/lib/Style/css/properties/grammar.cpp (integer_slots_ok 389-433); [..]
- Evidence: css/css-text/animations/text-indent-interpolation.html 132 + text-indent-composition 24 ('from' value should be supported: `[0px each-line]`, `[50px hanging]`); css/css-text/animations/hyphen-limit-chars-interpolation.html 64 (`[5 1 2]`, `[auto 1 2]`) + parsing/hyphenate-limit-chars-{computed,valid} 9+9; css/css-inline/animation/initial-letter-no-interpolation.html 42, [..]
- Fix: Table rows first (each is one line in table_modules.cpp with the spec's keywords/initial/inherited): text-spacing (`normal | none | auto | [trim-start||space-first||...]`), text-fit, text-group-align, word-space-transform, column-wrap, scroll-axis-lock, `size` as a shorthand of width/height, display's grid-lanes pair. Then the grammars that a scalar kind cannot express: make text-indent freeform with a small matcher `<length-percentage> && hanging? && each-line?` (canonical order: length, hanging, each-line), hyphenate-limit-chars `[auto | <integer>]{1,3}` (computed keeps `auto` for the omitted slots: `5` -> `5 auto`), initial-letter `normal | <number> [<integer> | drop | raise]?`, vertical-align per css-inline-3 §5 (`0` and `baseline` fold to `baseline`), margin-trim (`none | block | inline | [block-start||inline-start||block-end||inline-end]` with the pair-to-axis fold and duplicate [..]

### System font keywords (`font: caption | icon | menu | message-box | small-caption | status-bar`) are kept as an opaque whole and set no longhand

- **366 subtests / 2 files**, size S, overlaps none, confidence high
- Root cause: split_font (shorthands.cpp:378-385) returns split::whole for a single system-font keyword, so the cascade stores `font: caption` and font-style/variant/weight/stretch/size/line-height/family keep their previous values; getComputedStyle's font fold answers "" and an animation from an explicit font to `caption` interpolates each longhand against the OLD value (weight 400, size 16px, stretch 100%).
- Spec: CSS Fonts 4 §3.1.1 (system fonts: 'must be treated as a shorthand... set all the subproperties')
- Engine files: ctbrowser/lib/Style/css/properties/shorthands.cpp (split_font 374-430, the `system` keyword set 383-385); ctbrowser/lib/Style/engine.cpp (font pre-pass 1632-1699 reads the size a `font` carries)
- Evidence: css/css-fonts/animations/system-fonts.html 360/588 (`property <font> from [italic 100 small-caps ultra-expanded 100px / 100px Ahem] to [caption] at (0) :: expected "100 " but got "400 "`, `expected "100px " but got "16px "`, `expected "small-caps" but got "normal"`); css/css-fonts/parsing/font-computed.html 6 (`caption should be a supported system font. :: font should be set got disallowed value [..]
- Fix: In split_font, when the single word is a system keyword, emit the seven longhands from a fixed UA table instead of split::whole: style normal, variant normal, weight 400, stretch 100%, size 13.3333px (caption/icon/menu/message-box/status-bar) or 11px (small-caption), line-height normal, family sans-serif (any consistent choice passes: the harness derives its expectation from getComputedStyle of the same engine). Keep the read-back rule that a `font` whose longhands equal a system entry may serialise as the keyword only for el.style, not for the computed style (CSS Fonts 4 §3.1: the computed style is the longhands).

### `inherit` in a keyframe stays the literal word: the sampler has no way to ask for the parent's computed value

- **346 subtests / 33 files**, size S, overlaps round7, confidence high
- Root cause: animations.cpp:359-372 resolves `initial` and non-inherited `unset` to the table's initial (round seven) but carries an explicit ponytail note: "`inherit` needs the parent's computed text, which the underlying callback cannot ask for yet". The `underlying` callback built at entries.cpp:324 only reads `declared_on(at.chain.front(), property)`; the parent is `at.chain[1]` one line away. A keyframe `from { widows: inherit }` therefore emits "inherit" (or, for an inherited property with `unset`, the wrong value) and every pair with it is non-interpolable.
- Spec: CSS Animations 1 §3 ('the values of the keyframes... are computed values'); Web Animations 1 §5.4.3; CSS Cascade 4 §7.3 (inherit/unset)
- Engine files: ctbrowser/lib/Shell/bindings/animations.cpp (359-372, 381-386); ctbrowser/lib/Shell/bindings/computed_style/entries.cpp (323-327 the underlying callback); ctbrowser/include/ctbrowser/shell/bindings.hpp (animated_values signature)
- Evidence: 33 animation files, 14 subtests each in e.g. css/css-break/animation/widows-interpolation.html (`CSS Animations: property <widows> from [inherit] to [20] at (-3) :: expected "60" but got "inherit"`), css/css-fonts/animations/font-{palette,stretch,style}-interpolation.html, css/css-multicol/animation/column-{width,count}-interpolation.html (`from [inherit] to [20] at (0) :: expected "30" but got [..]
- Fix: Give animated_values a second callback `parent(property)` (entries.cpp passes `declared_on(at.chain.size() >= 2 ? at.chain[1] : node_id{}, property)`, css.cpp's before-change sampler passes the parent's previous style) and in the keyframe loop map `inherit`, and `unset` when known->inherited, to `parent(property)` falling back to known->initial when empty. Same three lines make Web Animations' `{width: 'inherit'}` right. Also apply the mapping AFTER shorthand expansion (css.cpp:633-650 expands `margin: initial` into longhands - check each longhand's keyword hits this branch).

### `grid` and `grid-template` have no shorthand split or fold: stored whole, longhands empty, invalid forms accepted

- **337 subtests / 11 files**, size M, overlaps none, confidence high
- Root cause: shorthands.cpp's shape table (120-160) has grid-row/grid-column/grid-area (shape::grid_lines) but nothing for `grid` or `grid-template`; table_modules.cpp:72-73 marks both `shorthand` so getComputedStyle enumerates them, and check_declaration reaches match_grid only for `grid-*` longhands (declaration.cpp:385, match_grid returns false for the shorthands), so `grid: none none`, `grid-template: 10px`, `grid: 20%` are stored as valid, `grid: none` sets no grid-auto-* / grid-template-areas longhand, and the read-back is the author's bytes (`auto-flow auto / 100px 100px`) instead of the canonical shortest form (`none / 100px 100px`).
- Spec: CSS Grid 2 §7.4 (grid-template), §7.8 (grid), §7.2/7.3; CSSOM §6.7.2 serialize a CSS declaration block (shorthand shortest serialization)
- Engine files: ctbrowser/lib/Style/css/properties/shorthands.cpp (shape table 120-160, split dispatch 640-680, fold dispatch 800-830); ctbrowser/lib/Style/css/properties/grid.cpp (track_list 417, template_areas 452, line_names 208 - the pieces a grid-template split reuses); ctbrowser/lib/Style/css/properties/table_modules.cpp (72-73)
- Evidence: css/css-grid/parsing/grid-shorthand-serialization.html 89/141 (`grid: auto-flow auto / 100px 100px should set grid :: expected "none / 100px 100px" but got "auto-flow auto / 100px 100px"`, `should set grid-auto-flow :: expected "row" but got ""`); css/css-grid/parsing/grid-template-shorthand-invalid.html 66/66 (`"auto" should not set the property value`, `"none none"`, `"10px"`), [..]
- Fix: Add shape::grid_template and shape::grid to shorthands.cpp. split_grid_template: `none` -> rows none / columns none / areas none; `<rows> / <columns>` via grid.cpp track_list twice; the areas form `[<line-names>? <string> <track-size>? <line-names>?]+ [/ <explicit-track-list>]?` -> rows = the sizes (auto when omitted) with merged line names, areas = the strings, columns = the explicit list or none. split_grid: `<'grid-template'>` (auto-* reset to auto/row) | `<rows> / [auto-flow && dense?] <'grid-auto-columns'>?` | `[auto-flow && dense?] <'grid-auto-rows'>? / <columns>`; refuse anything else. fold_grid_template/fold_grid produce the shortest form (CSS Grid 2 §7.4/§7.8 + CSSOM §6.7.2: `none` when everything is initial, `none / 100px 100px`, "" when the longhands cannot be expressed - e.g. grid-auto-flow column with explicit rows AND columns). Wire computed read-back through the existing [..]

### Keyword computed values are not absolutised at cascade time: font-weight normal/bold, font-stretch/font-width keywords, font-style oblique angles, word-/letter-spacing normal, column-rule-width thin/medium/thick

- **319 subtests / 11 files**, size M, overlaps none, confidence high
- Root cause: The cascade stores these keywords as text (table.cpp:214 font-weight k::number keywords 'normal bold bolder lighter' with no mapping, 216 font-stretch freeform, 240-241 letter/word-spacing 'normal', table_modules.cpp:101 column-rule-width 'thin medium thick') and neither engine.cpp's pass two nor entries.cpp maps them, so getComputedStyle answers `normal`/`condensed`/`medium` where the spec's computed value is `400`/`75%`/`3px`, and easing.cpp's numeric_of sees a keyword on one side and declares the pair discrete: `font-stretch` [condensed]->[expanded] flips, `[normal]`->`[oblique 10deg]` flips, transitions from `word-spacing: initial` (`normal`) never start, `column-rule-width: initial` [..]
- Spec: CSS Fonts 4 §2.2 (font-weight computed value: a number), §2.4 (font-width computed: a percentage), §2.5 (font-style: oblique angle, 0deg is normal, clamped to ±90deg); CSS Text 4 §8.2/8.3 [..]
- Engine files: ctbrowser/lib/Style/engine.cpp (pass two 2043-2100, beside the font-size pre-pass 1632-1699); ctbrowser/lib/Style/css/properties/table.cpp (214-226, 240-241); ctbrowser/lib/Style/css/properties/table_modules.cpp (101); ctbrowser/lib/Style/easing.cpp (computed_shape 417, numeric_of, numeric_text 257-279); ctbrowser/lib/Shell/bindings/computed_style/entries.cpp (900-940 the length fold)
- Evidence: css/css-fonts/animations/font-stretch-interpolation.html 114 (`from [condensed] to [expanded] at (0) :: expected "75 %" but got "condensed"`; `from [100%] to [200%] at (-2) :: expected "0 %" but got "- 100 %"`); css/css-fonts/animations/font-style-interpolation.html 64 (`from [normal] to [oblique 10deg] at (0.3) :: expected "oblique 3deg" but got "oblique 10deg"`); [..]
- Fix: One table-driven fold in engine.cpp pass two (where the relative lengths already fold to px): font-weight normal->400, bold->700, bolder/lighter relative to the parent's computed weight per CSS Fonts 4 §2.2's table, clamp [1,1000]; font-stretch/font-width ultra-condensed 50%, extra-condensed 62.5%, condensed 75%, semi-condensed 87.5%, normal 100%, semi-expanded 112.5%, expanded 125%, extra-expanded 150%, ultra-expanded 200% (reverse map only in the `font` shorthand fold, which 7a4c7feb already does); font-style `oblique` -> `oblique 14deg`, `oblique 0deg` -> `normal`, angle to deg and clamp ±90; letter-spacing/word-spacing normal -> 0px; the line-width keywords thin/medium/thick -> 1px/3px/5px for column-rule-width, outline-width and border-*-width (check which of those entries.cpp already maps). Mark font-stretch nonnegative. easing.cpp then interpolates them as [..]

### grid-template-columns/rows have no interpolation shape: track lists animate discretely and composition ignores the underlying list

- **268 subtests / 4 files**, size M, overlaps none, confidence high
- Root cause: easing.cpp's computed_shape/interpolate_pair (417, 811, 1104) know numerics, colours, transforms, filters, shadows and lists of those; there is no track-list shape and no `fr` unit anywhere in the file (rg 'fr\b' matches nothing). CSS Grid 2 says a track list animates by computed value when both lists have the same length and the same line names and every track size pair is numeric (length-percentage or flex), else discrete. So `1fr 1fr 1fr` -> `2fr auto 2fr` is a flip (expected `1.4fr 1fr 1.4fr` at 0.4: per-track, with `1fr`->`auto` discrete at 50%), and composition add of `50px 10%` + `100px 100px` gives `150px calc(10% + 100px)`.
- Spec: CSS Grid 2 §7.2 grid-template-columns 'Animation type: if the list lengths match, by computed value; otherwise discrete' (and the line-names/repeat() conditions in the same section); CSS Values 4 [..]
- Engine files: ctbrowser/lib/Style/easing.cpp (computed_shape 417-520, interpolate_pair 811/1104, add_pair 1211, numeric_of 192-230, numeric_text 257-279); ctbrowser/lib/Style/css/properties/grid.cpp (track_list, line_names - reuse to tokenise the list into names and sizes)
- Evidence: css/css-grid/animation/grid-template-columns-interpolation.html 46 (`from [1fr 1fr 1fr] to [2fr auto 2fr] at (0.4) :: expected "1.4fr 1fr 1.4fr" but got "2fr auto 2fr"`; `from [1fr [a b] 1fr [d] 1fr] to [2fr [c] auto 2fr] at (0.6) :: expected "1.6fr [c] auto 1.6fr"`) + 396 more once CSS.supports accepts repeat()/minmax(); css/css-grid/animation/grid-template-rows-interpolation.html 46 (+396); [..]
- Fix: Add a track_list shape in easing.cpp: split each side with grid.cpp's cursor into a vector of {line-names, size}; interpolable iff same length, identical name lists, and each size pair is either both `<length-percentage>` (numeric_of), both `<flex>` (treat `fr` as a third numeric type, serialise `Nfr`), or both minmax()/fit-content() with pairwise-interpolable arguments; repeat(N, ...) only against repeat(N, ...) with equal N; anything else discrete (flip at 50%). Emit numeric_text per track with grid's canonical spacing. add_pair for the same shape sums track by track (percent mix as calc). Depends on the value_functions fix for the repeat()/minmax() cases.

### Resolved value of grid-template-* on a grid container must be the used track list; there is no grid track-sizing algorithm to produce one

- **159 subtests / 8 files**, size L, overlaps none, confidence high
- Root cause: CSSOM's resolved-value special case: for a grid container, getComputedStyle(grid-template-columns) is the used value - every track in px, repeat() expanded (auto-fill/auto-fit counted against the container width), implicit tracks included, percentages and minmax()/auto/min-content resolved. grid.cpp:626-641 computed_grid absolutises lengths only (the commit 6470eba3 says 'a grid container's used track sizes need a layout this engine does not do') and entries.cpp:131-134/304-309 records that `display: grid` lays out as a block with no grid box kind. So `none` on a container with content reads `none` instead of `300px`, `20%` reads `20%` instead of `60px`, `minmax(min-content, 15px) auto` [..]
- Spec: CSS Grid 2 §12 (grid sizing algorithm), §7.2.3.2 (repeat-to-fill), §8.1 implicit grid; CSSOM §resolved-value ('grid-template-columns/rows: the resolved value is the used value' when the element [..]
- Engine files: ctbrowser/lib/Layout/** (no grid algorithm exists; box_builder/algorithm.cpp lay `display: grid` out as block); ctbrowser/lib/Style/css/properties/grid.cpp (computed_grid 626-641); ctbrowser/lib/Shell/bindings/computed_style/entries.cpp (grid_property 959-1040, at.grid_item 304-309)
- Evidence: css/css-grid/parsing/grid-content-sized-columns-resolution.html 42/42 (`Element 'gridMinContentFixedAndAuto' grid-template-columns value computes to '15px 75px' :: got "minmax(min-content, 15px) auto"`); css/css-grid/parsing/grid-columns-rows-get-set-multiple.html 27/40 (`expected "400px 800px" but got "50% 100%"`); css/css-grid/parsing/grid-template-{columns,rows}-computed-implicit-track.html [..]
- Fix: This is the grid layout algorithm (CSS Grid 2 §12: initialise track sizes, resolve intrinsic sizes from items' min/max-content contributions, maximise, expand flexible tracks, stretch auto) plus §7.2.3 auto-repeat counting and §8.1 implicit track creation, then entries.cpp asking the box for its used tracks. Not a parsing fix; belongs to whoever builds grid layout, and the reftests that would measure it are not run (run-wpt.py skips rel=match). Until then these 159 stay red; the parsing-only wins are clusters 1, 6 and 8.

### Font property grammars kept freeform: font-variant shorthand, font-variant-alternates, font-variation-settings, font-feature-settings, font-synthesis, font-family/font invalid forms

- **148 subtests / 18 files**, size M, overlaps none, confidence high
- Root cause: table.cpp:213-226 and table_modules.cpp:205-218 type these as k::freeform with no matcher in keywords.cpp/grammar.cpp, so any token sequence is accepted and serialised as written: `font-variant: normal none`/`small-caps normal` (normal/none are exclusive), `swash(several,different,aliases)` (one ident), `font-variation-settings: 700` / `wght 700` (must be `<string> <number>`), `font-family` with a bare number, `font: ...` invalid forms; and font-synthesis (a shorthand of the four -weight/-style/-small-caps/-position longhands, table_modules.cpp:208) has no split/fold so its computed value is "" and `oblique-only` is refused.
- Spec: CSS Fonts 4 §2.1 font-family, §3.1 font, §6.1 font-variant, §6.8 font-variant-alternates, §7.1 font-feature-settings, §11.1 font-variation-settings, §2.7 font-synthesis, §2.6 font-size-adjust
- Engine files: ctbrowser/lib/Style/css/properties/table.cpp (213-226); ctbrowser/lib/Style/css/properties/table_modules.cpp (205-218); ctbrowser/lib/Style/css/properties/keywords.cpp; ctbrowser/lib/Style/css/properties/grammar.cpp; ctbrowser/lib/Style/css/properties/shorthands.cpp (font-synthesis split/fold; font-variant split); ctbrowser/lib/Style/css/properties/font_family.cpp
- Evidence: css/css-fonts/parsing/font-synthesis-computed.html 21/21 (`'none' :: expected "none" but got ""`), font-synthesis-valid 9/23; css/css-fonts/parsing/font-variant-invalid.html 21/21 (`"normal none" should not set the property value`), font-variant-alternates-invalid 15/15, font-variant-{valid,serialization} 2+1; css/css-fonts/parsing/font-variation-settings-invalid.html 18/18 (`"700"`, `"wght [..]
- Fix: One matcher per grammar, wired like match_alignment in check_declaration: font-variation-settings / font-feature-settings `normal | [<string> <number|integer>?]#` (string exactly 4 ASCII chars for feature tags; serialise `"wght" 700`); font-variant-alternates `normal | [stylistic(<ident>) || historical-forms || styleset(<ident>#) || character-variant(<ident>#) || swash(<ident>) || ornaments(<ident>) || annotation(<ident>)]`; font-variant as a real shorthand split over caps/numeric/ligatures/east-asian/alternates/position/emoji with `normal|none` exclusive (CSS Fonts 4 §6.1); font-synthesis split `none | [weight || style || small-caps || position]` into the four longhands and a fold back (computed of initial is `weight style small-caps position`); font-family per §2.1 (unquoted idents cannot start with a number, generic families lower-cased, `""` invalid). font-size-adjust `from-font` [..]

### @font-face descriptor grammars (src tech()/format()/local(), font-width, metric overrides) and the @font-palette-values rule are not modelled

- **134 subtests / 10 files**, size M, overlaps none, confidence high
- Root cause: The @font-face src list is stored without validating its modifiers: `url("foo.ttf") tech()` and `format()` with no argument, `local(A) dummy()`, `tech(color-COLRv0)` must be lower-cased/keyword-checked (rules.cpp/source.cpp keep the author's text), the font-width descriptor (`auto | <font-width-absolute>{1,2}`) is refused, and @font-palette-values is parsed as a generic CSSRule with no CSSFontPaletteValuesRule interface (name, fontFamily, basePalette, overrideColors), so `rule.type`/`constructor.name` and every descriptor assertion fail; invalid @font-palette-values blocks are also kept instead of dropped (expected 25 rules, got 28).
- Spec: CSS Fonts 4 §4.3 (src descriptor, format(), tech()), §4.6 font-width descriptor, §4.10 metric overrides; CSS Fonts 4 §10.3 @font-palette-values and §10.4 CSSFontPaletteValuesRule
- Engine files: ctbrowser/lib/Shell/bindings/stylesheets/rules.cpp; ctbrowser/lib/Shell/bindings/stylesheets/source.cpp (font-face descriptors ~340-360); ctbrowser/lib/Shell/bindings/stylesheets/prototypes.cpp (font-width descriptor mention); ctbrowser/lib/Style/css/parser.cpp (at-rule descriptors); ctbrowser/lib/Style/css/properties/urange.cpp (the one descriptor with a grammar today - the pattern to copy)
- Evidence: css/css-fonts/parsing/font-palette-values-invalid.html 26/27 (`parsing @font-palette-values :: expected 25 but got 28`), font-palette-values-valid 24/36 (`expected "CSSFontPaletteValuesRule" but got "CSSRule"`), font-palette-invalid 4; css/css-fonts/parsing/font-face-src-tech.html 25/39 (`src: url("foo.ttf") tech() is invalid :: expected false but got true`, `tech(color-COLRv0) :: expected [..]
- Fix: Give @font-face's src a grammar in source.cpp beside unicode-range: `[ <url> [format(<font-format>)]? [tech(<font-tech>#)]? | local(<family-name>) ]#`, dropping an item whose modifiers are malformed (the list keeps the valid items, CSS Fonts 4 §4.3), lower-casing format/tech keywords, refusing unknown function names in the item; add font-width/font-stretch (`auto | <percentage>{1,2} | keyword{1,2}`), ascent-override/descent-override/line-gap-override (`normal | <percentage>`), size-adjust. Add @font-palette-values to the parser's at-rule table with its four descriptors (font-family, base-palette `light|dark|<integer>`, override-colors `[<integer> <color>]#`) and a CSSFontPaletteValuesRule prototype (type 0 in CSSRule constants but a distinct constructor name) in prototypes.cpp; drop a block whose prelude is not a <dashed-ident>.

### Property ranges only clamp at zero: widows/orphans/column-count need >=1, font-size-adjust and font-stretch have no range at all

- **118 subtests / 7 files**, size S, overlaps none, confidence high
- Root cause: numeric_text (easing.cpp:257-279) floors at 0 only when the table row says `nonnegative` and, for font-weight, hard-codes [1,1000] with a ponytail note 'give the table a range when a second one animates'. font-size-adjust (table.cpp:225) and font-stretch (216) are freeform rows without the flag, so an extrapolated `-1` / `-100%` is written; widows/orphans (298-299) and column-count (table_modules.cpp:97) are `<integer [1,∞]>` but only the zero floor applies, so `at (-3)` gives 0 where 1 is expected.
- Spec: CSS Break 3 §3.3 (orphans/widows: <integer [1,∞]>); CSS Multicol 1 §3.2 (column-count <integer [1,∞]>); CSS Fonts 4 §2.4 (font-width <percentage [0,∞]>), §2.6 (font-size-adjust <number [0,∞]>); CSS [..]
- Engine files: ctbrowser/lib/Style/easing.cpp (numeric_text 257-279); ctbrowser/lib/Style/css/properties/table.cpp (216, 225, 298-299); ctbrowser/lib/Style/css/properties/table_modules.cpp (97); ctbrowser/include/ctbrowser/style/css/properties.hpp (property_syntax 73-93)
- Evidence: css/css-fonts/animations/font-size-adjust-interpolation.html 40 (`from neutral to [2] at (-2) :: expected "0" but got "- 1"`) and -composition (`add [0.0] to add [2.0] at (-0.5) :: expected "0" but got "- 1"`); css/css-break/animation/widows-interpolation.html 34 (`from [10] to [20] at (-3) :: expected "1" but got "0"`), orphans-interpolation 10; [..]
- Fix: Replace the bool with a `float minimum` (default -inf; 0 for today's nonnegative rows; 1 for widows, orphans, column-count, font-weight's [1,1000] as min/max) in property_syntax and clamp in numeric_text after rounding; retype font-size-adjust as number (keywords `none`, plus the `<font-metric>? <number>` two-token form handled in the font grammars cluster) and font-stretch as percentage, both minimum 0. Keep the `!out.has_percent` guard for lengths with an unresolved percentage.

### The engine's hand-kept inherits() list (25 names) gates inheritance and `unset`, so every other inherited property neither inherits nor resolves `unset` to the parent

- **114 subtests / 17 files**, size S, overlaps none, confidence high
- Root cause: engine.hpp:353-374 `inherits()` is a fixed list (border-collapse ... writing-mode plus the three white-space longhands) that disagrees with the table's `inherited` flag (properties.hpp:80). It is consulted for `unset`/`revert` (engine.cpp:1352-1361: the declaration is erased and, when !inherits, the initial shows) and for building the inherited half (engine.cpp:2137-2143), so font-kerning, font-variant-*, font-feature/variation-settings, hyphens, line-break, overflow-wrap, word-break, tab-size, text-align-last, text-justify, text-orientation, text-combine-upright, orphans, widows, list-style-image, dominant-baseline, block-ellipsis... never reach a child, and `unset`/`inherit` on them in a [..]
- Spec: CSS Cascade 4 §7.1-7.3 (inherit, unset: 'if the property is inherited... acts as inherit'); each property's 'Inherited: yes' line (CSS Fonts 4, CSS Text 4, CSS Break 3, CSS Writing Modes 4)
- Engine files: ctbrowser/include/ctbrowser/style/engine.hpp (inherits 353-374); ctbrowser/lib/Style/engine.cpp (1350-1361, 2137-2143); ctbrowser/lib/Style/css/properties/table.cpp and table_modules.cpp (the `inherited` column)
- Evidence: css/css-fonts/inheritance.html 16 (`Property font-kerning inherits :: expected "none" but got "auto"`, font-variant-caps expected small-caps got normal, font-feature-settings, font-variation-settings, font-size-adjust, font-stretch); css/css-text/inheritance.html 15 (`hyphens inherits :: expected "none" but got "manual"`, line-break, overflow-wrap, tab-size, text-align-last, word-break, [..]
- Fix: Make inherits() answer from the table: `if (const auto * p = css::find_property(property)) return p->inherited;` after the `--` branch, keeping the white-space longhand note only if their rows lack the flag (they have it: table_modules.cpp:228-233). Audit the table column against the specs for the 25 names (font-size stays inherited as px, the pre-pass already guarantees that). The cost is a larger inherited half per element for pages that set these; measure bench_style before and after (a shared inherited block is the existing mitigation). Then the `unset`-in-transition and `inherit`-keyframe cases behave without further work in css.cpp.

### border-spacing computes to one length instead of the pair, so its interpolation and serialisation are one-valued

- **72 subtests / 1 files**, size S, overlaps none, confidence high
- Root cause: table.cpp:175 `{"border-spacing", k::freeform, "", "0px", true, false}`: the cascade keeps `10px` as written where CSS 2 defines the computed value as two absolute lengths (`10px 10px`), so getComputedStyle reads `7px` where `7px 7px` is expected and easing.cpp interpolates a scalar against a pair only by accident.
- Spec: CSS 2 §17.6.1 border-spacing ('Computed value: two absolute lengths'); CSS Tables 3 §2.2
- Engine files: ctbrowser/lib/Style/css/properties/table.cpp (175); ctbrowser/lib/Style/engine.cpp (pass two length fold 2060-2100); ctbrowser/lib/Style/easing.cpp (list-of-numerics shape, numeric_of)
- Evidence: css/css-tables/animations/border-spacing-interpolation.html 72/120 (`from neutral to [20px] at (0) :: expected "10px 10px" but got "10px"`), css/css-tables/parsing/border-spacing-computed.html 3, border-spacing-invalid 4
- Fix: A tiny grammar `<length [0,∞]>{1,2}` (refuse negatives and a third value) that canonicalises one value to `X X` at declaration time, so computed value and animation shape are always two lengths; interpolate as a two-numeric list (the padded-list path shadows/filters use, or two numeric_of calls) and serialise `a b`. Also drop the second value when equal only in el.style serialisation (CSSOM shortest form), not in the computed value.

**Long tail:** 513 failing subtests in 82 files outside the clusters: font-palette interpolation needs palette-mix(in oklab, light, dark 30%) (38, css-fonts/animations/font-palette-interpolation); list-style-image url()/gradient cross-fade interpolation (36); hyphenate-limit-chars `auto` slots in interpolation (32); column-rule-color transitions from `initial` = currentcolor (28); content-invalid counter(foo, none|unset) must be refused (24, css-lists/parsing/content-invalid); inheritance-initial oddities (21: block-size/inline-size initial in a sized container, display initial inline on the test's element, [..]

**Skipped (already fixed at HEAD):** Already addressed at HEAD (b722aa41) by round seven, counted from the baseline JSON but NOT claimed above - re-measure before assuming: (1) Composition add/accumulate, 1,190 subtests / 27 files (all the *-composition.html files except width/height/font-size and grid): animations.cpp:404-415 now runs style::composite_text (easing.cpp:1305-1387, commit 7f0f15ee '/the value types interpolate and add in Style') against the underlying value; the baseline had `text = points[start].text` with no composite at all (git diff 9f8da347..HEAD). (2) `initial`/`unset` in a keyframe, 638 / 39 (`from [..]

## css-visual — css visual + selectors: css-backgrounds, css-images, css-masking, css-transforms, css-transitions, css-animations, css-pseudo, css-scoping, css-shadow-parts, selectors, css-ui, css-color-adjust, css-color (post round seven), css-fonts, css-content, css-counter-styles, web-animations

Baseline: From /tmp/w-9f8da347/css.json filtered to the slice: 1,412 files, 390 fully passing (PASS with 0 failing subtests), subtests 28,015 PASS / 18,599 FAIL. Per suite (files / fully-passing / subtests pass / fail): css-backgrounds 152/52/3699/2346; css-images 31/22/3017/199; css-masking 49/20/1199/4884; css-transforms 93/25/1592/3743; css-transitions 30/12/551/358; css-animations 43/24/360/230; [..]

### <basic-shape> does not exist: clip-path / object-view-box refused by CSS.supports, no grammar, no interpolation

- **2858 subtests / 12 files**, size L, overlaps none, confidence high
- Root cause: circle(), ellipse(), inset(), polygon(), xywh(), rect(), shape(), path() are not in the value-function allow-list at ctbrowser/lib/Style/css/properties/grammar.cpp:76-116 (`value_functions`), so scan_tokens sets unknown_function (grammar.cpp:356-358) and supports_declaration (ctbrowser/lib/Style/css/properties/declaration.cpp:497-512) answers false - interpolation-testcommon.js's `assert_true(CSS.supports(property, from), "'from' value should be supported")` refuses every subtest before it runs. clip-path is `k::freeform` (ctbrowser/lib/Style/css/properties/table_modules.cpp:53) with no shape grammar, so clip-path-invalid accepts `auto`, `inset()`, `inset(1% 2% 3% 4% 5%)`, and [..]
- Spec: CSS Shapes 1 §5 (<basic-shape> functions, §5.1.2 computed value of rect/xywh as inset), §6 (interpolation); CSS Shapes 2 §3 shape(); CSS Masking 1 §7.1 clip-path, §7.1.1 geometry box; CSS Images 4 [..]
- Engine files: /home/alex/Downloads/claude/wt/ctbrowser-wpt/ctbrowser/lib/Style/css/properties/grammar.cpp; /home/alex/Downloads/claude/wt/ctbrowser-wpt/ctbrowser/lib/Style/css/properties/declaration.cpp; /home/alex/Downloads/claude/wt/ctbrowser-wpt/ctbrowser/lib/Style/css/properties/table_modules.cpp; /home/alex/Downloads/claude/wt/ctbrowser-wpt/ctbrowser/lib/Style/easing.cpp; [..]
- Evidence: css/css-masking/animations/clip-path-interpolation-002.html 720/720 ("'from' value should be supported expected true got false"); css/css-masking/animations/clip-path-interpolation-shape.html 692/692; css/css-masking/animations/clip-path-interpolation-001.html 414/414; css/css-masking/animations/clip-path-interpolation-shape-control-points.html 306/306; [..]
- Fix: (1) Add the eight shape functions to `value_functions` (grammar.cpp:76) - this alone flips the 'should be supported' gate for 2,746 subtests, after which the pairs fall into the generic list interpolation (component-wise lengths, `at`, keywords). (2) A `match_basic_shape(ts, found, serialized)` in a new properties/shapes.cpp hooked from declaration.cpp beside match_filter_list: per CSS Shapes 1 §5.1 each function's arity and unit rules, `rect()`/`xywh()` kept as written specified but computed to `inset()` (Masking 1 §7.1 / Shapes 1 §5.1.2), `round` radii validated like border-radius, `shape()` commands (Shapes 2 §3), fill-rule `nonzero` dropped in serialisation, `<geometry-box>` after the shape. (3) In easing.cpp computed_shape: canonicalise both endpoints to the same function; interpolate_pair: pair `inset(a b c d round r)` arg by arg, `circle(r at x y)` with `closest-side` keywords [..]

### mask-border-* longhands and the `mask` shorthand are not in the property table

- **1878 subtests / 12 files**, size M, overlaps none, confidence high
- Root cause: table_modules.cpp:55-64 lists mask, mask-image, mask-mode, mask-repeat, mask-position, mask-clip, mask-origin, mask-size, mask-composite, mask-type - no mask-border, mask-border-source, mask-border-slice, mask-border-width, mask-border-outset, mask-border-repeat, mask-border-mode (rg 'mask-border' over ctbrowser/lib and include finds only the `starts_with("mask-")` repeatable-list test in easing.cpp:407). find_property returns nullptr so supports_declaration (declaration.cpp:503) is false for every mask-border-* value, and `e.style['mask-border-outset']` after `mask: none` is undefined. `mask` is a shorthand row (table_modules.cpp:55, is_shorthand=true) but has NO entry in the [..]
- Spec: CSS Masking 1 §7.3 (mask shorthand), §8 (mask-border, §8.1-8.7 longhands, animation types 'by computed value' per component); CSS Backgrounds 3 §6 (border-image, the model to copy)
- Engine files: /home/alex/Downloads/claude/wt/ctbrowser-wpt/ctbrowser/lib/Style/css/properties/table_modules.cpp; /home/alex/Downloads/claude/wt/ctbrowser-wpt/ctbrowser/lib/Style/css/properties/shorthands.cpp; /home/alex/Downloads/claude/wt/ctbrowser-wpt/ctbrowser/lib/Style/css/properties/backgrounds.cpp; /home/alex/Downloads/claude/wt/ctbrowser-wpt/ctbrowser/lib/Style/easing.cpp
- Evidence: css/css-masking/animations/mask-border-width-interpolation.html 558/558 ("'from' value should be supported expected true got false"); css/css-masking/animations/mask-border-slice-interpolation.html 434/434; css/css-masking/animations/mask-border-source-interpolation.html 294/294; css/css-masking/animations/mask-border-outset-interpolation.html 168/168; [..]
- Fix: Add seven rows to table_modules.cpp mirroring border-image-* (table.cpp:156-161): mask-border-source none, mask-border-slice 0 (`<number-percentage>{1,4} fill?`), mask-border-width auto (`[<length-percentage>|<number>|auto]{1,4}`), mask-border-outset 0, mask-border-repeat stretch, mask-border-mode alpha, plus the `mask-border` shorthand with border-image's slash split (shorthands.cpp:312-319 already special-cases border-image's `slice / width / outset`; generalise the name). Add `mask` to the shorthands table as a layer list like `background` (backgrounds.cpp match_background_list already knows mask-* keywords at :306-335): `<mask-layer>#` where each layer is image || position [/ size] || repeat || geometry-box{1,2} || composite || mode, with the canonical order the test prints (image, position/size, repeat, origin, clip, composite, mode), `no-clip` as clip, and the shorthand resets [..]

### 3D transform functions missing from the value-function allow-list; no <transform-list> grammar; no matrix3d computed value

- **1250 subtests / 18 files**, size M, overlaps none, confidence high
- Root cause: grammar.cpp:76-116 `value_functions` names translate, translatex, translatey, translate3d, rotate, scale, scalex, scaley, skew, matrix, matrix3d, perspective - but NOT rotatex, rotatey, rotatez, rotate3d, scalez, scale3d, skewx, skewy, translatez. Any list containing one is an unknown function, CSS.supports(transform, 'rotateX(30deg)') is false, and the harness refuses the subtest (1,190 subtests in 12 files; e.g. transform-interpolation-verify-reftests 176/176, transform-interpolation-001's `skewX(10rad) perspective(400px)` rows). `transform` is k::freeform with no list grammar: transform-invalid accepts `none scale(2)`, `translateX(3%) none`, `matrix(1, 2)`; transform-valid wants [..]
- Spec: CSS Transforms 2 §8 (3D functions), §9 (serialization of computed value: matrix3d), §14 (interpolation of 3D matrices, quaternions, perspective); CSS Transforms 1 §9-10 (list interpolation, matrix [..]
- Engine files: /home/alex/Downloads/claude/wt/ctbrowser-wpt/ctbrowser/lib/Style/css/properties/grammar.cpp; /home/alex/Downloads/claude/wt/ctbrowser-wpt/ctbrowser/lib/Style/css/properties/transforms.cpp; /home/alex/Downloads/claude/wt/ctbrowser-wpt/ctbrowser/lib/Style/css/properties/declaration.cpp; /home/alex/Downloads/claude/wt/ctbrowser-wpt/ctbrowser/lib/Style/easing.cpp; [..]
- Evidence: css/css-transforms/animation/transform-interpolation-001.html 428/448 (83+14*6 rows "'from' value should be supported" for skewX(10rad) perspective(400px); `perspective(none)` rows); css/css-transforms/animation/transform-interpolation-verify-reftests.html 176/176 (rotateX(30deg) unsupported); css/css-transforms/animation/list-interpolation.html 16 rows rotateX(90deg) translateX(100px) [..]
- Fix: S first: add the nine names to `value_functions` - that alone lifts the supports gate (1,190 subtests) and lets the 2D-only paths answer where the list happens to be 2D. Then a `match_transform_list` in transforms.cpp (called from declaration.cpp beside the individual transforms at :358): per function the arity and argument types of CSS Transforms 2 §8 (`scale()` takes <number>|<percentage>, serialised as number; `perspective()` takes <length>|none, unitless only under -webkit-; `none` only alone; `matrix()` exactly 6 numbers, `matrix3d()` 16), refusing the rest. Then the 4x4 path: a `transform_matrix_text` that multiplies 3D functions into a 4x4 and serialises `matrix3d(...)` when any function is 3D (Transforms 2 §9), and in easing.cpp a 3D `decompose`/`recompose` (Transforms 2 §14: translation, scale, skew, perspective, quaternion slerp) used when either list has a 3D function or when [..]

### `inherit` is not resolved as an animation endpoint, and a transition's `initial` endpoint is the table's SPECIFIED keyword, not the computed value

- **906 subtests / 55 files**, size M, overlaps none, confidence medium
- Root cause: ctbrowser/lib/Shell/bindings/animations.cpp:363-372 maps `initial` and a non-inherited `unset` in a keyframe to `known->initial` (landed 3fed860e) but leaves `inherit` as the literal text ('ponytail: inherit needs the parent's computed text, which the underlying callback cannot ask for yet'), so every `from [inherit]` keyframe pair is discrete or serialises 'inherit' (418 subtests / 38 files still fail at HEAD by code inspection). For transitions, ctbrowser/lib/Shell/bindings/animations/css.cpp:201-207 value_on() substitutes `known->initial` when the cascade cleared the keyword (engine.cpp:1363-1373 clears `initial`), and that text is the specified form: `font-size: medium`, [..]
- Spec: CSS Animations 1 §3 (CSS-wide keywords in keyframes are their computed values), Web Animations 1 §5.4.3 (underlying value, neutral keyframe), CSS Transitions 1 §3 (before-change and after-change [..]
- Engine files: /home/alex/Downloads/claude/wt/ctbrowser-wpt/ctbrowser/lib/Shell/bindings/animations.cpp; /home/alex/Downloads/claude/wt/ctbrowser-wpt/ctbrowser/lib/Shell/bindings/animations/css.cpp; /home/alex/Downloads/claude/wt/ctbrowser-wpt/ctbrowser/lib/Style/easing.cpp; /home/alex/Downloads/claude/wt/ctbrowser-wpt/ctbrowser/lib/Style/css/properties/table.cpp
- Evidence: from [inherit] keyframe rows (CSS Animations / Web Animations): 418 subtests in 38 files, e.g. css/css-transforms/animation/scale-interpolation.html (86), translate-interpolation.html (66), css/css-backgrounds/animations/background-position-interpolation.html (40), css/css-fonts/animations/font-stretch-interpolation.html ('from [initial] to [inherit] at (0.3) should be [130%] got [initial]'), [..]
- Fix: animated_values() already receives `underlying(property)`; extend the callback (or pass the element's `inherited` computed_style, which update_css_animations has as `before.inherited`/`after.inherited`) so `inherit` becomes the parent's computed text for that property (the initial value at the root), and `unset` on an inherited property the same. For transitions, route value_on() through the same specified->computed mapping getComputedStyle uses (font-size keyword table xx-small..xxx-large -> px against the medium size, line-width keywords, `normal` line-height -> the used px, font-weight normal->400/bold->700 - see the font computed cluster), i.e. call the computed-style serialiser for the property on `before`/`after` rather than reading the raw declaration; `before` is a full computed_style so the resolver is available. Both are per-property tables that already exist in [..]

### Font value types have no animation shape: system fonts in `font`, font-stretch keywords, font-style oblique angles, font-size-adjust none/number, font-palette palette-mix(), font-variation-settings tag lists

- **884 subtests / 8 files**, size M, overlaps none, confidence high
- Root cause: (a) split_font (shorthands.cpp:375-384) returns split::whole for `caption|icon|menu|message-box|small-caption|status-bar`, so `font: caption` never reaches any longhand - system-fonts.html's `<font-style>` etc. read the untouched value (360 subtests) and font-computed says 'caption should be a supported system font ... got ""'. (b) font-stretch/font-width keywords are stored as keywords (table.cpp:216,221 freeform, initial `100%`/`normal`): easing.cpp computed_shape has no branch mapping `ultra-expanded`->`200%` (Fonts 4 §2.3 table), so `from [100%] to [200%]` pairs but the keyword endpoints and `initial`->`inherit` do not; the -2 extrapolation `-100%` should clamp to `0%` (numeric_text [..]
- Spec: CSS Fonts 4 §2.3 (font-width/stretch keyword-to-percentage mapping), §2.4 (font-style animation as oblique angle), §3.1 (system fonts in `font`), §7.6 (font-variation-settings animation by tag), [..]
- Engine files: /home/alex/Downloads/claude/wt/ctbrowser-wpt/ctbrowser/lib/Style/easing.cpp; /home/alex/Downloads/claude/wt/ctbrowser-wpt/ctbrowser/lib/Style/css/properties/shorthands.cpp; /home/alex/Downloads/claude/wt/ctbrowser-wpt/ctbrowser/lib/Style/css/properties/keywords.cpp; /home/alex/Downloads/claude/wt/ctbrowser-wpt/ctbrowser/lib/Style/css/properties/table.cpp
- Evidence: css/css-fonts/animations/system-fonts.html 360/588 ('from [italic 100 small-caps ultra-expanded 100px / 100px Ahem] to [caption] at (0) should be [italic] for <font-style> got [normal]'); css/css-fonts/animations/font-size-adjust-composition.html 212/500 ('should be [2.5] got [2]'); css/css-fonts/animations/font-stretch-interpolation.html 157/185 ('from [100%] to [200%] at (-2) should be [0%] got [..]
- Fix: In shorthands.cpp split_font: expand a system font keyword into the six longhands with fixed UA values (Fonts 4 §3.1: e.g. caption = normal normal 400 normal 13px/normal system-ui; the test only needs the SAME values on both sides, so any fixed table works) instead of split::whole. In easing.cpp computed_shape: `font-stretch`/`font-width` keyword -> percentage table (ultra-condensed 50% ... ultra-expanded 200%), `font-style: normal` -> `oblique 0deg`, `italic` discrete; give font-stretch/font-size-adjust `nonnegative` in the table so extrapolation clamps at 0. Add a `font-variation-settings`/`font-feature-settings` branch: parse `<string> <number>` pairs into a map keyed by tag, pair by key, serialise sorted as written with double quotes; discrete when key sets differ. `font-palette`: any two palette idents interpolate to `palette-mix(in oklab, A, B p%)` text (no rendering needed). [..]

### Font computed values: font-weight keywords never become numbers, font-width keywords never become percentages, font-synthesis/feature-settings/kerning rows missing, `font` shorthand computed reads empty

- **514 subtests / 8 files**, size M, overlaps none, confidence high
- Root cause: table.cpp:214 declares font-weight `k::number` with keywords `normal bold bolder lighter` and initial `400`; nothing maps `normal`->400, `bold`->700, `bolder`/`lighter` relative to the parent's weight (Fonts 4 §2.2 table), and calc results are not clamped to [1,1000] (`calc(0)` should compute `1`, got `0`); the only readers are paint's bold flags (engine.cpp:344, 2457). font-width/font-stretch (table.cpp:216,221 freeform) keep keywords: `ultra-condensed` should compute `50%`. table.cpp:227 says 'NOT HERE YET: font-feature-settings, font-palette, font-size-adjust, font-variant-alternates, font-variation-settings and font-width' - font-synthesis (and -weight/-style/-small-caps), font-kerning, [..]
- Spec: CSS Fonts 4 §2.2 (font-weight computed value, bolder/lighter table, range), §2.3 (font-width percentages), §2.4, §3.1, §6.3 (font-variant), §6.10 (font-synthesis), §7.1 (font-kerning), §7.4 [..]
- Engine files: /home/alex/Downloads/claude/wt/ctbrowser-wpt/ctbrowser/lib/Style/css/properties/table.cpp; /home/alex/Downloads/claude/wt/ctbrowser-wpt/ctbrowser/lib/Style/css/properties/keywords.cpp; /home/alex/Downloads/claude/wt/ctbrowser-wpt/ctbrowser/lib/Style/engine.cpp; /home/alex/Downloads/claude/wt/ctbrowser-wpt/ctbrowser/lib/Shell/bindings/computed_style/serialize.cpp; [..]
- Evidence: css/css-fonts/parsing/font-computed.html 315/315 ('Property font value xx-small serif expected "40px serif" but got ""'; 'caption should be a supported system font'); css/css-fonts/parsing/font-weight-computed.html 43/58 ('normal' expected "400" got "normal"; 'calc(0)' expected "1" got "0"; '100 made bolder computes to 400' got "bolder"); css/css-fonts/parsing/font-width-computed.html 22/30 [..]
- Fix: Compute font-weight in the cascade (engine.cpp where inherited font values are resolved, near :343): normal->400, bold->700, bolder/lighter from the parent's computed weight by the Fonts 4 §2.2 table, clamp [1,1000]; serialise as a number. font-width/font-stretch: keyword->percentage at computed time (share the table with the interpolation cluster), keep `font-stretch` as the legacy alias. Add table rows: font-synthesis (+ -weight/-style/-small-caps/-position, inherited, `none | weight || style || small-caps || position`), font-kerning (auto normal none, inherited), font-language-override (normal | <string>, inherited), font-feature-settings (normal | <string> <integer>?#, inherited, double-quoted serialisation), font-variant as a shorthand whose `normal`/`none` cannot combine. Then `font`'s computed read-back (already written in 7a4c7feb) has canonical longhands to print.

### :has() matching goes stale or misses after DOM mutation and form-state changes

- **251 subtests / 20 files**, size M, overlaps FLAT, confidence low
- Root cause: engine.cpp:1147-1173 answers `:has()` by running each argument as a scoped query on a second engine (`has_walker_`, built once and given `states_source_` at construction) anchored at the subject, or at the subject's parent when the argument's last combinator is a sibling one. The walker's root-bounded traversal (engine.cpp:705-716: 'ONLY THE PATH TO THE ROOT IS WALKED ABOVE IT') visits earlier siblings only at levels on the path, so an argument whose inner `:is()`/`:not()` reaches sideways or upward from a descendant (`#descendant:is(.p + .c_has_scope ~ .d .e)`) or that depends on siblings inserted/removed OUTSIDE the anchor's subtree answers false; the failing rows are exactly the [..]
- Spec: Selectors 4 §4.5 (:has() relative selectors, :scope anchoring), §3.4 (:is/:where forgiving), CSS Scoping 1 (host); HTML §4.16.2 pseudo-class state changes
- Engine files: /home/alex/Downloads/claude/wt/ctbrowser-wpt/ctbrowser/lib/Style/engine.cpp; /home/alex/Downloads/claude/wt/ctbrowser-wpt/ctbrowser/lib/Style/css/selector.cpp; /home/alex/Downloads/claude/wt/ctbrowser-wpt/ctbrowser/lib/Shell/bindings/document/as_node.cpp
- Evidence: css/selectors/invalidation/is-pseudo-containing-complex-in-has.html 48/476 ('insert/remove .invalid before #parent_previous : (removal) check matches (true) expected true but got false'); css/selectors/invalidation/not-pseudo-containing-complex-in-has.html 48/460; css/selectors/invalidation/has-in-ancestor-position.html 25/337, has-in-sibling-position.html 25/295, has-in-adjacent-position.html [..]
- Fix: Two candidate fixes; measure first. (1) Anchor correctly: for a `:has()` argument, run the relative selector with the SUBJECT as `:scope` but let the walker's matching of inner complex selectors (`:is(.p + .c_has_scope ~ .d .e)`) walk the real ancestors/siblings of the candidate, i.e. seed levels_ for the whole path to the document root rather than to `from`, and for `~`/`+` leading combinators start from the parent's first child. (2) Make the walker share the primary engine's per-node facts/state cache rather than its own, and reset it on every mutation generation (as_node.cpp:197 'mutated() makes every one of those a restyle' - confirm the walker is invalidated too). For has-complexity, cache per (subject, argument) answers per style pass. Form-state pseudo-classes: ensure the shell recomputes :checked/:indeterminate/:valid/:placeholder-shown/:default/:required/:out-of-range bits on [..]

### Shorthand splits that leave their longhands empty: background: none, border-image, transition (+ transition-behavior), outline canonical order

- **205 subtests / 9 files**, size M, overlaps none, confidence medium
- Root cause: The shorthands table (shorthands.cpp) has background/border-image/transition/outline rows but their splitters fail on the tested forms: `background: none` (and the full layer syntax) yields no longhands (background-valid 45/46: 'background-attachment should be canonical expected "scroll" but got ""'); `border-image: url() 1 2 3 4 fill` sets none of the five longhands (border-image-shorthand 21/30) and border-image-valid/-invalid have no per-longhand grammar (number-percentage{1,4} fill?, width with auto/number, outset, repeat pair); `transition: 1s -3s cubic-bezier(...) top` sets nothing and `allow-discrete` is not parsed or placed last (transition-shorthand 18/18, transition-behavior [..]
- Spec: CSS Backgrounds 3 §3.11 (background shorthand), §6.6 (border-image), CSS Transitions 2 §2.6/§2.7 (transition shorthand, transition-behavior, serialization order), CSS UI 4 §4.4 (outline shorthand, [..]
- Engine files: /home/alex/Downloads/claude/wt/ctbrowser-wpt/ctbrowser/lib/Style/css/properties/shorthands.cpp; /home/alex/Downloads/claude/wt/ctbrowser-wpt/ctbrowser/lib/Style/css/properties/backgrounds.cpp; /home/alex/Downloads/claude/wt/ctbrowser-wpt/ctbrowser/lib/Style/css/properties/table.cpp; /home/alex/Downloads/claude/wt/ctbrowser-wpt/ctbrowser/lib/Shell/bindings/animations/css.cpp
- Evidence: css/css-backgrounds/parsing/background-valid.html 45/46, background-shorthand-serialization.html 9/11; css/css-backgrounds/parsing/border-image-shorthand.sub.html 21/30, border-image-valid.html 16/30, border-image-invalid.html 11/17 (+ -slice/-width/-outset-invalid 6+5+5); css/css-transitions/parsing/transition-shorthand.html 18/18, transition-behavior.html 23/28, transition-computed.html 10/10, [..]
- Fix: background: split `none`/each layer into the eight longhands (`background: none` -> image none, position 0% 0%, size auto, repeat repeat, attachment scroll, origin padding-box, clip border-box, color transparent for the last layer only), comma layers pairing per longhand list. border-image: a slash-aware splitter `<source> || <slice> [/ <width>]? [/ <outset>]? || <repeat>` with the per-longhand grammars (slice numbers/percentages{1,4} + fill; width length-percentage|number|auto{1,4}; outset length|number{1,4}; repeat keyword{1,2}), refusing negatives and 5+ values. transition: per item `<property> || <duration> || <easing> || <delay> || allow-discrete|normal`, the first <time> is duration, serialise `property duration easing delay [allow-discrete]`; read_shorthand in animations/css.cpp:114 already reads the shorthand text - keep the two in one place. outline: computed order `width style [..]
