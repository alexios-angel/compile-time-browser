*.o
*.d
build/
.claude
*.pch
*.gch
*.pch.tmp
tests/*
!tests/*.cpp
!tests/CMakeLists.txt
tools/clang-std-embed/
examples/pong.inc
*.log
# Screenshots are build output - EXCEPT the goldens, which are the test data a
# byte comparison compares against. Ignored, they are missing from a fresh
# clone and every golden test fails with "no golden" on a machine that has
# never generated one.
*.ppm
!tests/golden/*.ppm
examples-windows/
examples-windows/
tools/llvm-mingw/
build*
